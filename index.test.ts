import { expect, test } from "bun:test";

test("scaffold sanity — bootstrap commit", () => {
  // Placeholder so `bun test` exits 0 on a fresh scaffold.
  // Real command tests land with US-005 (`ralph init`) and onward.
  expect(1 + 1).toBe(2);
});
