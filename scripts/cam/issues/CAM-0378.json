{
  "id": "CAM-378",
  "title": "Composite suggestion promotion + canonize the demand-driven pen-triage policy",
  "stage": "specified",
  "status": "open",
  "blockedBy": [],
  "createdAt": "2026-07-20T16:30:55.028Z",
  "updatedAt": "2026-07-21T05:56:10.767Z",
  "description": "Operator decision 2026-07-20 (Eduardo): the suggestion pen is triaged by TWO rules that compose, neither replacing the other.\n\n(1) PROMOCAO SOB DEMANDA: a pen entry is not promoted eagerly on sight. It stays in scripts/cam/suggestions.jsonl (which is durable, committed to main, so nothing is lost) and is promoted only when a cycle is about to touch the surface it names. This makes the backlog count reflect only work we intend to do, and it makes the grouping decision accurate by construction, since at promotion time the target surface is already known.\n\n(2) COMPOSICAO POR SITIO DE CORRECAO: when promoting, pen entries that share a fix site (same file, same rule, one coherent edit) become ONE issue with composite scope. Entries that merely share a theme, with independent fix sites that could ship in different cycles, stay separate. Shared fate is the underlying test; fix site is the proxy for it.\n\nWHY THIS NEEDS CODE, NOT JUST A DOC. Measured 2026-07-20: `cam suggestions promote` accepts exactly ONE fingerprint (src/commands/suggestions.ts:533, CLI usage line `promote <fingerprint>`). There is no composite path. Composing by hand today means promoting fp1 and dismissing fp2/fp3 -- but terminal-review dedup keys on the `suggestion-fingerprint: <fp>` line embedded in a filed issue's description (suggestions.ts:498), and `dismiss` removes the pen line WITHOUT filing anything. So every hand-composed promotion silently re-arms fp2/fp3 to be re-filed by the next terminal review. Manual composition is not merely tedious, it REGRESSES dedup.\n\nSCOPE:\n- `cam suggestions promote <fp1> [<fp2> ...]`: file ONE issue whose description carries a `suggestion-fingerprint:` line for EVERY promoted fingerprint, so dedup holds for all of them, and remove all promoted lines from the pen in ONE atomic on-main commit (preserving the existing commit-tree-to-main pattern and byte-preserving every non-matching pen line). Single-fingerprint invocation must remain byte-identical in behavior to today.\n- Composite description must preserve each entry's derivedFrom/sourceIssue provenance, not just the first one's.\n- Unknown fingerprint anywhere in the list: error, exit non-zero, mutate NOTHING (all-or-nothing, matching today's single-fp guarantee).\n- Canonize the policy in memory/ as a decision file (per the knowledge-layer routing table in scripts/cam/CLAUDE.md: cross-cutting decision / stakeholder policy -> memory/project_<topic>.md).\n- Reflect the operative rule in .claude/agents/subagent-orchestrator.md where the orchestrator actually consults it at triage time. CHECK THE DUAL-COPY: .claude/ and templates/ are NOT uniformly byte-identical; diff before assuming an edit forces a re-embed, and run `bun run embed-vendor:check` if templates/ is touched.\n\nNOT AN ADR: this is a triage policy, cheaply reversible, so it fails the ADR hard-to-reverse gate. memory/ is the correct channel.\n\nCONTEXT (measured, for whoever specs this): 43 promotions vs 35 dismissals historically, and 26 of 41 promoted issues (63%) already shipped -- the promotion filter is well calibrated and this issue must NOT tighten it. Intake runs ~2.9 issues/cycle against ~1.4 shipped/cycle; this issue targets the SHAPE of filed issues, not the acceptance bar.\n\nNEEDS REBUILD: touches src/, which the sidecar executes.",
  "spec": {
    "scope": "Extend `cam suggestions promote` from a single fingerprint to a variadic list: `cam suggestions promote <fp1> [<fp2> ...]` files exactly ONE backlog issue whose description carries a `suggestion-fingerprint: <fp>` line for EVERY promoted fingerprint (so terminal-review dedup holds for all of them), and removes all promoted lines from scripts/cam/suggestions.jsonl in ONE atomic on-main commit, byte-preserving every non-promoted pen line. The composite issue description must merge each promoted entry's derivedFrom/sourceIssue provenance, not only the first. Single-fingerprint invocation stays byte-identical to today. Any unknown fingerprint in the list => non-zero exit, mutate nothing (all-or-nothing). Canonize the two-rule triage policy (demand-driven promotion + composition-by-fix-site) as a memory/ decision file per the knowledge-layer routing table in scripts/cam/CLAUDE.md, and reflect the operative composition rule in .claude/agents/subagent-orchestrator.md where the orchestrator consults it at triage time. This is a triage policy (cheaply reversible) => memory/, NOT an ADR. Does NOT tighten the promotion filter (well-calibrated: 63% of promoted issues shipped); targets only the SHAPE of filed issues.",
    "acceptanceCriteria": [
      "`cam suggestions promote` accepts two or more fingerprint arguments and files exactly ONE backlog issue whose description carries a `suggestion-fingerprint: <fp>` line for each promoted fingerprint. A new test named exactly 'promote composes multiple fingerprints into one issue' asserts both fingerprint lines exist in the single filed issue. [oracle: grep -q 'promote composes multiple fingerprints into one issue' test/commands/suggestions.test.ts]",
      "All promoted pen lines are removed from scripts/cam/suggestions.jsonl in one atomic on-main commit while every non-promoted line is byte-preserved, and the composite issue description preserves each promoted entry's derivedFrom/sourceIssue provenance, not only the first. [oracle: reviewer-judgment]",
      "Single-fingerprint `cam suggestions promote <fp>` behavior is byte-identical to today: one issue, one fingerprint line, exactly that one pen line removed. [oracle: reviewer-judgment]",
      "An unknown fingerprint anywhere in the argument list causes a non-zero exit and mutates nothing (no issue filed, pen file unchanged): all-or-nothing, matching today's single-fp guarantee. A new test named exactly 'promote with an unknown fingerprint mutates nothing' asserts it. [oracle: grep -q 'promote with an unknown fingerprint mutates nothing' test/commands/suggestions.test.ts]",
      "Quality gates green. [oracle: bun run typecheck && bun test test/commands/suggestions.test.ts]",
      "The triage policy (demand-driven promotion + composition-by-fix-site) is canonized as a memory/ decision file per the knowledge-layer routing table. [oracle: file-assert test -f memory/project_suggestion_triage.md]",
      "The operative rule is reflected in .claude/agents/subagent-orchestrator.md under the policy name 'composition-by-fix-site' where the orchestrator consults it at triage time. [oracle: grep -q 'composition-by-fix-site' .claude/agents/subagent-orchestrator.md]",
      "The .claude/ vs templates/ copies of subagent-orchestrator.md are diffed first and NOT reconciled (they diverge by deliberate specialization); embed-vendor:check is run only if templates/ was touched. [oracle: reviewer-judgment]"
    ],
    "gotchas": [
      "ROOT REGRESSION this fixes: `dismiss` removes a pen line WITHOUT filing anything, and terminal-review dedup keys on the `suggestion-fingerprint:` line embedded in a filed issue's description (src/commands/suggestions.ts ~line 498). So hand-composing today (promote fp1, dismiss fp2/fp3) silently RE-ARMS fp2/fp3 for re-filing by the next terminal review. The composite path exists to make composition dedup-safe.",
      "Single-fp invocation MUST remain byte-identical in behavior (commit message shape included: `chore(cam): suggestions promote <fp> -> <id>`). Do not regress the existing single-fingerprint guarantee or its tests.",
      "All-or-nothing: validate EVERY fingerprint exists BEFORE mutating anything; an unknown fp anywhere => error, non-zero exit, zero mutation (no issue file written, pen untouched).",
      "Provenance: merge derivedFrom/sourceIssue for EVERY promoted entry into the composite issue description, not just the first entry's.",
      "The CLI arg-parse/dispatcher for the `promote` subcommand is SEPARATE from the promoteSuggestionOnMain library fn (function def is ~src/commands/suggestions.ts:533; the variadic arg wiring lives in the CLI dispatcher, re-locate it, do not trust drifted line numbers).",
      "Preserve the existing commit-tree-to-main atomic pattern for the pen edit + issue file in one commit (checkMainUpToDate guard, single commit).",
      "DUAL-COPY TRAP: .claude/agents/subagent-orchestrator.md and templates/agents/subagent-orchestrator.md are NOT byte-identical (they diverge by deliberate cam-cli specialization). The edit is ADDITIVE (append the composition rule), NEVER a `.claude/`<->`templates/` reconciliation. Diff before assuming an edit forces a re-embed; run `bun run embed-vendor:check` ONLY if templates/ was actually touched.",
      "memory/ directory does not exist yet: create it along with memory/project_suggestion_triage.md.",
      "src/commands/suggestions.ts is imported by the sidecar (src/commands/sidecar.ts) => sidecar-executed; a coherence rebuild is required after merge (orchestrator handles this)."
    ],
    "domainTerms": [
      "suggestion pen: scripts/cam/suggestions.jsonl, the durable committed holding area for reviewer SUGGESTION findings after a terminal review",
      "demand-driven promotion: a pen entry is promoted to a backlog issue only when a cycle is about to touch the surface it names, not eagerly on sight",
      "composition-by-fix-site: pen entries sharing a fix site (same file, same rule, one coherent edit) are promoted into ONE composite issue; entries that merely share a theme stay separate (shared fate is the test, fix site the proxy)",
      "fingerprint: the dedup key embedded as a `suggestion-fingerprint: <fp>` line in a filed issue's description"
    ]
  },
  "wsjf": {
    "value": 4,
    "timeCriticality": 2,
    "riskReduction": 4,
    "jobSize": 3
  },
  "type": "feat"
}
