{
  "id": "CAM-357",
  "title": "The drift guard resolves `actual = readPhaseModel(phase, repoConfigPath)` witho…",
  "stage": "idea",
  "status": "open",
  "blockedBy": [],
  "createdAt": "2026-07-19T17:37:16.424Z",
  "updatedAt": "2026-07-19T17:37:16.424Z",
  "description": "The drift guard resolves `actual = readPhaseModel(phase, repoConfigPath)` without threading the resolved backend, then for a non-claude phase asserts isClaudeAliasModel(actual) is false. Since the committed config is all-claude this is correct today, but if a future codex phase set only a nested [models.codex] key (no flat fallback), the un-threaded read would return the claude DEFAULT (opus) and the guard would falsely redden. Consider threading `backend` into the readPhaseModel call so the drift guard validates the spawn-accurate model. Non-blocking; matches the AC as written.\n\nSource: cam/issue-356\nReview round: 1\n\nsuggestion-fingerprint: 66d2e2a5927d",
  "derivedFrom": [
    "CAM-356"
  ]
}
