{
  "id": "CAM-534",
  "title": "templates/ e .claude/ colapsam em fonte unica: symlink para os pares identicos, overlay por ancora exata para os divergentes",
  "stage": "shipped",
  "status": "open",
  "blockedBy": [],
  "createdAt": "2026-08-11T07:34:21.361Z",
  "updatedAt": "2026-08-11T12:18:04.450Z",
  "description": "PROBLEMA\n\ntemplates/ e as copias instaladas (.claude/, scripts/cam/) sao dois conjuntos de documentos mantidos a mao, sem nenhum gate que reprove quando divergem. A consequencia nao e hipotetica: ja ha drift bidirecional em producao, e ele ja cegou o proprio orquestrador para um marcador de falha vivo.\n\nMEDICAO (2026-08-10, varredura read-only)\n\n27 arquivos em templates/, 27 pares mapeados. 15 byte-identicos, 12 divergentes, 0 template-only.\nDos 12 divergentes, 3 sao semente-contra-vivo e estao corretos (journal.md, patterns.md, suggestions.jsonl). Os outros 9 sao pares de documento que deveriam concordar.\n\nNAO EXISTE GATE DE DRIFT. embed-vendor:check compara apenas templates/ contra src/vendor/_generated.ts (scripts/generate-embedded-vendor.ts:137-154 le somente VENDOR_DIR e TEMPLATES_DIR). Ele e estruturalmente incapaz de comparar repo contra template. Prova: sai 0 hoje, com 12 pares divergentes. A unica enforcement existente e uma linha de checklist em .claude/agents/subagent-reviewer.md, ou seja, um checkbox de LLM. O drift observado prova que o checkbox nao segura.\n\nDRIFT REAL JA EM PRODUCAO\n\n1. Template a frente do repo. templates/agents/subagent-orchestrator.md:152 tem o item 12 do boot, ler .claude/.cam-sidecar-stalled.json, com o paragrafo de blocker em :250. A copia do repo tem zero ocorrencias e renumera para \"(7-12)\". O commit eb955511 (CAM-207) tocou apenas o caminho do template. O marcador e codigo vivo: src/supervisor/sidecar-stalled.ts, src/commands/sidecar-liveness-watch.ts:9, src/commands/status.ts:324. Efeito medido: a persona do orquestrador deste repo boota cega para um marcador de falha real.\n\n2. Repo a frente do template. .claude/agents/subagent-orchestrator.md:119 documenta a forma variadica de gship suggestions promote, e a triagem em memory/project_suggestion_triage.md. O template ainda documenta a forma de fingerprint unico. O commit 46a4180c (CAM-378) tocou o repo, index.ts e src/commands/suggestions.ts, e nao tocou templates/. O CLI shipado e variadico, entao o template mis-documenta comportamento vivo.\n\n3. Schemas, drift nos dois sentidos. scripts/cam/handoff.schema.json tem 3 propriedades que o template nao tem (appliedPatternIds, channel, localConventionConflict); projetos downstream recebem um schema que ignora campos que o implementer shipado escreve. Em sentido inverso, scripts/cam/issues.schema.json:4 e scripts/cam/orch-handoff.schema.json:4 e :46 ainda dizem cam onde o template ja diz gship: as copias do repo perderam o rename do produto.\n\n4. Auto-refutacao. scripts/cam/CLAUDE.md do repo ganhou a secao Domain Model Convention, que afirma textualmente valer \"para qualquer projeto downstream inicializado pelo gship init\". Essa secao esta ausente do arquivo que o gship init entrega. O mesmo vale para a secao Knowledge-Layer Routing, tambem generica e tambem ausente do template.\n\n5. Inventario re-enumerado a mao em 3 lugares, um ja stale. src/commands/setup.ts:386-388 informa ao agente de setup que os comandos instalados sao 6, omitindo cam-spec.md, que e o setimo shipado. Alem disso o array targets em setup.ts:359-364 lista 4 subtrees enquanto SUBTREES em src/templates/embedded.ts:30 tem 5.\n\nPOR QUE A RESPOSTA NAO E ADICIONAR UM GATE DE DRIFT\n\nAdicionar um gate que vigia duas copias mantidas a mao aumenta a complexidade que causou o problema. Com fonte unica, o gate deixa de precisar existir: nao ha o que divergir. Isso e a premissa do produto (root CLAUDE.md, secao Premise, commit 7f6e85aa) aplicada a propria oficina: encolher a superficie primeiro, policiar apenas o que sobra.\n\nESCOPO PROPOSTO\n\na) Colapsar em fonte unica os 15 pares hoje byte-identicos: 7 comandos (cam-issue, cam-next, cam-plan, cam-prune, cam-review, cam-ship, cam-spec), 5 skills, subagent-planner.md, o hook orch-agent-allowlist.sh e settings.json. Duas copias a mao dos mesmos bytes e risco puro sem beneficio.\n\nb) Colapsar os 3 schemas (handoff, issues, orch-handoff) apos reconciliar as 19 linhas de divergencia, que sao drift puro. Descrevem um unico formato de wire compartilhado entre harness e projetos downstream; nao ha razao legitima para duas versoes. Com (a), 18 dos 27 arquivos passam a ter fonte unica.\n\nc) Os 9 pares que precisam mesmo de duas versoes (4 personas de agente mais scripts/cam/CLAUDE.md, mais .gitignore) devem virar base compartilhada mais overlay do projeto, nao dois documentos mantidos a mao. A copia do repo nomeia bun run typecheck, Ink, tmux; o template precisa ser agnostico de stack. Com base mais overlay, melhoria generica (a regra de Read antes de Write, a tabela de roteamento, o item 12 do boot) cai nos dois por construcao.\n\nd) Reconciliar os defeitos vivos listados acima como parte do colapso, porque colapsar exige decidir qual lado vence em cada divergencia.\n\nDECISAO DE DESIGN QUE A SPEC PRECISA TOMAR\n\nQual mecanismo de fonte unica: geracao a partir de um diretorio canonico, includes por marcador, ou overlay declarativo. O prior art da industria e fonte unica mais geracao mais gate de drift no padrao do cruft check --exit-code; cookiecutter nunca resolveu isso (issue 784 aberta ha anos); copier e o unico scaffolder com update bidirecional de primeira classe. Nosso espelho mantido a mao e o padrao minoritario.\n\nRestricao dura: templates/ alimenta o codegen scripts/generate-embedded-vendor.ts, cuja saida src/vendor/_generated.ts (199100 bytes) e commitada e embarcada no binario por bun build --compile. Qualquer mecanismo novo precisa manter esse caminho intacto e o embed-vendor:check verde.\n\nFORA DE ESCOPO\n\nO gate agents-md e a corretude semantica da superficie de instrucao ficam no CAM-529. Este issue trata da duplicacao estrutural; aquele trata do que resta depois dela.\n\n---\n\nSUPERSESSAO DO SPEC DO CAM-530\n\nEste issue carrega o spec corrigido. O CAM-530 permanece como o registro da medicao original e da decisao de mecanismo; ele nao e replanejavel, porque o spec dele foi reprovado pelo audit e `gship spec --persist` recusa emenda em issue ja `stage:specified` (guard wrong-stage). A falta de caminho de emenda esta filada a parte.\n\nO que o audit do CAM-530 reprovou, em 2 rounds, e o que foi corrigido aqui:\n\nF-01 CRITICAL. Nenhum criterio verificava se o proprio Claude Code ainda DESCOBRE a superficie symlinkada. O PRD tinha medido essa exata classe de armadilha para o proprio scanner e nao aplicou a pergunta ao consumidor que importa. Corrigido com medicao: as cinco superficies de `.claude/` sao explicitamente symlink-aware (ripgrep com --follow incondicional para commands e agents, filtro isDirectory ou isSymbolicLink para skills, realpathSync para settings.json, string de comando sem stat gate para hooks), e `Bun.Glob` nao aparece nenhuma vez nesse caminho. Novo criterio prova a descoberta por diferencial, com --follow contra sem --follow, que nasce vermelho porque o diferencial e zero enquanto nao houver symlink.\n\nF-02 CRITICAL. O criterio de aridade contra base pristina era o unico do PRD sem varredura vermelha, e o oraculo era `bun test` pelado, verde na main. Corrigido: o oraculo aponta para o arquivo de teste especifico, e o criterio exige as duas fixtures que discriminam nas duas direcoes (cascade pega gerador sequencial, multiply pega contagem sobre buffer mutado).\n\nF-03 IMPORTANT. O criterio de wiring dos gates era tautologico: verificava que o teste fixa o que o array GATES ja contem, e continuava verde com zero gates novos. Corrigido com assercao de presenca por nome mais varredura de violacao plantada em worktree descartavel.\n\nF-04 IMPORTANT. O filtro de fidelidade do scripts/cam/CLAUDE.md era granular por linha sobre paragrafos com quebra rigida, e duas linhas exigidas verbatim eram a primeira metade de frases cuja continuacao um criterio irmao proibia no template. Satisfazer os dois shiparia frases truncadas no artefato que todo projeto downstream recebe. Corrigido para granularidade de paragrafo, com classifyBump somando-se ao conjunto de tokens repo-only.\n\nF-05, F-06, F-07 SUGGESTION. Comparando de fidelidade para os dois blocos de base autorados, criterio auto-contraditorio na colapso do teste de boot do orquestrador, e divisao do manifesto do reviewer em story propria. Todos incorporados como gotchas.\n",
  "specSource": "derived",
  "derivedFrom": [
    "CAM-530"
  ],
  "spec": {
    "scope": "Collapse the hand-maintained duplication between templates/ and the repo's own installed copies (.claude/, scripts/cam/) into a single source, in four verification-isolated stories: (US-001) reconcile all pure drift with the winning side decided per hunk BY THIS SPEC, which also graduates subagent-orchestrator.md to byte-identical; (US-002) collapse the 19 identical pairs to git-tracked symlinks plus a bidirectional integrity gate that also fences the three seed-vs-live files against ever being linked; (US-003) derive the four genuinely-divergent documents from templates/ base plus an exact-anchor {find, replace} overlay, with a --check gate that fails closed on anchor miss or ambiguity; (US-004) derive the installed-file inventory in setup.ts and INIT_HELP from the embedded template map instead of the four parallel hand-enumerations, two of which are already stale. OUT OF SCOPE, filed separately: installer write-policy data loss (CAM-531), the false nested-CLAUDE.md auto-load claim in five personas (CAM-532), and generifying the 22 stack-leaking lines in shared template text (CAM-533). The .gitignore pair keeps its existing behavioral parity gate; only the missing .env.*.local pattern is reconciled.",
    "acceptanceCriteria": [
      "US-001 | The repo-wins hunk (variadic `gship suggestions promote <fp> [<fp> ...]` + the 2026-07-20 triage-policy paragraph) lands verbatim in the template copy. Species: CHANGE-DETECTION. Oracle (the empty-anchor guard is mandatory: with a typo'd anchor both sides are empty and a bare diff returns 0, a false green):\nH1='/pending entries), `gship suggestions promote/,/^6\\. `git status`/'\nW=$(git show main:.claude/agents/subagent-orchestrator.md | sed -n \"${H1}p\")\n[ -n \"$W\" ] || { echo \"ORACLE BROKEN: anchor matched nothing on the winning side\"; exit 2; }\nprintf '%s\\n' \"$W\" | diff - <(sed -n \"${H1}p\" templates/agents/subagent-orchestrator.md)\nSwept RED on main 2026-08-11: exit 1 (winning side 15 lines, template side 5).",
      "US-001 | The template-wins hunks (boot item 12 for `.claude/.cam-sidecar-stalled.json`, plus its blocker-line paragraph) land verbatim in the repo copy, including the renumber back to 13 and the \"(7-13)\" shared-rule range. Species: CHANGE-DETECTION. Oracle:\nH2='/^12\\. `\\.claude\\/\\.cam-sidecar-stalled\\.json`/,/^\\*\\*Shared rule for every boot-surfaced marker/'\nH3='/^If `\\.claude\\/\\.cam-sidecar-stalled\\.json` is present/,/^If `\\.claude\\/\\.cam-gate\\.json` is present/'\nrc=0\nfor H in \"$H2\" \"$H3\"; do\n  W=$(git show main:templates/agents/subagent-orchestrator.md | sed -n \"${H}p\")\n  [ -n \"$W\" ] || { echo \"ORACLE BROKEN: anchor matched nothing on the winning side\"; exit 2; }\n  printf '%s\\n' \"$W\" | diff - <(sed -n \"${H}p\" .claude/agents/subagent-orchestrator.md) || rc=1\ndone\nexit $rc\nSwept RED on main 2026-08-11: exit 1 (winning side 13 + 11 lines, repo side 0 + 0).",
      "US-001 | After both directions land, the `subagent-orchestrator.md` pair is byte-identical, which is what graduates it into the symlink population. Species: CHANGE-DETECTION. Oracle:\ncmp -s .claude/agents/subagent-orchestrator.md templates/agents/subagent-orchestrator.md\nSwept RED on main 2026-08-11: exit 1.",
      "US-001 | The template is not rewritten anywhere outside the promote/triage hunk (no collateral edit rides along). Species: INVARIANCE PIN, comparand derived at check time from `git show main:templates/agents/subagent-orchestrator.md`. Oracle:\nH1='/pending entries), `gship suggestions promote/,/^6\\. `git status`/'\ndiff <(git show main:templates/agents/subagent-orchestrator.md | sed -e \"${H1}d\") <(sed -e \"${H1}d\" templates/agents/subagent-orchestrator.md)\nVerified GREEN on main 2026-08-11: exit 0.",
      "US-001 | `handoff.schema.json`: the template gains the repo's three extra properties and the pair becomes byte-identical. The property names are never written into the oracle, they are derived. Species: CHANGE-DETECTION. Oracle:\ndiff <(git show main:scripts/cam/handoff.schema.json | jq -S .) <(jq -S . templates/scripts/cam/handoff.schema.json) && cmp -s scripts/cam/handoff.schema.json templates/scripts/cam/handoff.schema.json\nSwept RED on main 2026-08-11: exit 1. Auditable listing (`comm -23` over `jq -r '[paths|join(\".\")]'` of the two main blobs) yields exactly properties.appliedPatternIds, properties.officialDocsValidated.items.properties.channel, properties.officialDocsValidated.items.properties.localConventionConflict.",
      "US-001 | `issues.schema.json` and `orch-handoff.schema.json`: the repo copies gain the cam->gship rename (template wins) and both pairs become byte-identical. Species: CHANGE-DETECTION. Oracle:\nrc=0\nfor f in issues.schema.json orch-handoff.schema.json; do\n  diff <(git show main:templates/scripts/cam/\"$f\") scripts/cam/\"$f\" || rc=1\n  cmp -s scripts/cam/\"$f\" templates/scripts/cam/\"$f\" || rc=1\ndone\nexit $rc\nSwept RED on main 2026-08-11: exit 1.",
      "US-001 | Every non-blank line of the generic `scripts/cam/CLAUDE.md` sections is present VERBATIM in the template, with the exclusion taken at PARAGRAPH granularity, never at line granularity. Evidence-fidelity oracle, not token-presence: the comparand is the live line set read out of main, so replacing a quoted claim with plausible invented prose makes that line missing and turns it red. Species: CHANGE-DETECTION (fidelity-grade). Oracle:\nM=$({ git show main:scripts/cam/CLAUDE.md | sed -n '/^## Knowledge-Layer Routing$/,/^## Cross-Repo PRDs/p'\n      git show main:scripts/cam/CLAUDE.md | sed -n '/^## Release Conventions$/,$p' | awk 'BEGIN{RS=\"\";ORS=\"\\n\\n\"} !/computeNextVersion|classifyBump|recovery-runbook/'\n      git show main:scripts/cam/CLAUDE.md | grep -F -e '- **Test quality**:'; } | grep -vE '^[[:space:]]*$' | grep -vF -e 'ADR-0059' -e 'lessons.archive.md')\n[ -n \"$M\" ] || { echo \"ORACLE BROKEN: derivation produced zero lines\"; exit 2; }\nprintf '%s\\n' \"$M\" | grep -Fxvf templates/scripts/cam/CLAUDE.md && exit 1\nexit 0\nWHY THE awk PARAGRAPH FILTER IS MANDATORY (audit finding F-04, reproduced): the previous line-granular form required two lines verbatim in the template that are the FIRST HALVES of sentences whose continuations a sibling criterion forbids there (\"...a 1.0.0 graduation requires a manual operator edit of\" -> `src/version.ts`, and \"Recovery scenarios for misfired bumps, tag drift, re-tagging, and the manual\" -> `docs/recovery-runbook.md`). Satisfying both criteria at once would ship TRUNCATED SENTENCES into the artifact every downstream project receives. The awk filter drops the whole 0.x-demotion and recovery-scenarios paragraphs as units, so no sentence can end mid-clause. `classifyBump` joins `computeNextVersion` in the repo-only token set for the same reason. The implementer must re-run this oracle and report the resulting line count; the pre-fix derivation produced 39 non-blank lines with 38 missing.",
      "US-001 | The stack-specific content stays repo-only (no Bun/Ink/tmux leak into the base via the Project Stack section) and the template keeps at least as many `<project ...>` placeholders as it has on main. Species: INVARIANCE PIN, both comparands derived at check time. Oracle:\nL=$(git show main:scripts/cam/CLAUDE.md | sed -n '/^## Project Stack$/,/^## Quality Gates$/p' | grep -vE '^[[:space:]]*$' | grep -vE '^## ' | grep -Fxf templates/scripts/cam/CLAUDE.md)\n[ -z \"$L\" ] || { echo \"STACK LEAK INTO TEMPLATE:\"; printf '%s\\n' \"$L\"; exit 1; }\n[ \"$(grep -coF '<project ' templates/scripts/cam/CLAUDE.md)\" -ge \"$(git show main:templates/scripts/cam/CLAUDE.md | grep -coF '<project ')\" ]\nVerified GREEN on main 2026-08-11: exit 0 (7 placeholders both sides). Do NOT replace this with the naive negative `! grep -q noUncheckedIndexedAccess templates/scripts/cam/CLAUDE.md`: that is jointly unsatisfiable, because the template legitimately carries `<project's strict-mode flag, e.g. noUncheckedIndexedAccess>`. Do NOT widen the sed range to `## Quality Gates` either: the line \"Run these before committing. Fix failures before proceeding:\" is shared boilerplate and the wider form over-fires (measured leak-count 1 on main).",
      "US-001 | The stack-agnostic `Read`-before-`Write` mandate propagates to the template implementer persona. Species: CHANGE-DETECTION. Oracle:\nA='/^3\\. Use the \\*\\*`Read` tool\\*\\*/'\nW=$(git show main:.claude/agents/subagent-implementer.md | sed -n \"${A}p\")\n[ -n \"$W\" ] || { echo \"ORACLE BROKEN\"; exit 2; }\nprintf '%s\\n' \"$W\" | diff - <(sed -n \"${A}p\" templates/agents/subagent-implementer.md)\nSwept RED on main 2026-08-11: exit 1.",
      "US-001 | The repo root `.gitignore` covers every pattern line present in `templates/.gitignore` (set containment, literal-free). Species: CHANGE-DETECTION. Oracle:\ntest -z \"$(comm -23 <(grep -vE '^[[:space:]]*(#|$)' templates/.gitignore | sort -u) <(grep -vE '^[[:space:]]*(#|$)' .gitignore | sort -u))\"\nSwept RED on main 2026-08-11: exit 1, difference set is exactly one line, `.env.*.local`, so this form is precisely satisfiable by the stated change and nothing more. Confirmed non-perturbing: `bash scripts/check-gitignore-parity.sh .` reports PROBES=43 EXIT=0 and its probe set derives only from cam-*/scripts/cam/* names.",
      "US-001 | The single TEMPLATE-WINS hunk in the reviewer persona lands: the repo copy restores `it` to the test-naming rule (`describe`/`test`/`it`), which the repo copy had narrowed for no reason while cam-cli's own suite uses `it`. Species: CHANGE-DETECTION. Oracle:\nA='/^- Test names read as behavior/,/strings/'\nW=$(git show main:templates/agents/subagent-reviewer.md | sed -n '139p')\n[ -n \"$W\" ] || { echo \"ORACLE BROKEN\"; exit 2; }\nprintf '%s\\n' \"$W\" | diff - <(sed -n '/`describe`\\/`test`/p' .claude/agents/subagent-reviewer.md | head -1)\nSwept RED on main 2026-08-11 (repo line reads `describe`/`test`, template reads `describe`/`test`/`it`). The implementer must re-derive the line numbers after the other US-001 edits rather than trusting 139; anchor on the backticked token list, not the line number.",
      "US-002 | Every population-A path is a symlink IN THE GIT INDEX (mode 120000) resolving to its `templates/` counterpart, and the population size is derived from the template tree rather than asserted. Checking the index rather than `test -L` is deliberate: it catches a worktree symlink committed as a regular file under core.symlinks=false. Species: CHANGE-DETECTION. Oracle:\ndest() { case \"$1\" in\n  commands/*)    echo \".claude/commands/${1#commands/}\";;\n  agents/*)      echo \".claude/agents/${1#agents/}\";;\n  scripts/cam/*) echo \"scripts/cam/${1#scripts/cam/}\";;\n  skills/*)      echo \".claude/skills/${1#skills/}\";;\n  *)             echo \"$1\";;\nesac; }\nNOTLINK='.gitignore\nscripts/cam/journal.md\nscripts/cam/patterns.md\nscripts/cam/suggestions.jsonl\n.claude/agents/subagent-implementer.md\n.claude/agents/subagent-auditor.md\n.claude/agents/subagent-reviewer.md\nscripts/cam/CLAUDE.md'\nrc=0; a=0\nfor t in $(cd templates && find . -type f | sed 's|^\\./||' | sort); do\n  d=$(dest \"$t\")\n  printf '%s\\n' \"$NOTLINK\" | grep -Fxq \"$d\" && continue\n  a=$((a+1))\n  m=$(git ls-files -s -- \"$d\" | cut -d' ' -f1)\n  [ \"$m\" = 120000 ] || { echo \"NOT-SYMLINK-IN-INDEX $d (mode=${m:-absent})\"; rc=1; continue; }\n  got=$(cd \"$(dirname \"$d\")\" && readlink -f \"$(basename \"$d\")\")\n  want=$(readlink -f \"templates/$t\")\n  [ \"$got\" = \"$want\" ] || { echo \"WRONG-TARGET $d -> $got (want $want)\"; rc=1; }\ndone\nexp=$(( $(cd templates && find . -type f | wc -l) - $(printf '%s\\n' \"$NOTLINK\" | wc -l) ))\n[ \"$a\" -eq \"$exp\" ] || { echo \"POPULATION-SIZE $a != derived $exp\"; rc=1; }\nexit $rc\nSwept RED on main 2026-08-11: exit 1, prints all 19 offenders, population=19 derived_expected=19. The number 19 is never written down, it falls out of 27 minus 8. The dest() ladder is an independent re-derivation of targetPath() at src/templates/embedded.ts:37-53.",
      "US-002 | The symlink target `templates/.claude/hooks/orch-agent-allowlist.sh` carries the same index mode that main gives the live hook, and is executable on disk. Without this the PreToolUse allowlist is silently disabled on every fresh clone. Species: CHANGE-DETECTION. Oracle:\nT=$(git ls-files -s -- templates/.claude/hooks/orch-agent-allowlist.sh | cut -d' ' -f1)\nM=$(git ls-tree main -- .claude/hooks/orch-agent-allowlist.sh | cut -d' ' -f1)\n[ -n \"$T\" ] && [ -n \"$M\" ] || { echo \"ORACLE BROKEN: empty mode\"; exit 2; }\n[ \"$T\" = \"$M\" ] && [ -x templates/.claude/hooks/orch-agent-allowlist.sh ]\nSwept RED on main 2026-08-11: exit 1 (tpl=100644, main-repo=100755). Measured: invoking the repo copy returns rc=0, invoking the template copy returns rc=126 (found, not executable).",
      "US-002 | The hook still executes through its registered path (never exit 126, the POSIX found-but-not-executable code). Species: INVARIANCE PIN (comparand is the POSIX protocol constant plus the resolved mode). Oracle:\nenv CAM_SESSION=probe .claude/hooks/orch-agent-allowlist.sh </dev/null >/dev/null 2>&1; rc=$?\n[ \"$rc\" -ne 126 ] && [ -x .claude/hooks/orch-agent-allowlist.sh ]\nVerified GREEN on main 2026-08-11: exit 0 (hook rc=0).",
      "US-002 | The three seed-vs-live files are NOT symlinks and keep main's index mode. This is the fence against the catastrophic failure mode: symlinking `scripts/cam/journal.md` to its seed would destroy the live journal, make the seed the journal, and ship cam-cli history to every downstream project. Species: INVARIANCE PIN, comparand derived from `git ls-tree main`. Oracle:\nrc=0\nfor f in scripts/cam/journal.md scripts/cam/patterns.md scripts/cam/suggestions.jsonl; do\n  [ -L \"$f\" ] && { echo \"IS-SYMLINK $f\"; rc=1; }\n  now=$(git ls-files -s -- \"$f\" | cut -d' ' -f1); was=$(git ls-tree main -- \"$f\" | cut -d' ' -f1)\n  [ -n \"$was\" ] || { echo \"NO-MAIN-ENTRY $f\"; rc=1; continue; }\n  [ \"$now\" = \"$was\" ] || { echo \"MODE-DRIFT $f now=$now main=$was\"; rc=1; }\ndone\nexit $rc\nVerified GREEN on main 2026-08-11: exit 0. The implementer must red-sweep this in a scratch worktree before trusting it: `ln -sf ../../templates/scripts/cam/journal.md scripts/cam/journal.md && git add -A && <gate>` must exit non-zero.",
      "US-002 | Link direction is one-way: `templates/` itself contains no symlinks. Inverting the direction would keep the embed working locally (statSync follows links) while git stores mode 120000 and CI checks out a link-to-a-link. Species: INVARIANCE PIN (literal-free set emptiness). Oracle:\ntest -z \"$(find templates -type l)\"\nVerified GREEN on main 2026-08-11: exit 0.",
      "US-002 | The agents-md gate's scan surface still covers every persona on disk after the personas become symlinks. This is the criterion that catches the Bun.Glob trap; `scripts/validate-agents-md.ts` must pass `followSymlinks: true` (or `onlyFiles: false`) to its scanSync call. Species: INVARIANCE PIN, comparand derived from a live readdirSync. Oracle:\nbun -e '\nconst { resolveScanTargets, SCAN_FILES } = await import(\"./scripts/validate-agents-md.ts\");\nconst fs = require(\"node:fs\");\nconst want = SCAN_FILES.length + fs.readdirSync(\".claude/agents\").filter(f => f.endsWith(\".md\")).length;\nconst got  = resolveScanTargets(process.cwd()).length;\nif (got !== want) { console.error(`agents-md scan surface ${got} != readdir-derived ${want}`); process.exit(1); }\nconsole.log(\"ok scan targets =\", got);\n'\nVerified GREEN on main 2026-08-11: exit 0, 7 targets. Measured evidence for the trap: on bun 1.3.13, default scanSync over a directory holding 457 symlinks and 1 regular file returned 1 of 458 entries; with followSymlinks:true it returned 458/458.",
      "US-003 | `bun scripts/generate-repo-overlays.ts --check` exits 0 against the committed tree. Species: CHANGE-DETECTION. Oracle:\nbun scripts/generate-repo-overlays.ts --check\nSwept RED on main 2026-08-11: exit 1 (module not found).",
      "US-003 | Each overlay destination is a regular file byte-equal to overlay(base) under an INDEPENDENT re-derivation of the substitution, and every `find` anchor matches exactly once against the PRISTINE base. This oracle deliberately re-implements the substitution instead of calling the generator, so a generator that is merely self-consistent cannot green it; it simultaneously covers the exactly-one-match criterion and the hand-edited-generated-file criterion. Species: CHANGE-DETECTION. Oracle:\nbun -e '\nconst fs = require(\"node:fs\");\nconst DIR = \"scripts/overlays\";\nlet rc = 0;\nconst files = fs.readdirSync(DIR).filter(f => f.endsWith(\".json\")).sort();\nif (files.length === 0) { console.error(\"overlay manifest set is empty\"); process.exit(1); }\nfor (const f of files) {\n  const o = JSON.parse(fs.readFileSync(`${DIR}/${f}`, \"utf8\"));\n  const base = fs.readFileSync(o.base, \"utf8\");\n  let out = base;\n  for (const { find, replace } of o.pairs) {\n    const n = base.split(find).length - 1;\n    if (n !== 1) { console.error(`${f}: anchor matches ${n}x against pristine base (must be exactly 1): ${JSON.stringify(find.slice(0,60))}`); rc = 1; continue; }\n    out = out.replace(find, replace);\n  }\n  if (fs.lstatSync(o.dest).isSymbolicLink()) { console.error(`${f}: ${o.dest} must be a regular file, not a symlink`); rc = 1; }\n  if (fs.readFileSync(o.dest, \"utf8\") !== out) { console.error(`${f}: ${o.dest} != overlay(${o.base}) - hand-edited or stale`); rc = 1; }\n}\nprocess.exit(rc);\n'\nSwept RED on main 2026-08-11: exit 1. Note the arity count is taken against `base`, never against the mutating `out` buffer.",
      "US-003 | Zero-match, multi-match and drift each produce their own non-zero exit code AND their own positive diagnostic marker. Species: CHANGE-DETECTION. Oracle:\nfor fx in zero-match multi-match; do\n  out=$(bun scripts/generate-repo-overlays.ts --check --overlay-dir \"test/fixtures/overlays/$fx\" 2>&1); rc=$?\n  [ \"$rc\" -eq 2 ] && printf '%s' \"$out\" | grep -q '^overlay-anchor: ' || { echo \"FAIL $fx (rc=$rc)\"; exit 1; }\ndone\nout=$(bun scripts/generate-repo-overlays.ts --check --overlay-dir test/fixtures/overlays/drift 2>&1); rc=$?\n[ \"$rc\" -eq 1 ] && printf '%s' \"$out\" | grep -q '^overlay-drift: '\nSwept RED on main 2026-08-11: exit 1. MEASURED TAUTOLOGY, do not use the naive form: `bun scripts/generate-repo-overlays.ts --check --overlay-dir .../drift >/dev/null 2>&1; test $? -eq 1` returns 0 TODAY, because a missing module also exits 1. Requiring the positive diagnostic marker plus a distinct exit code per class is what makes it falsifiable (patterns.md: positive evidence the subprocess ran to completion, never absence-of-a-bad-signal).",
      "US-003 | US-003 is a pure refactor: all four overlay destinations stay byte-identical to main. Species: INVARIANCE PIN, comparand derived from `git show main:<dest>`. Oracle:\nrc=0\nfor d in .claude/agents/subagent-implementer.md .claude/agents/subagent-auditor.md .claude/agents/subagent-reviewer.md scripts/cam/CLAUDE.md; do\n  diff <(git show main:\"$d\") \"$d\" >/dev/null || { echo \"DRIFT $d\"; rc=1; }\ndone\nexit $rc\nVerified GREEN on main 2026-08-11: exit 0. Satisfiable through all four stories because US-001 only moves content template-ward for these four paths, so no repo-side file changes.",
      "US-004 | The `buildSetupPrompt` inventory equals the embedded key set per subtree AND follows an injected template map, which proves derivation rather than a patched hardcoded list. Species: CHANGE-DETECTION. Oracle:\nbun -e '\nconst { buildSetupPrompt }  = await import(\"./src/commands/setup.ts\");\nconst { templatesContents } = await import(\"./src/vendor/_generated.ts\");\nconst basenames = (pre) => Object.keys(templatesContents).filter(x=>x.startsWith(pre)).map(x=>x.slice(pre.length)).sort();\nconst inv = (prompt, dest) => {\n  const line = prompt.split(\"\\n\").find(l => l.trim().startsWith(dest));\n  if (!line) { console.error(\"NO-INVENTORY-LINE \" + dest); return null; }\n  const m = /\\(([^)]*)\\)/.exec(line);\n  if (!m) { console.error(\"NO-LIST \" + dest); return null; }\n  return m[1].split(\",\").map(s=>s.trim()).sort();\n};\nconst CHECKS = [[\".claude/commands/\",\"commands/\"],[\".claude/agents/\",\"agents/\"],[\"scripts/cam/\",\"scripts/cam/\"]];\nlet rc = 0;\nconst real = buildSetupPrompt({ projectMode:\"new\", description:\"x\" });\nfor (const [dest, pre] of CHECKS) {\n  const want = basenames(pre), got = inv(real, dest);\n  if (JSON.stringify(want) !== JSON.stringify(got)) { console.error(\"MISMATCH\", dest, \"\\n  want:\", want, \"\\n  got: \", got); rc = 1; }\n}\nconst probe = buildSetupPrompt({ projectMode:\"new\", description:\"x\", templates: { \"commands/zzz.md\":\"\", \"agents/zzz.md\":\"\", \"scripts/cam/zzz.json\":\"\" } });\nfor (const [dest] of CHECKS) {\n  const g = inv(probe, dest) ?? [];\n  if (!g.some(x => x.startsWith(\"zzz\"))) { console.error(\"NOT-DERIVED\", dest, g); rc = 1; }\n}\nprocess.exit(rc);\n'\nSwept RED on main 2026-08-11: exit 1 with the exact expected diagnosis, MISMATCH .claude/commands/ (want 7 including cam-spec.md, got 6), MISMATCH scripts/cam/ (want 7, got 3), and NOT-DERIVED on all three subtrees. The probe-map half is the load-bearing one: it CANNOT be satisfied by appending cam-spec.md to the hardcoded list, it forces the DI seam (opts.templates defaulting to templatesContents). Note .claude/agents/ currently matches on count 5==5 yet still fails NOT-DERIVED, which is exactly the discrimination wanted.",
      "US-004 | `bun index.ts init --help` names exactly the derived destination-subtree set, in both directions (over-enumeration also fails). Species: CHANGE-DETECTION. Oracle:\nbun -e '\nconst { templatesContents } = await import(\"./src/vendor/_generated.ts\");\nconst SUBTREES = [\"commands\",\"agents\",\"scripts/cam\",\".claude\",\"skills\"];\nconst MAP = { commands:\".claude/commands/\", agents:\".claude/agents/\", \"scripts/cam\":\"scripts/cam/\", \".claude\":\".claude/\", skills:\".claude/skills/\" };\nconst destDir = (r) => { for (const s of SUBTREES) if (r.startsWith(s + \"/\")) return MAP[s]; return r; };\nconst want = [...new Set(Object.keys(templatesContents).map(destDir))].sort();\nconst help = await Bun.$`bun index.ts init --help`.text();\nconst m = /templates into (.+)\\.$/m.exec(help);\nconst got = m ? m[1].split(\",\").map(s=>s.trim()).sort() : null;\nif (!got) { console.error(\"ORACLE BROKEN: no inventory line in init --help\"); process.exit(2); }\nif (JSON.stringify(want) !== JSON.stringify(got)) { console.error(\"WANT:\", want, \"\\nGOT :\", got); process.exit(1); }\n'\nSwept RED on main 2026-08-11: exit 1. WANT [\".claude/\",\".claude/agents/\",\".claude/commands/\",\".claude/skills/\",\".gitignore\",\"scripts/cam/\"] versus GOT [\".claude/agents/\",\".claude/commands/\",\"scripts/cam/\"]. Uses `bun index.ts`, never the installed `cam`/`gship` binary, per the patterns.md rule on binary staleness.",
      "US-004 | `templatesContents` stays at exactly one line per template file and inside its own size budget, so the collapse never silently grows the embedded map. Species: INVARIANCE PIN, both sides derived. Oracle:\nbun -e '\nconst fs = require(\"node:fs\");\nconst n = fs.readFileSync(\"src/vendor/_generated.ts\",\"utf8\").split(\"\\n\").length;\nconst b = JSON.parse(fs.readFileSync(\"scripts/file-size-budget.json\",\"utf8\"))[\"src/vendor/_generated.ts\"];\nconst t = fs.readdirSync(\"templates\",{recursive:true}).filter(p=>{try{return fs.statSync(\"templates/\"+p).isFile()}catch{return false}}).length;\nconsole.log(`_generated=${n} budget=${b} templateFiles=${t}`);\nif (n > b) process.exit(1);\n'\nplus `bun scripts/generate-embedded-vendor.ts --check`.\nVerified GREEN on main 2026-08-11: exit 0, _generated=51 budget=51 templateFiles=27, and the embed check exits 0.",
      "US-002 + US-004 | The two new gates EXIST in the live GATES array by name, the test file pins the array, and each gate reddens on a planted violation. The name-presence half is what makes this non-tautological: the previous form only checked that the test pinned whatever GATES already contained, so it stayed green if zero gates were added. Species: CHANGE-DETECTION. Oracle:\nbun -e '\nconst { GATES } = await import(\"./scripts/check-all.ts\");\nconst src = require(\"node:fs\").readFileSync(\"test/check-all.test.ts\",\"utf8\");\nlet rc = 0;\nfor (const n of [\"symlink-integrity\",\"overlay-drift\"]) if (!GATES.some(g => g.name === n)) { console.error(`missing gate: ${n}`); rc = 1; }\nconst m = /expect\\(GATES\\)\\.toHaveLength\\((\\d+)\\)/.exec(src);\nif (!m) { console.error(\"no toHaveLength pin in test/check-all.test.ts\"); rc = 1; }\nelse if (Number(m[1]) !== GATES.length) { console.error(`test pins ${m[1]} but GATES has ${GATES.length}`); rc = 1; }\nfor (let i = 0; i < GATES.length; i++) if (!src.includes(`GATES[${i}]?.name`)) { console.error(`no name assertion for GATES[${i}] (${GATES[i].name})`); rc = 1; }\nprocess.exit(rc);\n'\nSwept RED on main 2026-08-11: exit 1 (neither gate name exists; main has 16 gates with GATES[0..15]?.name all asserted).\nPLANTED-VIOLATION HALF, run in a scratch git worktree so the real tree is never mutated, and REQUIRED as part of this criterion rather than left in prose:\nw=$(mktemp -d)/wt && git worktree add -q \"$w\" HEAD && ( cd \"$w\" \\\n  && ln -sf ../../templates/scripts/cam/journal.md scripts/cam/journal.md && git add -A \\\n  && ! bun scripts/check-all.ts --only symlink-integrity ) ; rc=$?\ngit worktree remove --force \"$w\" >/dev/null 2>&1; exit $rc\nThe seed-vs-live fence must exit non-zero when the journal is linked to its seed. CI parity needs no extra work: the workflow runs `bun run check:all -- --json`, the spine, which picks up every manifest gate automatically.",
      "US-002 | The real consumer's discovery primitive still finds every symlinked persona, proven by differential rather than by assertion. Claude Code enumerates `.claude/agents/*.md` and `.claude/commands/*.md` through ripgrep invoked with an unconditional `--follow` (claude-code-harness/utils/markdownConfigLoader.ts:564-568), so the falsifiable form is: with `--follow` the discovered set equals a symlink-inclusive readdir, and WITHOUT `--follow` it is strictly smaller. The differential is zero on main (no symlinks exist yet), which is what makes this a change-detection oracle rather than a tautology. Species: CHANGE-DETECTION. Oracle:\ncommand -v rg >/dev/null || { echo \"ORACLE BROKEN: rg not on PATH\"; exit 2; }\nrc=0\nfor d in .claude/agents .claude/commands; do\n  want=$(ls \"$d\"/*.md | xargs -n1 basename | sort)\n  follow=$(rg --files --hidden --follow --no-ignore --glob '*.md' \"$d\" | xargs -n1 basename | sort)\n  plain=$(rg --files --hidden --no-ignore --glob '*.md' \"$d\" | xargs -n1 basename | sort)\n  [ \"$follow\" = \"$want\" ] || { echo \"FOLLOW-SET-MISMATCH $d\"; diff <(printf '%s\\n' \"$want\") <(printf '%s\\n' \"$follow\"); rc=1; }\n  [ \"$(printf '%s\\n' \"$plain\" | grep -c .)\" -lt \"$(printf '%s\\n' \"$want\" | grep -c .)\" ] || { echo \"NO-DIFFERENTIAL $d: nothing is symlinked, the probe proves nothing\"; rc=1; }\ndone\nexit $rc\nSwept RED on main 2026-08-11: exit 1, NO-DIFFERENTIAL on both directories (zero symlinks today). MEASURED EVIDENCE that the plan is safe on the consumer side, gathered before this criterion was written: all five `.claude/` surfaces are explicitly symlink-aware, not accidentally so. Commands and agents go through ripgrep with `--follow` (proved against a real symlinked .md fixture: with `--follow` exit 0 and the file listed, without it exit 1); skills filter on `isDirectory() || isSymbolicLink()` (skills/loadSkillsDir.ts:425); settings.json is `realpathSync`-resolved before read (utils/settings/settings.ts:206) and the loader even logs a dedicated \"Broken symlink\" diagnostic, so links are an expected input; hooks are shell command strings run via `/bin/sh -c` with no stat gate. `Bun.Glob` appears ZERO times in Claude Code's `.claude/` discovery, so the scanSync hazard never touches it.",
      "US-003 | Pristine-base arity semantics are pinned by a dedicated test file, and the oracle names that file so it cannot be satisfied by \"the test was skipped\". Bare `bun test` is green on main and therefore worthless here. Species: CHANGE-DETECTION. Oracle:\nbun test test/overlays/pristine-arity.test.ts\nSwept RED on main 2026-08-11: the path does not exist, exit non-zero.\nThe test MUST contain both discriminating fixtures, because either one alone is passable by a wrong implementation:\n  FIXTURE `cascade` (catches a sequential/mutating-buffer generator): base is `alpha`; pair 1 is {find `alpha`, replace `gamma`}; pair 2 is {find `gamma`, replace `delta`}. Under pristine-base semantics pair 2's anchor matches ZERO times and the generator must exit 2 with the `overlay-anchor: ` diagnostic. A sequential generator sees `gamma` in the post-pair-1 buffer, matches once, and exits 0.\n  FIXTURE `multiply` (catches the inverse error, counting arity on the mutated buffer): base is `foo`; pair 1 is {find `foo`, replace `foo foo`}; pair 2 is {find `foo`, replace `bar`}. Under pristine-base semantics pair 2's anchor matches exactly once and the run must NOT exit 2. A generator counting against the mutated buffer sees two matches and wrongly exits 2.\nNeither fixture is satisfiable by accident, and a generator that passes both has demonstrably resolved every anchor against the unmodified base before writing any replacement."
    ],
    "gotchas": [
      "HIGHEST SEVERITY, green gate with lost coverage: `Bun.Glob.scanSync` silently omits symlinks under default options. Measured on bun 1.3.13 against a directory holding 457 symlinks and 1 regular file: default scanSync returned 1 of 458 entries, zero of the 457 symlinks; with `followSymlinks: true` OR `onlyFiles: false` it returned 458/458. `scripts/validate-agents-md.ts:64-65` runs `new Glob('.claude/agents/*.md').scanSync({ cwd, dot: true })` with defaults. After US-002, `subagent-orchestrator.md` and `subagent-planner.md` become symlinks and DROP OUT of the agents-md scan surface (7 targets to 5) WITHOUT the gate turning red. Fix `followSymlinks: true` and keep the derived-target-count pin as a hard acceptance criterion. `scripts/check-file-sizes.ts`, `check-test-sleeps.ts`, `check-test-tmpdir.ts` and `check-debt-markers.ts` also use scanSync but only over `{src,scripts,test}/**`, where no symlinks are planned; that assumption is pinned by the templates-contain-no-symlinks criterion.",
      "ORDERING, and it can break the implementer's own live session: the symlink target is not executable today. `git ls-files -s` reports `.claude/hooks/orch-agent-allowlist.sh` = 100755 and `templates/.claude/hooks/orch-agent-allowlist.sh` = 100644. `.claude/settings.json` registers the hook as a bare command path, so the exec bit is load-bearing; invoking the template copy returns rc=126. Run `git update-index --chmod=+x templates/.claude/hooks/orch-agent-allowlist.sh` BEFORE creating the link, otherwise the implementer loses its own PreToolUse allowlist mid-story. `test/embedded.test.ts` only asserts the MATERIALIZED mode (materializeTemplates chmods 0o755 at write time), so it is blind to this; no existing gate covers the repo's own hook mode.",
      "CROSS-STORY DEPENDENCY, not inferable from the story text: US-003 depends on US-001. US-001 adds roughly 41 lines to `templates/scripts/cam/CLAUDE.md` and rewrites step 3 of `templates/agents/subagent-implementer.md`. Any overlay anchored on pre-US-001 template text goes to zero-match (exit 2) the moment US-001 lands. Author every overlay against the POST-US-001 base.",
      "OVERLAY SEMANTICS, pin it explicitly: every `find` must match exactly once against the PRISTINE base, and all matches must be resolved before any replacement is written. Two pairs rewrite lines containing `unnecessary re-renders` (reviewer line 80 and rubric row 8 at line 120) and two rewrite lines containing `<project typecheck command>` (CLAUDE.md lines 29 and 53). If pairs are applied sequentially against the already-mutated buffer, anchor counts change mid-run and the arity check becomes meaningless.",
      "SIX ANCHORS collide in their short form and need the long form: (a) bare `<project typecheck command>` matches 2 lines, use the 3-line gates block; (b) `NO \\`_ref\\` channel` matches 2 sub-bullets, include `jscpd also has`; (c) `No hardcoded secrets` matches 2 lines, include the trailing `in code.`; (d) `unnecessary re-renders` matches 2 lines, anchor on the full `| 8 | **perf** |` table row; (e) bare `typecheck` matches 3 lines and bare `\\`bun test\\`` matches 2, use the prefix through `discovered at review time,`; (f) merge the reviewer's four checklist sections into ONE pair anchored on `### Security` rather than four pairs.",
      "TWO HUNKS REQUIRE TEXT THAT EXISTS ON NEITHER SIDE and must be authored: the implementer's `97c97,107` quality-gate block and the auditor's `91a95,102` section F. A story phrased as \"update the template to match the repo\" will silently produce a stack-specific base. The generic base text for both is specified in the story body; do not let the implementer improvise it.",
      "NUMBERING HAZARD: propagating the auditor carve-outs (items 21 and 25) into a NUMBERED list renumbers both sides. Merge auditor hunks 3+4+5 into a single overlay pair anchored on `Project-specific sanity`; two separate pairs would strand a base item 21/22 in the middle of the repo's item list.",
      "ZERO HEADROOM on every file this PRD touches. Measured actual/budget: scripts/check-all.ts 491/491, test/check-all.test.ts 478/478, src/commands/setup.ts 1268/1268, src/vendor/_generated.ts 51/51, src/templates/embedded.ts 124/124, scripts/generate-embedded-vendor.ts 159/159. Each needs a scripts/file-size-budget.json raise plus a dated `_ref` note naming CAM-530, and `git add scripts/file-size-budget.json` BEFORE re-running the gate, because the tracker-ref check reads only the staged diff. index.ts (3366 lines) is NOT budgeted (outside the {src,scripts,test} glob), so INIT_HELP changes are free.",
      "CORRECTION to a common assumption: new `scripts/*.ts` files do NOT need budget entries. `checkSizeCeilings` iterates `Object.entries(budget)` and skips undefined actuals; it never iterates the glob. Proof: scripts/validate-agents-md.ts is 516 lines with no budget entry and the gate is green. Adding entries for the new generator is hygiene, not a gate requirement, so do not write an acceptance criterion asserting the new script has a budget entry.",
      "`test/templates/orchestrator-boot.test.ts` degenerates after US-002 and must be collapsed deliberately, not left alone. Its `describe.each` runs over the repo copy read from disk AND the template copy from `templatesContents`; once the pair is one inode, the two legs differ only by embed staleness, which `embed-vendor` already covers byte-for-byte. Collapse it to a single-source assertion over `templatesContents['agents/subagent-orchestrator.md']` and record that the redundancy now comes from `embed-vendor` plus the symlink-integrity gate. The sibling `test/templates/subagent-reviewer-clean-findings.test.ts` is the OPPOSITE case: subagent-reviewer.md is overlay-generated, so its two legs stay genuinely independent and the test gains value. But no string it asserts may appear in any overlay `find`/`replace`, or an overlay edit could satisfy one leg while breaking the other.",
      "`.claude/commands/direct-fix.md` is tracked in the repo and absent from `templates/`. It is excluded from the symlink population by construction (the oracle iterates the template tree forward) and it must NOT be added to `templates/`: that would make `_generated.ts` 52 lines and trip the 51-line budget.",
      "`test/embedded.test.ts:105` (exact key-set equality between templatesContents and an on-disk walk) is unaffected, but only because of link direction. Both walkers use statSync, which FOLLOWS symlinks; the walk root is `templates/`, and `.claude/` is not reachable from it. Verified: `bun scripts/generate-embedded-vendor.ts --check` exits 0 with templateFiles=27 and _generated.ts at 51 lines. Note the walkers differ: `walkFiles()` guards with isFile() while the test's own `walk()` does not, so a dangling link inside templates/ throws ENOENT in both but with different messages.",
      "SYMLINK CREATION IS NOT EXPRESSIBLE VIA THE Write TOOL. US-002 needs Bash (`ln -s`) plus `git update-index --chmod=+x`. Verified the allowlist hook does not block this PRD: `.claude/hooks/orch-agent-allowlist.sh:86` denies CAM_WORKER=1 writes only for `*scripts/cam/prd.json` and `*scripts/cam/issues/*`; every write target here is allowed.",
      "This repo has ZERO tracked symlinks today (`git ls-files -s | awk '$1==\"120000\"'` is empty), so US-002 introduces the first ones. `git config core.symlinks` is unset (defaults true on macOS/Linux) and `readlink -f` is available, with `/bin/realpath` as fallback. CI runner symlink support is UNVERIFIED and must be a US-002 smoke, not an assumption.",
      "SHELL AUTHORING HAZARDS hit while sweeping, both cost a false result: (a) backticks inside a sed anchor get command-substituted if the oracle is wrapped in `bash -c \"...\"`; every anchor in this spec contains backticks, so oracles must be delivered as plain multi-line snippets or single-quoted throughout. (b) `grep -F '- **Test quality**:'` fails with \"invalid option\" because the pattern starts with `-`; it must be `grep -F -e '...'`.",
      "SCOPE DECLARATION, state it in the PRD so a future reader does not \"fix\" it: \"stack-agnostic base\" is an ASPIRATION, not a currently-true invariant. 22 lines of SHARED, non-divergent template text already name bun, tmux or cam-cli source paths (subagent-reviewer.md 13, subagent-implementer.md 7, subagent-auditor.md 1, templates/scripts/cam/CLAUDE.md 1). Consequence: the overlay boundary in this PRD is \"what differs\", NOT \"stack-specific versus agnostic\". Making the aspiration true is CAM-533 and is deliberately out of scope here, because those lines live in the shared portion that none of these four stories touches.",
      "HUNK OWNERSHIP IS SPEC-DECIDED, NOT IMPLEMENTER-DECIDED. All 39 divergent hunks are classified in the story bodies: 1 TEMPLATE-WINS, 10 REPO-WINS, 28 OVERLAY-OWNED, with 11 carve-outs (8 overlay carve-outs inside repo-wins hunks, 3 repo-wins carve-outs inside overlay-owned hunks). The implementer must not adjudicate any hunk. Post-reconciliation the 28 overlay hunks plus 7 applied carve-outs collapse into 22 {find, replace} pairs (5 CLAUDE.md, 3 implementer, 3 auditor, 11 reviewer). None of the four overlay files graduates to symlink; do not promise that.",
      "DIFF ALIGNMENT ARTIFACTS: after US-001 lands, re-diffing will re-align two hunk pairs differently (CLAUDE.md `33c46`/`34a48,49`, the Biome paragraph versus the --no-verify line; and `59c75`/`60a77,115`, where the Marking-a-bullet-resolved paragraph is relocated into the new section and retitled). Do not read the new alignment as fresh divergence.",
      "MEASURED, and it retires the biggest open risk in this PRD: Claude Code discovers symlinked `.claude/` entries on all five surfaces (commands, agents, skills, settings.json, hooks), and does so deliberately rather than accidentally. Commands and agents: ripgrep with unconditional `--follow` at claude-code-harness/utils/markdownConfigLoader.ts:564-568, proved empirically against a real symlinked .md (with `--follow` listed and exit 0, without it exit 1) and against a symlinked DIRECTORY with the same result. Skills: `!entry.isDirectory() && !entry.isSymbolicLink()` reject-filter at skills/loadSkillsDir.ts:425. settings.json: `realpathSync` at utils/settings/settings.ts:206, with a dedicated broken-symlink log line proving links are an expected input. Hooks: a shell command string spawned through `/bin/sh -c`, no stat gate, and `execve` follows links. `Bun.Glob` has ZERO occurrences in that discovery path. Corroboration that this is designed behavior: Claude Code ships `worktree.symlinkDirectories` (utils/settings/types.ts:440-447), a feature that deliberately plants symlinked directories inside project trees. CAVEAT ON PROVENANCE: claude-code-harness/ is a gitignored, untracked local reference copy, so it may drift from the installed binary; the empirical ripgrep probes were run against the real bundled binary and are the stronger evidence.",
      "The `scripts/validate-agents-md.ts` symlink fix is now measured with the exact call shape, not inferred: `new Glob('.claude/agents/*.md').scanSync({ cwd, dot: true })` returned 0 entries over a symlink-only directory, while `scanSync({ cwd, dot: true, followSymlinks: true })` returned 11. Because that scanner excludes `templates/**` by construction, the un-fixed version reports SUCCESS while validating nothing. Contrast, and do not \"fix\" it: `vendor/check-agent-frontmatter.ts:210` uses `readdirSync(AGENT_DIR)` WITHOUT `withFileTypes`, which returns plain strings and is symlink-safe as-is. The silent-drop idiom to watch for anywhere else is `readdirSync(dir,{withFileTypes:true}).filter(e => e.isFile())`, measured at 0 of 11 on the same directory; the safe form adds `|| e.isSymbolicLink()`.",
      "PORTABILITY CONDITION on the symlink collapse: a contributor whose git has `core.symlinks=false` (the Windows default) gets the links materialized as plain text files containing their target paths, which breaks all five discovery surfaces at once. The US-002 index-mode criterion (asserting mode 120000 in the index rather than `test -L` on disk) is what catches this, and it is the reason that criterion checks the index. LATENT, not currently in play: claude-code-harness/utils/plugins/validatePlugin.ts:737 filters skills directories with a bare `.filter(e => e.isDirectory())` and WOULD drop a symlinked skill dir, while the runtime loader accepts it. That validator/runtime divergence only bites if `templates/` is ever shipped as a Claude Code plugin; the repo has no `.claude-plugin/` marker today.",
      "AUDIT FINDING F-05, applies to the story that authors the two new base blocks: a criterion asserting text lands \"byte-for-byte as authored\" is not verified by an oracle that only checks two anchors are non-empty, that no bun/Ink/tmux token appears, and that two phrases are present, because improvised prose containing those phrases passes. This is the one place in the PRD where NEW instruction text is authored that ships to every downstream project, so it needs a fidelity-grade comparand: diff a here-doc of the authored text against the sed-extracted block, the same shape as the CLAUDE.md line-set criterion.",
      "AUDIT FINDING F-06, a self-contradicting criterion to avoid when collapsing `test/templates/orchestrator-boot.test.ts`: asking the implementer to record the collapse rationale in the test header while the oracle forbids the literal string `.claude` anywhere in that file makes the natural rationale sentence redden the gate. Either state that the header must avoid the bare token, or narrow the absence check to a path-literal form such as `! grep -qF \"'.claude'\"`.",
      "AUDIT FINDING F-07, decomposition: the story carrying all four overlay manifests holds 22 find/replace pairs, 11 of them in the reviewer persona alone, plus two authored base blocks, template edits, gate wiring and an embed regeneration. Split the reviewer manifest into its own story sequenced after the other three manifests are green, and keep the gate wiring with the last one. Decompose by verification mode and risk isolation, never by story count."
    ],
    "domainTerms": [
      "template pair",
      "seed-vs-live pair",
      "overlay pair",
      "anchor arity",
      "pristine-base matching",
      "carve-out",
      "symlink-integrity gate",
      "reconciliation direction"
    ]
  },
  "wsjf": {
    "value": 3,
    "timeCriticality": 4,
    "riskReduction": 5,
    "jobSize": 5
  }
}
