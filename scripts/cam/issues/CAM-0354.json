{
  "id": "CAM-354",
  "title": "CAM-54 slice: codex reviewer confabulates the review - force diff-grounding by injecting git diff main...HEAD into the codex reviewer prompt",
  "stage": "idea",
  "status": "open",
  "blockedBy": [],
  "createdAt": "2026-07-19T15:49:51.574Z",
  "updatedAt": "2026-07-19T15:49:51.574Z",
  "description": "LIVE FINDING (slice-4 de-risk probe, 2026-07-19): dispatched the real codex reviewer argv (rendered via CodexAdapter.buildSpawnArgv('reviewer'), model gpt-5.5, -c model_instructions_file=<stripped subagent-reviewer.md>, REVIEWER_TASK_PROMPT) against a scratch branch whose ONLY diff vs main was a 4-line doc file. Result: codex faithfully WROTE a schema-valid scripts/cam/review-report.json (verdict CLEAN, findings[], artifactOfRecord) AND review-artifact.txt -- but it reviewed the WRONG changeset. The artifact header read 'Branch: cam/issue-352 (CAM-352 ...)' (already merged, NOT on the scratch branch), the single finding was about codex-auth.test.ts (not in the diff), and the Layer-B oracle lines were fabricated ('bun test: 5131 pass / 0 fail across 290 files'). codex NEVER ran git diff main...HEAD; it pattern-matched to repo/context and confabulated a confident CLEAN verdict about code not under review. This is the most dangerous failure mode: the sidecar would parse <review>CLEAN</review> and ship on a review that never looked at the real diff.\n\nROOT CAUSE: ADR-0047 mismatch (d) -- codex has no --agent harness that forces the reviewer's grounding steps. The claude reviewer reliably runs `git diff main...HEAD` itself via its Bash tool + agent harness; codex, fed the same body as a plain developer prompt via -c model_instructions_file, does not.\n\nFIX (recommended): the CodexAdapter reviewer path should pre-compute the actual `git diff main...HEAD` (and/or the changed-file list) and inject it into the reviewer prompt/instructions so codex physically has the diff to review, rather than trusting it to self-fetch. A stronger imperative directive ('run git diff main...HEAD FIRST and review ONLY that diff') is a cheaper fallback but relies on the same obedience codex just ignored, so injection is the robust option. Scope this to the codex (reviewer) path only; the claude path must stay byte-identical.\n\nSECONDARY OBSERVATIONS (not this slice's scope, file/track separately if pursued): (1) codex reviewer was very slow (>10min, did not complete) at xhigh reasoning while running full gates; the sidecar review timeout must accommodate codex or tune reasoning effort. (2) <review> sentinel-to-pane streaming is unconfirmed -- piped stdout block-buffers and the run was killed before flush; needs a real TTY/tmux-pane capture test once grounding is fixed. Both are moot until the reviewer actually reviews the right diff.\n\nPREREQUISITE ORDERING: this slice must land before the slice-4 live ceremony (a codex CLEAN is meaningless until it reviews the real diff) and before slice-5 (per-backend model-id decoupling). Also note the config-flip ceremony is separately blocked by drift-guard tests (models.test.ts drift guard pins reviewer to a known claude model; review.test.ts dispatch-shape tests read the real project.toml) -- making project.toml codex-tolerant is slice-5's job."
}
