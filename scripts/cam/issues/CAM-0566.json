{
  "id": "CAM-566",
  "title": "cam web B1: scaffold do cliente e entrada de webui na spine de gates",
  "stage": "specified",
  "status": "open",
  "blockedBy": [],
  "createdAt": "2026-08-14T05:17:49.913Z",
  "updatedAt": "2026-08-14T05:19:01.812Z",
  "description": "Refile do CAM-564 (fechado como subsumed em 2026-08-14) com description corrigida: a anterior dizia SEIS gates porque foi escrita antes de medir o file-size, e a spec persistida depois dizia CINCO. O auditor pesa description acima de spec.scope e o desvio ja custou um finding. Recorte do CAM-559 pelo MODO DE VERIFICACAO: esta metade se verifica por SAIDA DE GATE, a metade B2 (CAM-565) por bun test e fixture de script.\n\nENTRA: scaffold de webui/ com vite e Tailwind v4 CSS-first via @tailwindcss/vite, sem tailwind.config e sem postcss.config; split de tsconfig com a raiz excluindo webui e webui ganhando tsconfig.app.json com lib DOM; o script typecheck da raiz passando a rodar DOIS programas encadeados, com vite.config.ts dobrado dentro do include do tsconfig.app.json em vez de ganhar um terceiro programa proprio (decisao do operador em 2026-08-14: cada programa a mais e mais um lugar onde a cobertura fura em silencio); a TROCA DO COMANDO DO GATE typecheck no manifesto GATES de scripts/check-all.ts, de 'bunx tsc --noEmit' para 'bun run typecheck'; as dependencias do cliente no package.json da RAIZ, sem workspace; a arvore webui entrando em CINCO gates que hoje a deixam de fora por omissao (lint/biome, dead-code/knip, test-sleeps, test-tmpdir, debt-markers); e a emenda do ADR-0068, cujo texto vigente esta falsificado por esta issue.\n\nNAO ENTRA: vendorizacao do COSS, tema, componentes, polling e testes de componente, que sao a CAM-565; embarcar ou servir o bundle, que e a CAM-560; e QUATRO gates deixados de fora com motivo medido, a saber file-size (nao e expressavel, ver abaixo), version-skips (subsumido pelo skip-ratchet, que ja cobre webui e falha em qualquer skip no host), gitignore-parity (as probes derivam de literais de path de artefato escritos por codigo, e bundle de browser nao escreve artefato) e dup/jscpd (exige editar os args posicionais do proprio comando do gate, com retorno abaixo do custo numa arvore que nasce pequena).\n\nO ACHADO QUE MOTIVOU O REFILE, medido em 2026-08-14: o manifesto GATES nao roda o script, ele roda 'bunx tsc --noEmit' DIRETO (scripts/check-all.ts:167). Editar apenas o script typecheck do package.json deixaria a segunda invocacao INERTE no CI, porque check:all e o CI executam o comando do manifesto. Hoje os dois textos sao identicos, o que torna a divergencia invisivel. Isso falsifica a frase do ADR-0068 que diz que o manifesto GATES fica intocado e o CI fica intocado, e por isso a emenda do ADR entra no escopo desta issue em vez de ficar como divida.\n\nVARREDURA QUE PROVA A DISCRIMINACAO, feita em worktree isolado sobre a arvore pos-scaffold simulada em 2026-08-14: com o script typecheck ja corrigido para os dois programas e o manifesto AINDA em 'bunx tsc --noEmit', um erro de tipo em webui/src/main.tsx passa pelo gate (oraculo RED); com o manifesto trocado para 'bun run typecheck', o mesmo erro reprova o gate (oraculo VERDE). A troca do manifesto e portanto load-bearing e nao cosmetica.\n\nPOR QUE O file-size NAO ENTRA: checkSizeCeilings itera Object.entries(budget) e faz continue quando o path nao tem entrada em scripts/file-size-budget.json, entao arquivo sem entrada nunca e visitado, em qualquer tamanho. Medido: arquivo novo de 3000 linhas em src/ sai exit 0. Alargar o glob da varredura seria inerte. O mecanismo de entrada no orcamento e follow-up separado, ainda nao filado.",
  "spec": {
    "scope": "Metade B1 do cliente web: scaffold da arvore webui/ e entrada dessa arvore na spine de gates. ENTRA: scaffold vite com Tailwind v4 CSS-first via @tailwindcss/vite (sem tailwind.config, sem postcss.config); split de tsconfig com a raiz excluindo webui e webui ganhando tsconfig.app.json com lib DOM; o script typecheck da raiz rodando DOIS programas encadeados, com vite.config.ts dobrado dentro do include do tsconfig.app.json e NAO ganhando um terceiro programa proprio (decisao do operador em 2026-08-14: cada programa a mais e mais um lugar onde a cobertura fura em silencio; se o vite.config.ts quebrar sob lib DOM, o terceiro programa e o fallback correto e o desvio tem que ser declarado no banner do PRD); a troca do comando do gate typecheck no manifesto GATES, de bunx tsc --noEmit para bun run typecheck, sem a qual a segunda invocacao fica inerte no CI; dependencias do cliente no package.json da raiz, sem workspace; a arvore webui entrando em CINCO gates que hoje a deixam de fora por omissao (lint/biome, dead-code/knip, test-sleeps, test-tmpdir, debt-markers); e a emenda do ADR-0068. NAO ENTRA: vendorizacao do COSS, tema, componentes, polling e testes de componente, que sao a metade B2 (CAM-565); embarcar ou servir o bundle, que e a CAM-560; e QUATRO gates deixados de fora com motivo medido em 2026-08-13, a saber file-size (alargar o glob e INERTE: checkSizeCeilings itera o orcamento e faz continue em path sem entrada, medido com arquivo novo de 3000 linhas saindo exit 0; o mecanismo de entrada no orcamento e follow-up separado, ainda nao filado), version-skips (subsumido pelo skip-ratchet, que ja cobre webui e falha em qualquer skip no host), gitignore-parity (as probes derivam de literais de path de artefato escritos por codigo, e bundle de browser nao escreve artefato) e dup/jscpd (exige editar os args posicionais do proprio comando do gate em check-all.ts, com retorno abaixo do custo numa arvore que nasce pequena). O corte B1/B2 e por MODO DE VERIFICACAO: esta metade se verifica por saida de gate, a B2 por bun test e fixture de script.",
    "acceptanceCriteria": [
      "O CSS construido pelo vite contem regra de utilitario efetivamente GERADA a partir de uma classe usada na marcacao, e nao a diretiva crua nao processada. Especie: deteccao de mudanca. Varrido RED na main em 2026-08-13 (build:ui ausente). ATENCAO, a forma anterior deste oraculo ancorava em inicio de linha e era INSATISFAZIVEL, porque o Tailwind v4 emite utilities dentro de @layer utilities e o vite minifica por padrao, deixando o arquivo praticamente numa linha unica que comeca por comentario legal; nao reintroduzir ancora de linha. [oracle: file-assert bun run build:ui >/dev/null 2>&1 && grep -qF 'class=\"flex' webui/index.html && ! grep -qF '@import \"tailwindcss\"' webui/dist/assets/*.css && grep -qF 'display:flex' webui/dist/assets/*.css]",
      "O typecheck fica partido em dois mundos: a raiz exclui a arvore do cliente e o cliente declara DOM, e o typecheck da raiz continua passando. Especie: deteccao de mudanca. Varrido RED na main em 2026-08-13. ATENCAO, a forma anterior rodava jq direto contra tsconfig.json e era INSATISFAZIVEL, porque o tsconfig da raiz e JSONC e jq aborta com parse error e exit 5 tanto antes quanto depois da mudanca; o caminho correto e bunx tsc --showConfig, que resolve o JSONC e emite JSON real. Medido em 2026-08-13 que --showConfig emite .exclude como array de strings nuas ([\"node_modules\",\"vendor\",\"claude-code-harness\"]), portanto index(\"webui\") e a forma certa e o verde e alcancavel. Nao voltar a passar tsconfig.json direto para jq. [oracle: file-assert bunx tsc --showConfig 2>/dev/null | jq -e '.exclude|index(\"webui\")' >/dev/null && jq -e '.compilerOptions.lib|index(\"DOM\")' webui/tsconfig.app.json >/dev/null && bun run typecheck]",
      "O gate typecheck DO MANIFESTO cobre a arvore do cliente: com um erro de tipo plantado em webui/src/main.tsx, o comando que o manifesto GATES declara para o gate typecheck reprova. Especie: deteccao de mudanca. FORMA DERIVADA AO VIVO de proposito: o oraculo importa GATES de scripts/check-all.ts e executa cmd mais args, em vez de rodar bun run typecheck ou check-all inteiro. Rodar o SCRIPT nao discrimina, porque o manifesto executa o comando dele proprio e nao o script (medido em 2026-08-14: scripts/check-all.ts:167 declara bunx tsc --noEmit direto, e hoje os dois textos sao identicos, o que torna a divergencia invisivel). Rodar CHECK-ALL inteiro tambem nao discrimina, porque qualquer outro gate vermelho satisfaz o oraculo: medido que uma probe orfa em webui/src ja deixa a spine vermelha pelo gate de dead-code depois que o knip passa a enxergar webui, com ou sem a troca do manifesto. A probe vai dentro de webui/src/main.tsx, que o knip ja alcanca como entry, e por isso a arvore pre-mudanca fica VERDE: a raiz exclui webui, as duas regras de complexidade do biome ignoram erro de tipo, e bun test nunca carrega main.tsx. Varrido nas DUAS direcoes em worktree isolado sobre a arvore pos-scaffold simulada em 2026-08-14: com o script ja corrigido para dois programas e o manifesto ainda em bunx tsc --noEmit o oraculo devolve RED, e com o manifesto trocado para bun run typecheck devolve VERDE. [oracle: file-assert ( t=$(mktemp); cp webui/src/main.tsx \"$t\"; printf '\\nexport const __typeprobe: string = 1;\\n' >> webui/src/main.tsx; c=$(bun -e 'import { GATES } from \"./scripts/check-all.ts\"; const g = GATES.find((x) => x.name === \"typecheck\"); console.log([g.cmd, ...g.args].join(\" \"));'); if eval \"$c\" >/dev/null 2>&1; then cp \"$t\" webui/src/main.tsx; rm -f \"$t\"; false; else cp \"$t\" webui/src/main.tsx; rm -f \"$t\"; true; fi )]",
      "As dependencias do cliente vivem no package.json da RAIZ e nenhum package.json de workspace nasce em webui/. Especie: deteccao de mudanca. Varrido RED na main em 2026-08-13. Decisao do operador: um segundo grafo de dependencia abriria buraco no pino de harness de DOM, que ancora em package.json da raiz. [oracle: file-assert ! test -e webui/package.json && jq -e '.devDependencies[\"@tailwindcss/vite\"]' package.json >/dev/null]",
      "O gate de lint (biome) passou a enxergar a arvore do cliente: uma funcao acima do teto de linhas plantada em webui/src reprova o gate. Especie: deteccao de mudanca. Varrido nas DUAS direcoes em 2026-08-14: a MESMA violacao em src/ ja reprova hoje (prova que a forma da violacao e real) e em webui/ passa despercebida, com o gate verde no controle sem probe. Regra escolhida por medicao: biome.json tem recommended:false e SOMENTE duas regras ativas (noExcessiveCognitiveComplexity e noExcessiveLinesPerFunction), entao violacao de variavel nao usada ou import morto NAO reprova e nao serve de probe. O TETO E DERIVADO AO VIVO de biome.json e nunca congelado no texto: congelar 85 aqui apodrece silenciosamente no dia em que alguem baixar maxLines, porque a probe deixa de violar e o criterio fica verde sem o gate funcionar. Nao substituir a derivacao por numero. [oracle: file-assert ( p=webui/src/__lintprobe.ts; mkdir -p webui/src; n=$(jq -r '.linter.rules.complexity.noExcessiveLinesPerFunction.options.maxLines' biome.json); { echo 'export function probeFn(): number {'; i=0; while [ \"$i\" -lt \"$n\" ]; do echo \"  const v$i = $i;\"; i=$((i+1)); done; echo '  return 0;'; echo '}'; } > \"$p\"; if bunx biome lint --error-on-warnings >/dev/null 2>&1; then rm -f \"$p\"; false; else rm -f \"$p\"; true; fi )]",
      "O gate de dead-code (knip) passou a enxergar a arvore do cliente: um arquivo orfao plantado em webui/src reprova o gate. Especie: deteccao de mudanca. Varrido nas DUAS direcoes em 2026-08-14: a MESMA violacao em src/ ja reprova hoje e em webui/ passa. Probe escolhida por medicao: knip.json exclui os issue types exports, types e duplicates, entao export nao usado NAO serve de probe; arquivo orfao (issue type files) serve. FORMA SEM LITERAL de proposito: veredito consumido por if/else, nunca por comparacao contra codigo de saida literal (regra frozen-comparand, patterns.md:903). [oracle: file-assert ( p=webui/src/__kniporphan.ts; mkdir -p webui/src; printf 'export const orphanProbe = 42;\\n' > \"$p\"; if bunx --bun knip >/dev/null 2>&1; then rm -f \"$p\"; false; else rm -f \"$p\"; true; fi )]",
      "O gate test-sleeps passou a enxergar os testes do cliente: um sleep de duracao fixa plantado em webui/src reprova o gate. Especie: deteccao de mudanca. Varrido nas DUAS direcoes em 2026-08-14: a MESMA violacao em test/ ja reprova hoje e em webui/ passa. FORMA SEM LITERAL de proposito: veredito consumido por if/else, nunca por comparacao contra codigo de saida literal (regra frozen-comparand, patterns.md:903). [oracle: file-assert ( p=webui/src/__sleepprobe.test.ts; mkdir -p webui/src; printf 'import { test } from \"bun:test\";\\ntest(\"p\", () => { Bun.sleepSync(500); });\\n' > \"$p\"; if bun scripts/check-test-sleeps.ts >/dev/null 2>&1; then rm -f \"$p\"; false; else rm -f \"$p\"; true; fi )]",
      "O gate test-tmpdir passou a enxergar os testes do cliente: uma construcao de path a partir de tmpdir() plantada em webui/src reprova o gate. Especie: deteccao de mudanca. Varrido nas DUAS direcoes em 2026-08-14: a MESMA violacao em test/ ja reprova hoje e em webui/ passa. Probe escolhida por medicao: o gate NAO flagra literal de string /tmp hardcoded (medido, gate verde); ele casa join( seguido de chamada tmpdir(), por regex sobre texto cru. FORMA SEM LITERAL de proposito: veredito consumido por if/else, nunca por comparacao contra codigo de saida literal (regra frozen-comparand, patterns.md:903). [oracle: file-assert ( p=webui/src/__tmpprobe.test.ts; mkdir -p webui/src; printf 'import { join } from \"node:path\";\\nimport { tmpdir } from \"node:os\";\\nexport const d = join(tmpdir(), \"x\");\\n' > \"$p\"; if bun scripts/check-test-tmpdir.ts >/dev/null 2>&1; then rm -f \"$p\"; false; else rm -f \"$p\"; true; fi )]",
      "O gate debt-markers passou a enxergar a arvore do cliente: um marcador de divida sem citacao de issue plantado em webui/src reprova o gate. Especie: deteccao de mudanca. Varrido nas DUAS direcoes em 2026-08-14: a MESMA violacao em src/ ja reprova hoje e em webui/ passa. FORMA SEM LITERAL de proposito: veredito consumido por if/else, nunca por comparacao contra codigo de saida literal (regra frozen-comparand, patterns.md:903). [oracle: file-assert ( p=webui/src/__debtprobe.ts; mkdir -p webui/src; printf '// TODO: probe sem citacao\\nexport const d = 1;\\n' > \"$p\"; if bun scripts/check-debt-markers.ts >/dev/null 2>&1; then rm -f \"$p\"; false; else rm -f \"$p\"; true; fi )]",
      "O unico pacote dispensado da analise de import do knip e o tailwindcss, que e importado de dentro do CSS e portanto invisivel a qualquer analisador de import de TS. Especie: deteccao de mudanca. Varrido RED na main em 2026-08-13 (ignoreDependencies contem apenas jscpd). Este criterio e assercao de config por natureza, e nao violacao plantada, porque nao existe violacao plantavel para uma dispensa de analise. [oracle: file-assert jq -e '.ignoreDependencies|index(\"tailwindcss\")' knip.json >/dev/null]",
      "PINO DE INVARIANCIA: nenhum harness de DOM entra no repositorio, nem como dependencia nem como preload de escopo global. Varrido VERDE na main em 2026-08-13 e provado falsificavel nas DUAS metades na mesma data: injetar happy-dom em devDependencies deixa o pino RED, e injetar um preload happy-dom em bunfig.toml tambem deixa RED. [oracle: file-assert ! grep -qE '\"(happy-dom|jsdom)\"' package.json && ! grep -qF 'happy-dom' bunfig.toml]",
      "PINO DE INVARIANCIA: o bundle da UI NAO entra pelo gerador de embed de templates, nem pelo mapa gerado nem pela arvore de origem. Esta fatia produz o dist e nao o embarca; embarcar e a CAM-560, por outra rota de asset. Varrido VERDE na main em 2026-08-13 e provado falsificavel nas DUAS metades na mesma data: injetar uma raiz webui no mapa gerado deixa o pino RED, e criar templates/webui tambem deixa RED. ESTE ORACULO NAO PODE SER SUBSTITUIDO por 'bun test test/embedded.test.ts': aquele teste compara Object.keys(templatesContents) com o walk de templates/ em disco, entao se o bundle for copiado para templates/ e o codegen regenerado os DOIS lados concordam e o teste fica VERDE, que e precisamente o modo de falha temido. [oracle: file-assert ! grep -q '\"webui/' src/vendor/_generated.ts && ! test -e templates/webui]",
      "A spine de qualidade inteira passa com a arvore nova dentro dela. Especie: deteccao de mudanca (o gate so pode ser avaliado depois que a arvore existe). [oracle: named-command bun run check:all]",
      "O ADR-0068 deixa de afirmar o que esta issue falsifica: a frase que diz que o manifesto GATES fica intocado e o CI fica intocado sai do texto, e o ADR passa a registrar que o comando do gate typecheck no manifesto e re-apontado para o script, sem o que a segunda invocacao de tsc fica INERTE no CI. Especie: deteccao de mudanca. Varrido RED na main em 2026-08-14: a frase falsificada esta viva em docs/adr/0068-o-typecheck-de-arvore-nova-entra-pela-segunda-invocacao-no-script-da-raiz-nao-por-gate-novo-no-manifesto.md linha 9. A emenda aterrissa junto com o codigo que a torna verdadeira, em vez de virar ADR de supersessao escrito antes da mudanca existir. [oracle: file-assert ! grep -qF 'o manifesto GATES fica intocado e o CI fica intocado' docs/adr/0068-*.md && grep -qF 'bun run typecheck' docs/adr/0068-*.md]"
    ],
    "gotchas": [
      "O typecheck da raiz esta INSIDE por default, e nao fora por omissao: tsconfig.json nao tem include nem files, so exclude, entao tsc pega toda a arvore da raiz para baixo. Medido em 2026-08-13: com um .ts plantado em webui/, bunx tsc --noEmit sai exit 2. A exclusao de webui e portanto DELIBERADA (senao a raiz quebra em globais de DOM, porque lib e ESNext e types e bun), e e exatamente por isso que a segunda invocacao de tsc e obrigatoria: sem ela a arvore do cliente fica sem typecheck nenhum.",
      "bun test descobre teste dentro de webui/ com ZERO configuracao: bunfig.toml so tem preload, sem root e sem testMatch. Consequencia dura e nao obvia: o gate skip-ratchet espera EXATAMENTE 0 skips no host, entao qualquer .skip ou skipIf em webui derruba a spine inteira, nao apenas o arquivo. Efeito colateral menor: test/helpers/reap-preload.ts passa a pre-carregar antes dos testes do cliente tambem.",
      "Rota recusada por medicao: composite mais noEmit sob tsc --build funciona no TS 5.9.3 do repo e pega erro real, mas escreve tsconfig.tsbuildinfo (2002 bytes medidos) e --build PULA projeto que julga up-to-date. Isso introduziria verde-pelo-motivo-errado no comando mais invocado do repo. Nao migrar a raiz para tsc --build.",
      "Rota recusada por medicao: registrar um gate typecheck:ui novo no manifesto GATES. O registro e um passo esquecivel e esquecer e INVISIVEL, porque check-ci-parity so quantifica sobre nomes que ja estao em GATES, a Rule 2 e codigo morto (ci.yml roda bun run check:all, entao hasSpine e true) e check:all esta no BUN_RUN_ALLOWLIST da Rule 1. Um script em package.json fora de GATES nunca roda em lugar nenhum e nada acusa.",
      "O gate file-size NAO pode cobrir arvore nova, e por isso ficou de fora: checkSizeCeilings itera Object.entries(budget) e faz continue quando sizes[path] e undefined, entao arquivo sem entrada em scripts/file-size-budget.json nunca e visitado, em qualquer tamanho. Medido em 2026-08-13: arquivo novo de 3000 linhas em src/ sai exit 0. A varredura de glob so monta a tabela de lookup, nao enumera violacao.",
      "ACHADO FORA DO ESCOPO desta issue, registrado aqui para quem for mexer no gate: o ratchet de file-size esta congelado desde que nasceu. Nada obriga arquivo novo a ganhar entrada no orcamento, entao todo arquivo criado depois do snapshot esta descoberto no repo INTEIRO, nao so em webui. Os 167 entries cobraveis estao todos exatamente no teto (headroom zero), e test/review.test.ts ja e entrada morta que no-opa silenciosamente pelo mesmo continue.",
      "biome.json tem recommended:false e SOMENTE duas regras ativas: noExcessiveCognitiveComplexity e noExcessiveLinesPerFunction com maxLines:80. Cobrir webui com biome compra teto de tamanho de funcao e complexidade, e mais nada: variavel nao usada, import morto e regras de correctness NAO sao verificadas. Contagem de linha exclui linha so-comentario e inclui linha em branco. Existem 21 overrides desligando uma ou ambas as regras para arquivos nomeados; nenhum deles esta em webui.",
      "O knip nao e silencioso quando a arvore fica de fora: ele continua auditando o package.json da raiz e reporta dependencia importada apenas de arquivo nao-analisado como unused, ou seja gate VERMELHO e nao pass silencioso. Com webui dentro de entry e project, a regra de que cada dependencia e declarada na story que primeiro a importa passa a ser enforcada MECANICAMENTE: declarar cedo deixa o gate vermelho.",
      "Folga do ratchet de cobertura medida em 2026-08-13: +16,36pp em funcoes e +14,16pp em linhas (92,01/92,79 medido contra pisos 75,65/78,63 com tolerancia de 0,5pp). Bun conta modulo CARREGADO e nao walk estatico de arquivo. A arvore nova nao ameaca o gate de cobertura e nenhum escopo de bunfig e necessario. Nota de lado: os pisos sao snapshot de junho (CAM-60) e a suite drifou 14 a 16pp acima sem nunca ser re-ratchetada.",
      "As probes dos oraculos de gate criam webui/src antes de escrever e removem apenas o ARQUIVO, nunca o diretorio. Nao trocar por rm -rf webui: depois da mudanca esse diretorio contem a arvore real. Diretorio vazio nao e rastreado pelo git, entao a varredura na main nao deixa residuo.",
      "Todo oraculo que assevera REPROVACAO de gate consome o veredito por if/else e nunca por captura de codigo de saida comparada com literal (test \"$c\" -ne 0). A regra frozen-comparand do lint deterministico (patterns.md:903) flagra a segunda forma, e a regra do repo manda reescrever sem literal em vez de pedir isencao. Medido em 2026-08-14: as seis formas com captura de codigo de saida foram rejeitadas pelo lint antes de o auditor ser spawnado, a custo zero de rodada.",
      "Todo oraculo de violacao plantada roda dentro de subshell ( ... ) para nao vazar as variaveis do probe (p, n, i) para o shell que o avalia. Medido em 2026-08-14: sem o subshell, o contador i do probe do biome sobrescreveu o contador do laco que avaliava os oraculos em sequencia. Nao remover os parenteses.",
      "O manifesto GATES NAO roda os scripts do package.json: cada gate carrega o proprio comando, e o gate typecheck declara bunx tsc --noEmit direto (scripts/check-all.ts:167). Editar apenas .scripts.typecheck do package.json deixa a segunda invocacao INERTE no CI, porque check:all e o CI executam o comando do manifesto. Hoje os dois textos sao identicos, o que torna a divergencia invisivel a leitura. Qualquer criterio que queira provar cobertura de gate precisa derivar o comando de GATES ao vivo, nunca rodar o script.",
      "Probe plantada em webui/src que seja arquivo NOVO reprova o gate de dead-code assim que o knip passa a enxergar a arvore (issue type files nao esta no exclude do knip.json). Por isso o probe do gate typecheck vai DENTRO de webui/src/main.tsx, que ja e entry, e nunca num arquivo orfao novo: com arquivo novo o oraculo fica vermelho com ou sem a mudanca que ele deveria detectar.",
      "relatedDocs desta fatia tem que citar ADR-0064 (bundle nao embarcado, origem do pino de embed), ADR-0067 (teste de componente sem harness de DOM, origem do pino de harness) e ADR-0068 (o split de typecheck, emendado por esta issue), alem do ADR-0063. officialDocsConsulted tem que carregar linha para @biomejs/biome 2.5.1 (semantica de files.includes, que decide se o gate alargado enxerga webui de fato) e para react-dom (entry de cliente), alem de tailwindcss, vite e knip."
    ],
    "domainTerms": [
      "spine de gates: o conjunto de 16 gates registrados no manifesto GATES de scripts/check-all.ts, executado por bun run check:all e usado pelo CI para decidir o merge.",
      "gate path-scoped: gate cuja cobertura e delimitada por glob, whitelist de config ou argumento posicional, e que portanto deixa arvore nova de fora por omissao em vez de por decisao.",
      "violacao plantada: forma de oraculo que escreve um arquivo violando a invariante do gate dentro da arvore alvo, roda o comando daquele gate isolado, assevera exit nao-zero e remove o arquivo. Distingue glob certo de glob presente porem errado, coisa que assercao de texto de config nao faz.",
      "varredura nas duas direcoes: provar que a MESMA violacao ja reprova dentro do escopo atual do gate (a forma da violacao e real) e passa na arvore alvo (o buraco existe), com controle verde sem probe. Varrer so o vermelho na main nao detecta vacuidade por forma de violacao invalida."
    ]
  },
  "wsjf": {
    "value": 5,
    "timeCriticality": 3,
    "riskReduction": 8,
    "jobSize": 3
  },
  "type": "feat"
}
