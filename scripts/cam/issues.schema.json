{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Cam Issue Entry",
  "description": "Schema for a single per-file issue managed by cam issue commands.",
  "type": "object",
  "required": ["id", "title", "stage", "status", "blockedBy", "createdAt", "updatedAt"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Issue identifier, e.g. CAM-42."
    },
    "title": {
      "type": "string",
      "description": "Short human-readable title."
    },
    "stage": {
      "type": "string",
      "enum": ["idea", "specified", "planned", "shipped"],
      "description": "Lifecycle stage: idea -> specified -> planned -> shipped."
    },
    "status": {
      "type": "string",
      "enum": ["open", "abandoned"],
      "description": "open = active; abandoned = closed without shipping."
    },
    "blockedBy": {
      "type": "array",
      "description": "List of issue ids that block this issue.",
      "items": { "type": "string" }
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 creation timestamp."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 last-activity timestamp. Equal to createdAt at creation time."
    },
    "description": {
      "type": "string",
      "description": "Optional free-text description or motivation."
    },
    "wsjf": {
      "type": "object",
      "description": "WSJF scoring (Weighted Shortest Job First). Populated in Epico B.",
      "required": ["value", "timeCriticality", "riskReduction", "jobSize"],
      "properties": {
        "value": { "type": "number", "description": "Composite WSJF score." },
        "timeCriticality": { "type": "number" },
        "riskReduction": { "type": "number" },
        "jobSize": { "type": "number" }
      },
      "additionalProperties": false
    },
    "rank": {
      "type": "number",
      "description": "Implementation priority rank. Lower is higher priority. Populated in Epico C."
    },
    "spec": {
      "description": "Structured specification artifact. Shape is defined by Epico B."
    },
    "specSource": {
      "type": "string",
      "enum": ["interview", "derived", "operator"],
      "description": "How the spec was produced: interview (interactive spec interview session), derived (consolidated from sibling issues), operator (hand-filed by operator without a spec interview)."
    },
    "derivedFrom": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Issue ids this issue was derived from. Required (minItems 1) when specSource is derived."
    },
    "type": {
      "type": "string",
      "enum": ["feat", "fix", "chore", "docs"],
      "description": "Optional PR-title/label signal. Defaults to feat on read when absent."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "stage": { "const": "specified" } },
        "required": ["stage"]
      },
      "then": {
        "required": ["specSource"]
      }
    },
    {
      "if": {
        "properties": { "specSource": { "const": "derived" } },
        "required": ["specSource"]
      },
      "then": {
        "required": ["derivedFrom"],
        "properties": { "derivedFrom": { "minItems": 1 } }
      }
    },
    {
      "if": {
        "properties": { "specSource": { "enum": ["derived", "operator"] } },
        "required": ["specSource"]
      },
      "then": {
        "required": ["description"],
        "properties": { "description": { "minLength": 1 } }
      }
    }
  ],
  "additionalProperties": false
}
