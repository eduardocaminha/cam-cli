## Codebase Patterns

Durable patterns carried across cycles. Implementers append new ones as they emerge.

- **Bun runtime**: prefer `Bun.spawn` / `Bun.$` / `Bun.file` over `node:child_process` / `node:fs` in NEW code. But match the surrounding file's idiom: `src/commands/run.ts`, `dashboard.ts`, and `status.ts` already use `node:fs` / `node:path` / `node:child_process`; keep that style there, do not mix `Bun.file` mid-file.
- **Permission mode**: never register a `--permission-mode` CLI flag on any subcommand parser, enforced by `test/no-permission-mode-flag.test.ts` (greps `index.ts` and `src/commands/`). The literal may appear only inside a forwarded argv (e.g. the orchestrator `claude --permission-mode bypassPermissions` in `run.ts`, intentional, and the spawned worker argv in CAM-22).
- **noUncheckedIndexedAccess**: tsconfig has it on. Array indexing and regex capture groups are `T | undefined`; guard with `?? fallback` or a justified non-null assertion. JSON.parse results need runtime shape checks before property access.
- **`bun test` green is NOT proof of type-safety**: the runner passes even when `tsc --noEmit` would fail. Always run `bun run typecheck` separately and trust its exit code before claiming the gate passed.
- **Read-only readers never throw**: `readSnapshot` / `buildStatusReport` and their helpers wrap every IO in try/catch and fall through to defaults, because dashboard/status are read-only and a render failure must not brick the operator's terminal (see `readPrd` / `readState` in `dashboard.ts`).
- **Transcript discovery**: Claude Code transcripts live at `~/.claude/projects/<encode(cwd)>/<sessionId>.jsonl`, where `encode = cwd.replace(/[^a-zA-Z0-9]/g, '-')` (verified empirically). Filename == sessionId. Each line is one JSON object; assistant lines carry `message.usage` with `input_tokens` / `output_tokens` / `cache_read_input_tokens` / `cache_creation_input_tokens`. Dedupe by `(message.id, requestId)` before summing (one JSONL line per content block carries identical usage; inflation ~1.44x).
- **src/tmux/session.ts is the canonical session module**: `projectSessionName`, `ensureProjectSession`, `openPaneInSession`, `isInsideProjectSession`, `tmuxArgs` all live here with injectable `SpawnFn`. All tmux calls go through `tmuxArgs(sub)` which prepends `['-L', 'cam']`; test fakes route by `args[2]` (guard: `args[0] === '-L' ? args[2] : args[0]`).
- **tmux worker primitives (CAM-22)**: `respawn-pane -k -t <pane> <cmd>` kills the running command and reuses the SAME pane id; `wait-for <chan>` blocks the client until `wait-for -S <chan>` signals it (the signal is NOT guaranteed buffered if sent before the wait, so always confirm the outcome by reading `prd.json` / `handoff.json`, never trust the channel alone); `capture-pane -p [-S -N]` dumps a pane to stdout for sentinel grepping.
- **Claude worker invocation (CAM-22)**: custom slash commands do NOT run in headless `-p` mode. A worker session is `claude -p --permission-mode <mode> --session-id <uuid> --output-format json --agent <name> "<task>"`, never `claude -p "/cam-..."`. Autonomous workers (implementer/reviewer) exit on their own so a chained `; tmux -L cam wait-for -S <chan>` works; interactive workers (plan, optional review) do NOT exit, so detect completion by scraping `capture-pane -p` for the `CAM_*_STATUS` / `<review>` sentinel.
- **Ink screens**: success/failure is signalled by the glyph (✓ accent / ✗ destructive), never by divider color. Render and look at the real output; do not trust header comments (see `lessons.md` 2026-06-05).
- **Tests**: `bun test` from repo root. Tests for `src/commands/*` and `src/tmux/*` live FLAT at `test/<name>.test.ts` (e.g. `test/next.test.ts`, `test/tmux-session.test.ts`); subdir mirrors exist for `test/retry/*`, `test/transcript/*`, and (new in CAM-22) `test/supervisor/*`. Inject fake reader/writer/spawn shapes (and injectable `home` / `genSessionId`) instead of touching real stdin/stdout/`~/.claude`/tmux.
- **Commits**: conventional commits required (`feat:`, `fix:`, `chore:`, `refactor:`, `docs:`).
- **No em dashes** in persisted files (PRD notes, progress, docs): use colon, comma, parentheses, or period.
- **Pure transcript parser pattern**: `parseTranscriptUsage` splits on `\n`, `JSON.parse` each trimmed line in a try/catch, checks `message?.usage` existence, accumulates with `toNumber()` guard (returns 0 for non-number). `formatTokens` uses `>= 1_000_000` / `>= 1_000` thresholds. Module lives in `src/transcript/usage.ts`; tests under `test/transcript/usage.test.ts`. `orchestratorTranscriptPath(cwd, claudeDir)` reads `.claude/.cam-orch-session`; CAM-22 generalizes it to `transcriptPathForSession(uuid, cwd, claudeDir)`.
- **openPaneInSession returns pane id (US-001)**: the function now uses `stdio: 'pipe'` and `-P -F #{pane_id}` to capture and return the stable pane id string (e.g. `%5`). All test fakes for split-window must handle `opts?.stdio === 'pipe'` and return a non-empty `stdout` buffer, or they will crash with `Cannot read property toString of undefined`.
- **Worker pane marker (US-001)**: the first write of `.claude/.cam-worker-pane` happens in `plan.ts` immediately after `openPaneInSession`. Supervisor and lifecycle commands address the slot via `readWorkerPaneMarker(claudeDir)`. Returns null if the file is missing or empty; never throws.

## 2026-06-08 19:00 - US-006: runSupervisor() loop tying primitives together

### What was done
- Created `src/supervisor/loop.ts` with `runSupervisor(opts): Promise<SupervisorResult>`.
- All side effects injected: spawn, waitFor, capturePane, readPrd, writePrd, readHandoff, clock, genUuid, genChannel, reviewDispatch, writeSessionMarker.
- Per iteration: calls decideNextAction -> implement branch mints uuid + channel, respawns worker pane, waits on channel, captures pane, calls readWorkerOutcome, then calls writeSessionMarker(actualStoryId, uuid) keyed to actual completed story (not advisory).
- Review branch is a placeholder: calls injected reviewDispatch(uuid), exits blocked on error, continues on ok.
- Hard max-iterations cap defaults to 50 (MAX_ITERATIONS constant).
- Returns `{ status: 'complete' | 'blocked' | 'max-iterations', iterations, lastOutcome }`.
- Worker outcome 'fail' or 'unknown' exits as 'blocked' (conservative: do not retry silently).
- Created `test/supervisor/loop.test.ts` with 14 tests covering: complete-immediately, blocked-immediately, PRD-unreadable, happy-path-2-stories, one-blocked, max-iterations, PRD_COMPLETE-cycle, fail-outcome, unknown-outcome, review-dispatch-error, writeSessionMarker-with-actual-id, spawn-called-per-iteration.

### Files changed
- `src/supervisor/loop.ts` (new)
- `test/supervisor/loop.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 621 pass

### Step 5.5 docs validation
- lib: none (pure TS, no external libs), status: no_external_lib_touched

### Notes
- `fileReader` for `readWorkerOutcome` calls `readPrd()` once (after worker exits) to get updated prd state. Tests must account for 2 readPrd() calls per implement iteration (top-of-loop + fileReader), except PRD_COMPLETE path which skips the fileReader.
- Advisory storyId from decideNextAction is used only for channel naming and logging; writeSessionMarker is always called with outcome.storyId (actual story from handoff/sentinel).
---

## 2026-06-08 18:00 - US-005: Port Branch-decision matrix to pure TS

### What was done
- Created `src/supervisor/decide.ts` with `decideNextAction(prd): SupervisorAction`.
- Returns one of: `{ kind: 'implement', storyId }`, `{ kind: 'review' }`, `{ kind: 'complete' }`, `{ kind: 'blocked-no-implementable' }`.
- Mirrors cam-next.md matrix: implementable story with passes=false picked first (priority asc, id asc tie-break), then blocked-no-implementable if only operator stories remain, then review/complete based on review state.
- Cap row: `roundsCompleted >= maxRounds` (default 3) returns complete regardless of lastVerdict.
- Terminal verdicts: CLEAN and MAX_ROUNDS_DEBT both return complete.
- Created `test/supervisor/decide.test.ts` with 23 tests covering all 4 outcome kinds, tie-breaking, operator-only, cap row, and implement-over-review priority.

### Files changed
- `src/supervisor/decide.ts` (new)
- `test/supervisor/decide.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 607 pass

### Step 5.5 docs validation
- lib: none (pure TS, no external libs), status: no_external_lib_touched

### Notes
- `blocked-no-implementable` fires when operator-incomplete stories exist even if non-operator stories all pass; review/complete only fires when ALL stories (including operator ones) have passes=true. This is the semantically correct interpretation: operator-pending stories block autonomous loop progression.
---

## 2026-06-08 17:00 - US-004: Deterministic worker result reader

### What was done
- Created `src/supervisor/result.ts` with `readWorkerOutcome({ prdPath, handoffPath, capturedPaneText, readFile })`.
- Returns `{ kind: 'pass' | 'fail' | 'blocked' | 'unknown', storyId: string | undefined, detail: string }`.
- Pure function: all file I/O injected via `FileReader = (path: string) => string | null` callback.
- Sentinel parsed from pane text via regex (`CAM_IMPLEMENTER_STATUS=(\S+)(?:\s+story=(US-\S+))?`); guarded for `noUncheckedIndexedAccess`.
- `pass` requires all three signals to agree: sentinel is DONE, `handoff.json lastCompletedStory.id` matches sentinel `story=US-XXX`, and prd.json shows `passes:true` for that story.
- `blocked` detected by any `BLOCKED_*` sentinel prefix OR a raw `BLOCKED_\w+` token in pane text without a full sentinel.
- `PRD_COMPLETE` sentinel produces `kind: 'pass', storyId: undefined` (all stories already passing).
- `unknown` when no sentinel found and no blocked token present.
- Created `test/supervisor/result.test.ts` with 14 tests covering all four outcome kinds with fake prd/handoff fixtures.

### Files changed
- `src/supervisor/result.ts` (new)
- `test/supervisor/result.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 584 pass

### Step 5.5 docs validation
- lib: none (no external libs; pure JSON/regex only), status: no_external_lib_touched

### Notes
- Guard pattern for `noUncheckedIndexedAccess` on regex: `match[1] ?? ''` and `match[2]` (already `string | undefined`, no assertion needed).
- Three-way cross-check (sentinel + handoff + prd) is the architectural invariant; any single source alone is insufficient.
---

## 2026-06-08 16:00 - US-003: Headless implementer worker argv builder

### What was done
- Created `src/supervisor/worker-argv.ts` with `buildImplementerWorkerArgv(opts)`: pure function, no I/O, returns the shell string for respawn-pane.
- Shape: `claude -p --permission-mode <mode> --session-id <uuid> --output-format text --agent <agentName> '<task>' ; tmux -L cam wait-for -S '<channel>'`.
- `shellEscape(s)` wraps the value in single quotes and replaces embedded `'` with `'\''`; applied to both taskPrompt and channel.
- Default `agentName` is `'subagent-implementer'` (exported as `DEFAULT_IMPLEMENTER_AGENT`).
- Created `test/supervisor/worker-argv.test.ts` with 11 tests: all required flags, wait-for chain ordering, prompt with `'` + `$` + backtick escaping, channel in wait-for argument, agentName override, and DEFAULT_IMPLEMENTER_AGENT value assertion.

### Files changed
- `src/supervisor/worker-argv.ts` (new)
- `test/supervisor/worker-argv.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 570 pass

### Step 5.5 docs validation
- lib: none (no external libs; pure string manipulation only), status: no_external_lib_touched

### Notes
- Shell escaping uses POSIX single-quote strategy (`'...'` + `'\''` for embedded quotes); chosen over double-quote because single quotes neutralize `$`, backtick, `\`, and `!` without special-casing each.
---

## 2026-06-08 15:00 - US-002: Per-story session marker + transcriptPathForSession resolver

### What was done
- Added `transcriptPathForSession(uuid, cwd, claudeDir)` to `src/transcript/usage.ts`: generalized resolver that builds the `<claudeDir>/projects/<encode(cwd)>/<uuid>.jsonl` path from an explicit uuid.
- Refactored `orchestratorTranscriptPath(cwd, claudeDir)` into a thin wrapper over `transcriptPathForSession`; external API unchanged.
- Added `writeWorkerSessionMarker(claudeDir, storyId, uuid)` and `readWorkerSessionMarker(claudeDir, storyId)` for `.claude/.cam-worker-<storyId>.session` round-trip; returns null on absence/empty; creates dir if needed.
- Added `WORKER_SESSION_MARKER_PREFIX` constant (module-private) for consistent filename construction.
- Extended `test/transcript/usage.test.ts` with 10 new tests covering `transcriptPathForSession` (3 tests) and marker round-trip (7 tests); updated import list.

### Files changed
- `src/transcript/usage.ts`
- `test/transcript/usage.test.ts`

### Quality gates
- Typecheck: ok
- Lint: n/a (no linter configured)
- Tests: 559 pass

### Step 5.5 docs validation
- lib: none (only Node.js built-ins fs, path; no new external libs), status: no_external_lib_touched

### Notes
- `orchestratorTranscriptPath` now delegates to `transcriptPathForSession`; all existing tests still pass unchanged, confirming backward compatibility.
---

## 2026-06-08 14:00 - US-001: tmux worker-slot primitives in session.ts

### What was done
- Added `respawnPaneArgv`, `waitForArgv`, `signalWaitForArgv`, `capturePaneArgv` pure argv builders to `src/tmux/session.ts`, all prepending `-L cam` via `tmuxArgs`.
- Added `workerWaitChannel(storyId, uuid)` for deterministic channel naming.
- Added `writeWorkerPaneMarker(claudeDir, paneId)` and `readWorkerPaneMarker(claudeDir)` for `.claude/.cam-worker-pane` persistence; exported `WORKER_PANE_MARKER` constant.
- Changed `openPaneInSession` return type from `void` to `string`: adds `-P -F #{pane_id}` and `stdio: 'pipe'` to the split-window call, returns the captured pane id.
- Updated `plan.ts` to capture the returned pane id and call `writeWorkerPaneMarker` before returning (first write of the slot marker).
- Extended `test/tmux-session.test.ts` with argv-shape tests for all new builders, marker round-trip tests, and openPaneInSession return value test.
- Extended `test/plan.test.ts` with two tests verifying `.claude/.cam-worker-pane` is written with a non-empty pane id.
- Fixed `test/attach-hint.test.ts` fake spawn to handle `stdio: 'pipe'` for split-window (returning a `stdout` buffer), which broke after the openPaneInSession signature change.

### Files changed
- `src/tmux/session.ts`
- `src/commands/plan.ts`
- `test/tmux-session.test.ts`
- `test/plan.test.ts`
- `test/attach-hint.test.ts`

### Quality gates
- Typecheck: ok
- Lint: n/a (no linter configured)
- Tests: 548 pass

### Step 5.5 docs validation
- lib: none (no new external libraries; only Node.js built-ins `fs`, `path`, `crypto`), status: no_external_lib_touched

### Notes
- The `attach-hint.test.ts` fake was the only collateral breakage: it returned bare `{ status: 0 }` without `stdout`, which crashed when `openPaneInSession` called `.stdout.toString()`. Pattern: any fake spawn for split-window/new-session must include `stdout: Buffer.from(...)` to avoid crashes on the return value.
---
