## Codebase Patterns

<!--
Populated by implementers as reusable patterns emerge during stories.
Examples of what belongs here:

- **Bun runtime**: always `Bun.spawn`/`Bun.$`/`Bun.file` over `node:child_process`/`fs` per project CLAUDE.md.
- **Permission mode**: never register a `--permission-mode` CLI flag on any subcommand —
  enforced by `test/no-permission-mode-flag.test.ts` (greps `index.ts` and `src/commands/`).
- **Tests**: `bun test` from repo root; test files mirror source under `test/`.
- **Conventional commits**: `feat:`, `fix:`, `chore:`, `refactor:`, `docs:`.
- **License attribution**: ported files under `src/retry/` carry the MIT header
  (claude-auto-retry v0.2.2, cheapestinference contributors, Eduardo Caminha port).
- **noUncheckedIndexedAccess**: tsconfig has `noUncheckedIndexedAccess: true`. Array indexing returns `T | undefined`; always guard with `?? fallback` or non-null assertion when the index is provably in-bounds.
- **Regex capture groups**: `match()` capture groups are typed as `string | undefined` under strict+noUncheckedIndexedAccess — always use `?? ''` fallback on capture group access.
- **Bun.spawn detached option**: `detached?: boolean` is in `bun.d.ts` (calls `setsid()` on POSIX) but the docs webpage omits it. Check `node_modules/bun-types/bun.d.ts` directly for full option surface. Always pair with `proc.unref()` and `stdio: ['ignore','ignore','ignore']` to prevent parent from waiting on child.
-->

## [2026-05-14 06:00] - US-007: Remove external claude-auto-retry dependency from init.ts, resume.ts, embedded vendor, and tests

### What was done
- Removed `validateClaudeAutoRetry()` from `src/commands/init.ts` and its call in `runInit()`. Removed vendored smoke for `claude-auto-retry-patterns.ts` from init flow.
- Replaced `isAutoRetryAlive(spawnFn)` in `src/commands/resume.ts` with `isRetryMonitorAlive()` — reads `~/.cam/retry.pid` via `readRetryPid()` + `isRetryPidAlive()` from `src/util/retry-pid.ts`. Added `retryMonitorFn?: () => boolean` to `ResumeOptions` for test injection.
- Renamed `autoRetryAlive` field in `ResumeReport` to `retryPidAlive`.
- Scrubbed `claude-auto-retry` from `index.ts` INIT_HELP and RESUME_HELP strings.
- Cleaned comment in `src/commands/status.ts`.
- Removed `claudeAutoRetryPatternsContents` import, `'claude-auto-retry-patterns.ts'` union member, and map entry from `src/vendor/embedded.ts`.
- Deleted `vendor/claude-auto-retry-patterns.ts`.
- Removed `claude-auto-retry-patterns.ts` from `scripts/generate-embedded-vendor.ts` file list.
- Regenerated `src/vendor/_generated.ts` via codegen script.
- Updated `vendor/check-agent-frontmatter.sh` and `scripts/build-release.sh` comments.
- Updated `vendor/README.md` to remove all references to the deleted file.
- Updated `test/embedded.test.ts` (removed key from VENDOR_KEYS and expected list).
- Updated `test/vendor.test.ts` comment.
- Rewired `test/resume.test.ts`: `isAutoRetryAlive` → `isRetryMonitorAlive`, `makeFakeSpawn` lost `autoRetryAlive` handler, all `buildResumeReport`/`runResume` calls now inject `retryMonitorFn`.
- Rewired `test/resume/modes.test.ts`: Mode 4 tests use `retryMonitorFn: () => true`, spy test updated to assert `pgrep` is absent.
- Rewired `test/fixtures/resume/scenario.ts`: renamed `autoRetryAlive` → `retryPidAlive` in `SpawnHandlers`, `makeFakeSpawn` no longer dispatches pgrep calls.
- Updated `test/init.test.ts` and `README.md` comments.

### Files changed
- src/commands/init.ts (modified: remove validateClaudeAutoRetry, remove smoke runner call for claude-auto-retry-patterns)
- src/commands/resume.ts (modified: replace isAutoRetryAlive with isRetryMonitorAlive, add retryMonitorFn injection, rename autoRetryAlive → retryPidAlive)
- src/commands/status.ts (modified: comment cleanup)
- src/util/retry-pid.ts (modified: comment cleanup)
- src/vendor/embedded.ts (modified: remove import + union member + map entry)
- src/vendor/_generated.ts (modified: regenerated - removed claudeAutoRetryPatternsContents)
- index.ts (modified: scrub INIT_HELP and RESUME_HELP)
- vendor/claude-auto-retry-patterns.ts (deleted)
- vendor/check-agent-frontmatter.sh (modified: comment cleanup)
- vendor/README.md (modified: remove claude-auto-retry-patterns.ts references)
- scripts/build-release.sh (modified: comment cleanup)
- scripts/generate-embedded-vendor.ts (modified: remove claude-auto-retry-patterns.ts entry)
- test/embedded.test.ts (modified: remove claude-auto-retry-patterns.ts from VENDOR_KEYS and expected list)
- test/vendor.test.ts (modified: comment cleanup)
- test/resume.test.ts (modified: rewire isAutoRetryAlive tests, add retryMonitorFn to all calls)
- test/resume/modes.test.ts (modified: Mode 4 retryMonitorFn, spy test updated)
- test/fixtures/resume/scenario.ts (modified: rename autoRetryAlive → retryPidAlive, remove pgrep dispatch)
- test/init.test.ts (modified: comment cleanup)
- README.md (modified: remove claude-auto-retry prerequisite)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 417 pass / 0 fail

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched
- No new external library exercised. All changes are removal of external-tool references and rewiring to internal PID-file helpers already validated in US-006.

### Notes
- `ResumeOptions.retryMonitorFn` is the injection point for tests to control Mode 4 (retry-monitor alive). All test calls now explicitly pass `retryMonitorFn: () => false` (for non-noop paths) or `retryMonitorFn: () => true` (for Mode 4 noop tests) to avoid flakiness from the real `~/.cam/retry.pid` on the host machine.
- The `SpawnSyncFn` in `resume.ts` is now only used for git log (not pgrep). The spy assertion in Mode 1 test was updated to assert `pgrep` is absent and only `git` is present.
---

## [2026-05-14 05:00] - US-006: Switchover: route internal claude spawns through cam claude and add retry-pid helpers

### What was done
- Created `src/util/retry-pid.ts` exporting `retryPidPath`, `writeRetryPid`, `readRetryPid`, `isRetryPidAlive`, `removeRetryPid`. PID file lives at `~/.cam/retry.pid`. Liveness uses `process.kill(pid, 0)` — throws ESRCH if dead, EPERM if alive-but-not-ours (treated as alive).
- Updated `DetachedSpawnAdapter` interface in `src/retry/launcher.ts` to return `number` (the child PID) instead of `void`. Updated `defaultDetachedSpawn` to return `proc.pid`.
- Added `onMonitorSpawned?: (monitorPid: number) => void` to `InteractiveOptions`. `forkMonitor` now calls `onMonitorSpawned(monitorPid)` immediately after spawning the child.
- Updated `src/commands/claude.ts`: imports `writeRetryPid` and `removeRetryPid`; passes `onMonitorSpawned: writeRetryPid` and wires `cleanup: () => removeRetryPid()` in the `forkMonitor` call.
- Fixed comment in `src/commands/next.ts` line 148: removed outdated "claude or claude-auto-retry" reference — now reads "claude, spawned by cam next".
- Updated `test/retry/interactive.test.ts`: all `DetachedSpawnAdapter` fakes now return `0` (required by the new `number` return type). Added 2 new tests for `onMonitorSpawned` callback.
- Created `test/retry/retry-pid.test.ts` with 16 tests covering write/read/liveness/remove with the real `~/.cam/retry.pid` path (saves and restores in beforeEach/afterEach to avoid polluting the host machine).

### Files changed
- src/util/retry-pid.ts (new)
- test/retry/retry-pid.test.ts (new)
- src/commands/claude.ts (modified: import retry-pid, wire onMonitorSpawned + cleanup)
- src/commands/next.ts (modified: comment fix — remove claude-auto-retry reference)
- src/retry/launcher.ts (modified: DetachedSpawnAdapter returns number, forkMonitor calls onMonitorSpawned)
- test/retry/interactive.test.ts (modified: fake adapters return 0, 2 new tests)
- scripts/cam/prd.json (modified: US-006 passes: true)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 419 pass / 0 fail (18 new: 16 retry-pid + 2 interactive)

### Step 5.5 docs validation
- lib: node:process.kill (built-in), status: no_external_lib_touched
- No third-party library exercised. `process.kill(pid, 0)` is documented Node.js built-in behavior; no fetch needed.

### Notes
- `DetachedSpawnAdapter` signature change (void → number) required updating all fake adapters in existing interactive tests. Any future story that adds a fake `DetachedSpawnAdapter` must return a `number`.
- `isRetryPidAlive` treats EPERM as "alive" — a process owned by a different UID is still running, just not signable by us.
- US-007 is the wider cleanup: remove claude-auto-retry checks from init.ts, resume.ts, index.ts HELP strings.
---

## [2026-05-14 04:00] - US-005: Implement interactive mode with monitor fork and cam retry-monitor internal subcommand

### What was done
- Extended `src/retry/launcher.ts` with `forkMonitor(opts)` function that forks a detached `cam retry-monitor <pane> <pid>` child via `Bun.spawn({ detached: true, stdio: ['ignore','ignore','ignore'] })` and `proc.unref()`. Both `DetachedSpawnAdapter` and `SignalHandler` are injectable for tests.
- Added `validateInteractiveEnv(args, tmux)` helper that returns `null` when inside tmux and no `-p`/`--print` in args, or an error string otherwise.
- Updated `src/commands/claude.ts` routing: when args include `-p`/`--print` → print mode (existing `launchClaude`); when `$TMUX` set and no print → interactive mode (`Bun.spawn` attached to TTY + `forkMonitor`); otherwise → error pointing at `cam run`.
- Created `src/commands/retry-monitor.ts` with `runRetryMonitor`, `parseRetryMonitorArgs`, `RETRY_MONITOR_HELP`. Not advertised in top-level `HELP`; responds to `--help`. Does NOT register `--permission-mode`.
- Wired `case 'retry-monitor'` in `index.ts` (after `case 'claude'`). Import added.
- Created `test/retry/interactive.test.ts` with 22 tests covering: detached spawn argv structure, signal handler registration, SIGINT/SIGTERM cleanup, validateInteractiveEnv paths, non-tmux error path, print-mode routing, parseRetryMonitorArgs, and runRetryMonitor injectable monitor.

### Files changed
- src/retry/launcher.ts (modified: added forkMonitor, validateInteractiveEnv, DetachedSpawnAdapter, SignalHandler, InteractiveOptions)
- src/commands/claude.ts (modified: added interactive routing, forkMonitor call, findClaudeBinary/filteredProcessEnv helpers)
- src/commands/retry-monitor.ts (new)
- index.ts (modified: import + case 'retry-monitor')
- test/retry/interactive.test.ts (new, 22 tests)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 401 pass / 0 fail (22 new + 379 pre-existing)

### Step 5.5 docs validation
- lib: Bun.spawn (detached option), status: ok
- Confirmed: `detached?: boolean` is in `bun.d.ts` (calls `setsid()` on POSIX). `proc.unref()` exists on `Subprocess` and detaches from parent event loop. `stdio: ['ignore','ignore','ignore']` maps to `[Writable, Readable, Readable]` tuple. Doc page didn't list `detached` but the type definition confirms it. Implementation matches API.

### Notes
- `detached` option is in `bun.d.ts` but the online docs page omits it. Always cross-check the type definitions in `node_modules/bun-types/bun.d.ts` for Bun API completeness.
- `forkMonitor` uses `process.argv[1]` as the main script path so the child is invoked as `[bunExecPath, mainScript, 'retry-monitor', pane, pid]`. In production, `mainScript` is the cam binary path.
- `validateInteractiveEnv` returns an error string (not throws) so callers can choose how to surface it.
---

## [2026-05-14 03:00] - US-004: Implement launcher.ts and cam claude subcommand (print mode)

### What was done
- Created `src/retry/launcher.ts` with `launchClaude(opts)` function that spawns `claude` in print mode via `Bun.spawn`, captures stdout+stderr, applies rate-limit retry policy, and replays buffered output on clean exit.
- `SpawnAdapter` interface makes the spawn function injectable so tests never spawn a real claude process.
- `bunSpawnAdapter` is the default (uses `Bun.spawn` with `stdin: 'inherit'`, `stdout: 'pipe'`, `stderr: 'pipe'`); uses `new Response(proc.stdout).text()` pattern.
- Does NOT call `createTmuxSession` — only print mode (interactive/tmux is US-005).
- Carries MIT attribution header.
- Created `src/commands/claude.ts` with `parseClaudeArgs` and `runClaude`. The parser consumes only a leading `--help` / `-h`; all other args forwarded verbatim including `--permission-mode`.
- Does NOT register `--permission-mode` as a cam flag — textual smoke in `test/no-permission-mode-flag.test.ts` still passes.
- Wired `case 'claude'` into `index.ts`; added `claude [args...]` line to global HELP.
- Created `test/retry/launcher.test.ts` with 12 tests covering args forwarding, exit-code propagation, rate-limit retry, and exhausted retries.

### Files changed
- src/retry/launcher.ts (new)
- src/commands/claude.ts (new)
- test/retry/launcher.test.ts (new)
- index.ts (modified: added claude import, HELP line, switch case)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 379 pass / 0 fail (12 new + 367 pre-existing)

### Step 5.5 docs validation
- lib: Bun.spawn, status: ok
- Confirmed: `Bun.spawn(argv, { stdout: 'pipe', stderr: 'pipe' })` returns Subprocess with `.stdout: ReadableStream`, `.stderr: ReadableStream`, `.exited: Promise<number>`. `new Response(proc.stdout).text()` is valid (equivalent to `proc.stdout.text()`). `proc.exited` resolves to the exit code as a number. API matches implementation exactly.

### Notes
- `parseClaudeArgs` only checks the first arg for `--help`; any other position is forwarded. This matches the acceptance criterion: "cam does not parse the forwarded argv beyond consuming a leading `--help`".
- `filteredProcessEnv()` strips `undefined` values from `process.env` since Bun.spawn's `env` type is `Record<string, string>` not `Record<string, string | undefined>`.
- setTimeout patching in tests: override with `(fn, _ms) => { fn(); return 0; }` to make wait-loops synchronous. Requires `@ts-expect-error` since the global type expects a `Timeout` return.
---

## [2026-05-14 02:00] - US-003: Port tmux.ts + monitor.ts with processOneTick adapter for tests

### What was done
- Ported `tmux.js` → `src/retry/tmux.ts` using `Bun.spawn`/`Bun.spawnSync` instead of `node:child_process`. All arg-builder helpers typed with string params/return types.
- Ported `monitor.js` → `src/retry/monitor.ts` with key architectural change: `processOneTick(state, pane, deps)` accepts a `TickDeps` object bundling all I/O (now, capturePane, sendKeys, getPaneCommand, isClaudeForeground, sleep, logger, config, isAlive) so tests can drive a full cycle synchronously.
- `startMonitor` wires real Bun-backed adapters and runs the recursive-setTimeout loop, preserving original end-to-end behavior.
- Both files carry MIT attribution header.
- 20 tmux tests + 17 monitor tests; all isolated (no real tmux, no real sleep > 0ms, no real claude process).
- `noUncheckedIndexedAccess` required `as unknown as string` double-cast in tests when bun typed `expect(null).toBe(...)`.

### Files changed
- src/retry/tmux.ts (new)
- src/retry/monitor.ts (new)
- test/retry/tmux.test.ts (new)
- test/retry/monitor.test.ts (new)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 367 pass / 0 fail (37 new + 330 pre-existing)

### Step 5.5 docs validation
- lib: Bun.spawn, status: ok
- Confirmed: `Bun.spawn(cmd, { stdout: "pipe" })` returns Subprocess with `.stdout: ReadableStream` and `.exited: Promise<number>`. `new Response(proc.stdout).text()` is the idiomatic read. `Bun.spawnSync` returns `SyncSubprocess` with `.stdout: Buffer | undefined`. Implementation matches API.

### Notes
- `Bun.spawn` stdout is `ReadableStream | null | undefined` depending on stdout option. With `{ stdout: "pipe" }` (the default), it's always a `ReadableStream` — safe to pass to `new Response()`.
- `processOneTick` signature is `(state, pane, deps)` — the `deps` param is a single object so future stories can add fields without breaking call sites.
- The "isAlive after waitUntil" check returns `"exit"` before doing the rate-limit check — matches original monitor.js logic exactly.
---

## [2026-05-14 01:00] - US-002: Port config.ts + logger.ts with cam-namespaced paths

### What was done
- Ported `config.js` → `src/retry/config.ts` with TOML config loaded from `~/.config/cam/retry.toml` (injected `baseDir` default).
- Used `Bun.TOML.parse` (throws `BuildMessage extends Error` on invalid TOML); catches via `err instanceof Error`, extracts `.message`.
- On first read (file absent), writes a documented TOML defaults file to disk and returns `DEFAULT_CONFIG`.
- `validate()` clamps below-min numbers back to defaults, filters bad regex patterns in `customPatterns`, and omits empty `foregroundCommands`.
- Ported `logger.js` → `src/retry/logger.ts` with logs written under `~/.cam/retry-logs/` (injected `dir` default).
- Logger uses `node:fs/promises.appendFile` for atomic append (same as original); `Bun.file`/`Bun.write` avoided for append to keep atomicity.
- Directory created lazily on first `log()` call via `node:fs/promises.mkdir({ recursive: true })`.
- Rotation cleanup is fire-and-forget (void), matching original behavior.
- Both files carry MIT attribution header.
- 14 config tests + 10 logger tests written, all isolated via injected `baseDir`/`dir` — no real `~/.config/cam` or `~/.cam` touched.

### Files changed
- src/retry/config.ts (new)
- src/retry/logger.ts (new)
- test/retry/config.test.ts (new)
- test/retry/logger.test.ts (new)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 334 pass / 0 fail (24 new + 310 pre-existing)

### Step 5.5 docs validation
- lib: Bun.TOML, status: ok
- Confirmed: `Bun.TOML.parse(str)` returns `object`, throws `BuildMessage` (extends `Error`) on invalid TOML; caught via `err instanceof Error`. API matches implementation.

### Notes
- `Bun.TOML.parse` throws `BuildMessage` (not a plain `Error` or `SyntaxError`) — but since `BuildMessage` extends `Error`, `err instanceof Error` correctly catches it and `.message` is accessible.
- For logger append, `node:fs/promises.appendFile` is preferred over `Bun.file` read+write to preserve append atomicity (two concurrent loggers would corrupt data with read+write).
- `noUncheckedIndexedAccess` requires indexing `raw["field"]` via bracket notation on `Record<string, unknown>`, not dot-access through typed props.
---

## [2026-05-14 00:00] - US-001: Port patterns.ts + time-parser.ts with tests and license attribution

### What was done
- Ported `patterns.js` → `src/retry/patterns.ts` with full TypeScript types on all exports (`stripAnsi`, `isRateLimited`, `findRateLimitMessage`).
- Ported `time-parser.js` → `src/retry/time-parser.ts` with typed interfaces (`AbsoluteParsedTime`, `RelativeParsedTime`, `ParsedTime`) and identical semantics.
- Both files carry MIT attribution header per acceptance criteria.
- Created `LICENSES/claude-auto-retry-MIT.txt` with verbatim MIT license text from source package.
- Wrote 50 tests across `test/retry/patterns.test.ts` and `test/retry/time-parser.test.ts` covering every exported function with positive, negative, and edge cases.
- No changes to `package.json` — zero new runtime dependencies.

### Files changed
- LICENSES/claude-auto-retry-MIT.txt (new)
- src/retry/patterns.ts (new)
- src/retry/time-parser.ts (new)
- test/retry/patterns.test.ts (new)
- test/retry/time-parser.test.ts (new)

### Quality gates
- Typecheck: ok
- Lint: ok (no lint command configured)
- Tests: 310 pass (50 new + 260 pre-existing)

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched

### Notes
- `noUncheckedIndexedAccess: true` in tsconfig requires guarding regex capture group access and array index reads with `?? fallback`. Fixed 5 type errors after initial port.
- The `findRateLimitMessage` function in the original JS accepts `customPatterns` but ignores it — parameter preserved for API parity.
---
