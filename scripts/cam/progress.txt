## Codebase Patterns

## [2026-06-13] - US-001: deletar byte-scan morto (isApproveLine/findApproveLine)
- Removidas as 2 funcoes + o comment de secao de src/commands/plan.ts (path PTY removido; runPlan nao as chamava). Removidos os 2 describe blocks + imports + coverage-comments de test/plan.test.ts.
- grep isApproveLine/findApproveLine: ZERO hits no repo. runPlan/buildPlanArgv/parsePlanArgs intactos.
- Gates: typecheck ok, plan.test 19 pass, suite cheia ok.
---

## [2026-06-13] - US-002: reconciliar FLOW.md ao modelo supervisor
- FLOW.md secao 4 (flowchart+prose de cam next): reescrita pro runNext real (le worker pane -> arma state -> lock -> runSupervisor in-process; sem stop hook, sem settings.local.json, sem pane /cam-next). Secao 7 (prose+1 edge): supervisor dirige o loop via decideNextAction(prd)+review verdict, nao <promise>/stop-hook; +state-primary (CAM-32) + stream-json descartado por -p banido (CAM-42). Secao 8 (state machine edges+prose): "stop hook re-injeta" -> "supervisor despacha proximo worker (in-process)".
- Preservado: flags --completion-promise/--max-iter (ainda parseadas; promise e so display). README ja estava correto (256/272/280), intocado. Sem em-dash.
- Gates: typecheck ok, 759 pass.
---

## [2026-06-13] - Review round 1 FIXES_PENDING:2 -> fixes -> round 2 CLEAN
- Round 1 (2 WARNINGs, ambos em regiao reescrita): heading da secao 4 ("lancador de pane") contradizia o body; state machine da secao 8 roteava max_iterations pra Complete (na real -> Paused, so 'complete' remove o state file). Corrigidos + reconciliado o mermaid da secao 7 (NEXTC/DEC/COMPLETE) que a prose nova ja contradizia.
- Out-of-scope (secao 1 mermaid, secao 2.5 + README orchestrator pane dizendo /cam-next) filados como CAM-45 (P3).
- Round 2: APPROVE/CLEAN. 2 sugestoes cosmeticas (underscore vs hyphen no label; absorvidas no CAM-45).
---
