## Codebase Patterns

Durable patterns carried across cycles. Implementers append new ones as they emerge.

- **Bun runtime**: prefer `Bun.spawn` / `Bun.$` / `Bun.file` over `node:child_process` / `node:fs` in NEW code. But match the surrounding file's idiom: `src/commands/run.ts`, `dashboard.ts`, and `status.ts` already use `node:fs` / `node:path` / `node:child_process`; keep that style there, do not mix `Bun.file` mid-file.
- **Permission mode**: never register a `--permission-mode` CLI flag on any subcommand parser, enforced by `test/no-permission-mode-flag.test.ts` (greps `index.ts` and `src/commands/`). The literal may appear only inside a forwarded argv (e.g. the orchestrator `claude --permission-mode bypassPermissions` in `run.ts`, intentional).
- **noUncheckedIndexedAccess**: tsconfig has it on. Array indexing and regex capture groups are `T | undefined`; guard with `?? fallback` or a justified non-null assertion. JSON.parse results need runtime shape checks before property access.
- **`bun test` green is NOT proof of type-safety**: the runner passes even when `tsc --noEmit` would fail. Always run `bun run typecheck` separately and trust its exit code before claiming the gate passed.
- **Read-only readers never throw**: `readSnapshot` / `buildStatusReport` and their helpers wrap every IO in try/catch and fall through to defaults, because dashboard/status are read-only and a render failure must not brick the operator's terminal (see `readPrd` / `readState` in `dashboard.ts`).
- **Transcript discovery**: Claude Code transcripts live at `~/.claude/projects/<encode(cwd)>/<sessionId>.jsonl`, where `encode = cwd.replace(/[^a-zA-Z0-9]/g, '-')` (verified empirically). Filename == sessionId. Each line is one JSON object; assistant lines carry `message.usage` with `input_tokens` / `output_tokens` / `cache_read_input_tokens` / `cache_creation_input_tokens`.
- **src/tmux/session.ts is the canonical session module**: `projectSessionName`, `ensureProjectSession`, `openPaneInSession`, `isInsideProjectSession`, `tmuxArgs` all live here with injectable `SpawnFn`. All tmux calls go through `tmuxArgs(sub)` which prepends `['-L', 'cam']`; test fakes route by `args[2]` (guard: `args[0] === '-L' ? args[2] : args[0]`).
- **Ink screens**: success/failure is signalled by the glyph (✓ accent / ✗ destructive), never by divider color. Render and look at the real output; do not trust header comments (see `lessons.md` 2026-06-05).
- **Tests**: `bun test` from repo root; test files live under `test/`, mirroring source. Inject fake reader/writer/spawn shapes (and injectable `home` / `genSessionId`) instead of touching real stdin/stdout/`~/.claude`.
- **Commits**: conventional commits required (`feat:`, `fix:`, `chore:`, `refactor:`, `docs:`).
- **No em dashes** in persisted files (PRD notes, progress, docs): use colon, comma, parentheses, or period.
- **Pure transcript parser pattern**: `parseTranscriptUsage` splits on `\n`, `JSON.parse` each trimmed line in a try/catch, checks `message?.usage` existence, accumulates with `toNumber()` guard (returns 0 for non-number). `formatTokens` uses `>= 1_000_000` / `>= 1_000` thresholds with `toFixed(1)M` / `Math.round/k` / raw. Module lives in `src/transcript/usage.ts`; tests under `test/transcript/usage.test.ts`.

## [2026-06-08 01:00] - US-002: Capture orchestrator session id in cam run + transcript path resolver

### What was done
- Added `genSessionId?: () => string` to `RunOptions` (default `randomUUID` from `node:crypto`).
- `setupOrchestratorSession` now generates a uuid via `genSessionId`, writes it to `.claude/.cam-orch-session` (utf8), and inserts `--session-id <uuid>` between `bypassPermissions` and the prompt in the orchestrator `agentCmd`.
- Re-attach path (`created: false`) returns before the marker write so the original session id persists across `cam run` invocations.
- `src/transcript/usage.ts` now exports `orchestratorTranscriptPath(cwd, claudeDir)`: reads `.claude/.cam-orch-session`, encodes cwd via `cwd.replace(/[^a-zA-Z0-9]/g, '-')`, returns `<claudeDir>/projects/<encoded>/<uuid>.jsonl` or null if the marker is absent/empty.
- Extended `test/run.test.ts`: three new tests assert `--session-id` presence in argv, marker file content, and no-rewrite on re-attach.
- Extended `test/transcript/usage.test.ts`: six new tests cover absent marker, empty marker, whitespace-only, encoding correctness, claudeDir injection (not hardcoded), and trailing-newline trim.

### Files changed
- src/commands/run.ts (modified, +18 lines)
- src/transcript/usage.ts (modified, +43 lines)
- test/run.test.ts (modified, +55 lines)
- test/transcript/usage.test.ts (modified, +86 lines)

### Quality gates
- Typecheck: ok
- Tests: 513 pass / 0 fail

### Step 5.5 docs validation
- lib: claude-cli --session-id flag, status: ok (confirmed via `claude --help` locally: "Use a specific session ID for the conversation")

### Notes
- orchestratorTranscriptPath takes `claudeDir` as a parameter (not hardcoded); callers must resolve `process.env.CLAUDE_CONFIG_DIR ?? join(os.homedir(), '.claude')`.
- `noUncheckedIndexedAccess`: readFileSync result is trimmed before returning; empty-after-trim returns null.
---

## [2026-06-08 00:00] - US-001: Pure transcript-usage parser + formatTokens

### What was done
- Created `src/transcript/usage.ts` with `parseTranscriptUsage(jsonl)` and `formatTokens(n)`.
- Created `test/transcript/usage.test.ts` with 13 tests covering multi-line fixtures, malformed lines, sidechain lines, missing sub-fields, all three formatTokens magnitudes, 0, and edge values.

### Files changed
- src/transcript/usage.ts (new, 82 lines)
- test/transcript/usage.test.ts (new, 152 lines)

### Quality gates
- Typecheck: ok
- Tests: 503 pass / 0 fail

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched

### Notes
- noUncheckedIndexedAccess: used runtime shape checks (isTranscriptLine guard) and toNumber() helper to safely extract optional number fields from parsed JSON.
---

## [2026-06-08 02:00] - US-003: Surface orchestrator tokens in the dashboard

### What was done
- `DashboardData` gains four optional token fields: `tokensInput?`, `tokensOutput?`, `tokensCacheRead?`, `tokensCacheCreation?`.
- `readSnapshot` now accepts `claudeDir?` (defaults to `CLAUDE_CONFIG_DIR ?? ~/.claude`), calls `readTranscriptTokens` (new private helper), and populates token fields when the marker + JSONL are found. Never throws.
- `readTranscriptTokens` wraps `orchestratorTranscriptPath` + `existsSync` + `readFileSync` + `parseTranscriptUsage` in a single try/catch; returns null on any miss.
- `snapshotKey` now includes `tokensInput` and `tokensOutput` so the non-Ink render loop redraws when tokens change.
- `RunDashboardOptions` and `RunDashboardInkOptions` both gain `claudeDir?` and plumb it through to `readSnapshot`.
- `Dashboard.tsx` imports `formatTokens` and renders a `tokens` SummaryRow (label "tokens") after `since` and before `branch`; the row is omitted when `tokensInput` is undefined.
- Two new tests in `test/dashboard.test.ts`: (a) fixture with marker file + transcript JSONL, asserts all four token fields; (b) missing-marker path, asserts fields are undefined and no throw.

### Files changed
- src/commands/dashboard.ts (modified, +70 lines)
- src/ui/Dashboard.tsx (modified, +14 lines)
- test/dashboard.test.ts (modified, +65 lines)

### Quality gates
- Typecheck: ok
- Tests: 515 pass / 0 fail

### Step 5.5 docs validation
- lib: none (node:fs built-ins, own transcript/usage module), status: no_external_lib_touched

### Notes
- The tokens row uses total input volume: `tokensInput + tokensCacheCreation + tokensCacheRead` (per AC), not just the non-cached portion. US-004 adds the cached breakdown.
- claudeDir injection pattern: `options.claudeDir ?? (process.env.CLAUDE_CONFIG_DIR ?? join(homedir(), '.claude'))` resolves in readSnapshot, consistent with how US-002 documented the contract.
---

## [2026-06-08 03:00] - US-004: Cached-token honesty in dashboard + cam status parity

### What was done
- Added `renderTokensLine(t: TranscriptUsage): string` to `src/transcript/usage.ts`. Single source of truth for the shared format: `<in> in (<cached> cached) · <out> out` where in = input + cacheCreation + cacheRead and cached = cacheRead only (cacheCreation is fresh billed spend). The "(N cached)" suffix is omitted when cached is 0.
- Updated `Dashboard.tsx` (Ink component) to import and call `renderTokensLine` instead of the previous inline expression. Removed stale `formatTokens` import from that file.
- Updated `composeDashboard` (non-Ink alt-screen renderer in `dashboard.ts`) to render a `tokens` row using `renderTokensLine` when `tokensInput` is defined; row is absent when undefined.
- Extended `StatusReport` with an optional `tokens?: { input; output; cacheRead; cacheCreation }` field.
- Extended `StatusOptions` with `claudeDir?: string` (defaults to `CLAUDE_CONFIG_DIR ?? ~/.claude`).
- Updated `buildStatusReport` to resolve the orchestrator transcript via `orchestratorTranscriptPath` using the injectable `claudeDir`, parse it with `parseTranscriptUsage`, and populate `report.tokens`. Never throws: wrapped in try/catch, leaves `tokens` undefined on any error.
- Updated `runStatus` to render a `tokens` entry via `renderEntry` after `last` and before `promise` in the active/paused branch, using `renderTokensLine`. Row skipped when `report.tokens` is undefined.
- Added 5 `renderTokensLine` tests in `test/transcript/usage.test.ts` covering: cached suffix present when cacheRead > 0, omitted when 0, cacheCreation folded into in total only, all-zero, and tiny cacheRead.
- Added 3 `composeDashboard` tests in `test/dashboard.test.ts` for: cached suffix present, suffix absent when zero, row absent when tokensInput undefined.
- Added 2 `buildStatusReport` tests in `test/status.test.ts` for: tokens populated from fixture transcript, and undefined when marker absent.

### Files changed
- src/transcript/usage.ts (modified, +26 lines)
- src/ui/Dashboard.tsx (modified, import swap + 6 lines changed)
- src/commands/dashboard.ts (modified, +12 lines)
- src/commands/status.ts (modified, +38 lines)
- test/transcript/usage.test.ts (modified, +56 lines)
- test/dashboard.test.ts (modified, +36 lines)
- test/status.test.ts (modified, +62 lines)

### Quality gates
- Typecheck: ok
- Tests: 525 pass / 0 fail

### Step 5.5 docs validation
- lib: none (all changes touch own source modules and node built-ins only), status: no_external_lib_touched

### Notes
- The key invariant: cacheCreation is fresh spend (billed above base rate), so it MUST stay in `in` total but MUST NOT appear in `cached`. Only `cacheRead` contributes to the `(N cached)` suffix. This lives in one place (`renderTokensLine`) so it cannot drift.
- Sample numbers from the issue: cacheRead 450k, fresh input 22k. With this implementation `482k in (450k cached) · Xk out` would render correctly.
---
