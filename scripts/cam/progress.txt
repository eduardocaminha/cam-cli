## Codebase Patterns

## [2026-06-13] - US-001 + US-002: build smoke hermetico + guard estatico
- build-release.sh AC4: smoke roda em SMOKE_DIR=$(mktemp -d) como cwd, com BIN_ABS absoluto, `init --no-tmux --existing --issue-system none </dev/null`, rm -rf no fim. As flags zeram o wizard (catch do auditor F-01: --no-tmux sozinho ainda bloquearia no collectSetupAnswers). Mutacoes caem no tmpdir, nunca no repo.
- test/build-release-smoke.test.ts (novo): guard estatico (estilo no-permission-mode-flag.test.ts). Assere mktemp -d, o flag set completo + </dev/null, e que TODA invocacao `init` do binario e hermetica (cd SMOKE_DIR + --no-tmux + ${BIN_ABS}, nunca ${BIN} relativo), + cleanup + semantica non-zero-aceitavel.
- Validacao runtime ja feita pelo orquestrador (efetivamente a US-003): rodei ./scripts/build-release.sh no repo real, git status antes==depois (so a edicao do proprio build-release.sh), 10 arquivos clobberaveis intactos, zero sessao cam-setup, zero menu orfao.
- Gates: typecheck ok, 768 pass.
---

## [2026-06-13] - Review round 1: CLEAN (+ 3 sugestoes aplicadas)
- subagent-reviewer APPROVE, zero CRITICAL/WARNING, validou hermeticidade (flags zeram o wizard, set -e preservado, regex do guard pega regressao). Sugestoes aplicadas: trap rm -rf no EXIT (nao vaza tmpdir em abort); comentario no regex do teste mostrando a linha-alvo; entrada lessons.md 2026-06-13 (pointer fiel) + comentario do script atualizado.
- Re-rodei o build real apos o trap: working tree intacto, 0 tmpdirs vazados.
- Gates: typecheck ok, 768 pass.
---

## [2026-06-13] - US-003: validacao real do build hermetico (operator, executada pelo orquestrador)
- Rodei ./scripts/build-release.sh (sem --install) no repo real, claude logado, 2x (antes e depois do trap). git status antes == depois nas duas: so a edicao do proprio build-release.sh (a implementacao), nenhum side effect.
- Os 10 arquivos que o bug clobberava (.claude/agents/*.md, .claude/commands/*.md, scripts/cam/project.toml, journal.md, CLAUDE.md, .cam-orchestrator-prompt.txt) intactos (git diff --name-only vazio). Zero sessao cam-setup, zero .cam-setup-menu.sh orfao, zero tmpdir vazado.
- A saida do smoke confirma o caminho hermetico: "Templates installed - skipping tmux (--no-tmux)" + "init: ok (hermetic tmpdir, no tmux)".
---
