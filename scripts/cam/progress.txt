## Codebase Patterns

Durable patterns carried across cycles. Implementers append new ones as they emerge.

- **Bun runtime**: prefer `Bun.spawn` / `Bun.$` / `Bun.file` over `node:child_process` / `node:fs` in NEW code. But match the surrounding file's idiom: `src/commands/run.ts`, `dashboard.ts`, and `status.ts` already use `node:fs` / `node:path` / `node:child_process`; keep that style there, do not mix `Bun.file` mid-file.
- **Permission mode**: never register a `--permission-mode` CLI flag on any subcommand parser, enforced by `test/no-permission-mode-flag.test.ts` (greps `index.ts` and `src/commands/`). The literal may appear only inside a forwarded argv (e.g. the orchestrator `claude --permission-mode bypassPermissions` in `run.ts`, intentional, and the spawned worker argv in CAM-22).
- **noUncheckedIndexedAccess**: tsconfig has it on. Array indexing and regex capture groups are `T | undefined`; guard with `?? fallback` or a justified non-null assertion. JSON.parse results need runtime shape checks before property access.
- **`bun test` green is NOT proof of type-safety**: the runner passes even when `tsc --noEmit` would fail. Always run `bun run typecheck` separately and trust its exit code before claiming the gate passed.
- **Read-only readers never throw**: `readSnapshot` / `buildStatusReport` and their helpers wrap every IO in try/catch and fall through to defaults, because dashboard/status are read-only and a render failure must not brick the operator's terminal (see `readPrd` / `readState` in `dashboard.ts`).
- **Transcript discovery**: Claude Code transcripts live at `~/.claude/projects/<encode(cwd)>/<sessionId>.jsonl`, where `encode = cwd.replace(/[^a-zA-Z0-9]/g, '-')` (verified empirically). Filename == sessionId. Each line is one JSON object; assistant lines carry `message.usage` with `input_tokens` / `output_tokens` / `cache_read_input_tokens` / `cache_creation_input_tokens`. Dedupe by `(message.id, requestId)` before summing (one JSONL line per content block carries identical usage; inflation ~1.44x).
- **src/tmux/session.ts is the canonical session module**: `projectSessionName`, `ensureProjectSession`, `openPaneInSession`, `isInsideProjectSession`, `tmuxArgs` all live here with injectable `SpawnFn`. All tmux calls go through `tmuxArgs(sub)` which prepends `['-L', 'cam']`; test fakes route by `args[2]` (guard: `args[0] === '-L' ? args[2] : args[0]`).
- **tmux worker primitives (CAM-22)**: `respawn-pane -k -t <pane> <cmd>` kills the running command and reuses the SAME pane id; `wait-for <chan>` blocks the client until `wait-for -S <chan>` signals it (the signal is NOT guaranteed buffered if sent before the wait, so always confirm the outcome by reading `prd.json` / `handoff.json`, never trust the channel alone); `capture-pane -p [-S -N]` dumps a pane to stdout for sentinel grepping.
- **Claude worker invocation (CAM-22)**: custom slash commands do NOT run in headless `-p` mode. A worker session is `claude -p --permission-mode <mode> --session-id <uuid> --output-format json --agent <name> "<task>"`, never `claude -p "/cam-..."`. Autonomous workers (implementer/reviewer) exit on their own so a chained `; tmux -L cam wait-for -S <chan>` works; interactive workers (plan, optional review) do NOT exit, so detect completion by scraping `capture-pane -p` for the `CAM_*_STATUS` / `<review>` sentinel.
- **Ink screens**: success/failure is signalled by the glyph (✓ accent / ✗ destructive), never by divider color. Render and look at the real output; do not trust header comments (see `lessons.md` 2026-06-05).
- **Tests**: `bun test` from repo root. Tests for `src/commands/*` and `src/tmux/*` live FLAT at `test/<name>.test.ts` (e.g. `test/next.test.ts`, `test/tmux-session.test.ts`); subdir mirrors exist for `test/retry/*`, `test/transcript/*`, and (new in CAM-22) `test/supervisor/*`. Inject fake reader/writer/spawn shapes (and injectable `home` / `genSessionId`) instead of touching real stdin/stdout/`~/.claude`/tmux.
- **Commits**: conventional commits required (`feat:`, `fix:`, `chore:`, `refactor:`, `docs:`).
- **No em dashes** in persisted files (PRD notes, progress, docs): use colon, comma, parentheses, or period.
- **Pure transcript parser pattern**: `parseTranscriptUsage` splits on `\n`, `JSON.parse` each trimmed line in a try/catch, checks `message?.usage` existence, accumulates with `toNumber()` guard (returns 0 for non-number). `formatTokens` uses `>= 1_000_000` / `>= 1_000` thresholds. Module lives in `src/transcript/usage.ts`; tests under `test/transcript/usage.test.ts`. `orchestratorTranscriptPath(cwd, claudeDir)` reads `.claude/.cam-orch-session`; CAM-22 generalizes it to `transcriptPathForSession(uuid, cwd, claudeDir)`.
- **openPaneInSession returns pane id (US-001)**: the function now uses `stdio: 'pipe'` and `-P -F #{pane_id}` to capture and return the stable pane id string (e.g. `%5`). All test fakes for split-window must handle `opts?.stdio === 'pipe'` and return a non-empty `stdout` buffer, or they will crash with `Cannot read property toString of undefined`.
- **Worker pane marker (US-001)**: the first write of `.claude/.cam-worker-pane` happens in `plan.ts` immediately after `openPaneInSession`. Supervisor and lifecycle commands address the slot via `readWorkerPaneMarker(claudeDir)`. Returns null if the file is missing or empty; never throws.
- **Supervisor timeout pattern (US-011)**: `tmux wait-for` has NO native timeout. Bound it by running via `spawnSync(..., { timeout: ms })`; when the deadline fires, spawnSync kills the client process (SIGTERM) and sets `result.signal`. Check `result.signal !== null` for `timedOut`. Pane-death detection: `spawnSync('tmux', ['-L', 'cam', 'list-panes', '-t', paneId])` exits 0 if pane exists, non-zero if dead. Timeout/pane-died blocked outcomes continue the loop (proceed to decideNextAction) rather than exiting, enabling recovery to the next story. Decouple reviewer wait (`ReviewWaitForFn = (channel) => void`) from supervisor `WaitForFn` to avoid forcing the timeout signature onto the unbounded review wait.
- **Sentinel polling mode (US-012)**: interactive workers (plan/review with human input) do NOT exit on their own, so the supervisor polls `capture-pane` instead of waiting for a tmux channel. Use `implementerMode: 'sentinel'` in `RunSupervisorOptions`; inject `sleepFn` (no-op for tests), `nowMs` (deterministic time source), and `pollIntervalMs`. `buildImplementerWorkerArgv({ interactive: true })` omits `-p` and `--output-format` and the wait-for chain. `parseAnySentinel(text)` in `result.ts` detects `CAM_IMPLEMENTER_STATUS=*`, `CAM_REVIEWER_STATUS=*`, or `<review>...</review>` tags. Same timeout/pane-died semantics as the exit path: both `continue` the supervisor loop.

## 2026-06-08 21:00 - US-008: Review worker dispatch (autonomous headless reviewer)

### What was done
- Created `src/supervisor/review.ts` with three exports:
  - `buildReviewerWorkerArgv({ uuid, channel, agentName? })`: pure argv builder mirroring
    `buildImplementerWorkerArgv` but without `--permission-mode` (reviewer is read-only).
  - `parseReviewVerdict(capturedPaneText)`: pure pane-text parser; extracts the last
    `<review>CLEAN</review>` or `<review>FIXES_PENDING:N</review>` tag; returns null
    if no tag found; returns `{ verdict, findingsCount, newStories: [] }`.
  - `makeReviewDispatch(opts)`: factory returning a `ReviewDispatch` closure that spawns
    the reviewer, waits for channel, captures pane, parses verdict, and updates prd.json:
    CLEAN sets lastVerdict='CLEAN'; FIXES_PENDING prepends US-R{round}-NNN stories with
    passes=false; newRound > maxRounds sets lastVerdict='MAX_ROUNDS_DEBT' (terminal).
- Updated `src/supervisor/loop.ts`:
  - `ReviewDispatch` type now takes `(uuid: string, channel: string)` (channel passed
    from loop, consistent with implement branch pattern).
  - Review branch now calls `genChannel('review', uuid)` and passes channel to dispatch.
  - Removed "placeholder" comment (wiring is now real).
- Updated `src/supervisor/decide.ts`:
  - Added `title?: string`, `acceptanceCriteria?: string[]`, `notes?: string` to
    `PrdSnapshot.userStories` element type to support fix-story creation.
- Updated `src/commands/next.ts`:
  - Replaced placeholder `reviewDispatch` with real `makeReviewDispatch(...)` wiring.
- Updated `test/supervisor/loop.test.ts`:
  - Fixed three `reviewDispatch` fakes to match the new two-argument signature.
- Created `test/supervisor/review.test.ts`: 29 tests covering all verdict paths, story
  creation, ID format, max-rounds debt, idempotency, and spawn/wait assertions.

### Files changed
- `src/supervisor/review.ts` (new)
- `test/supervisor/review.test.ts` (new)
- `src/supervisor/loop.ts` (ReviewDispatch type + review branch)
- `src/supervisor/decide.ts` (PrdSnapshot type extension)
- `src/commands/next.ts` (real reviewDispatch wiring)
- `test/supervisor/loop.test.ts` (fake signature fix)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 634 pass

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched
- summary: US-008 is pure TypeScript over existing tmux primitives. No new npm packages.
  tmux wait-for channel pattern validated in US-003.

### Notes
- Reviewer does NOT get --permission-mode; it is read-only and only needs claude -p with
  --output-format text + the agent flag.
- MAX_ROUNDS_DEBT is set by makeReviewDispatch when newRound > maxRounds, never by the
  raw parser (parseReviewVerdict only knows about CLEAN and FIXES_PENDING).
- fix stories get priorities 1..N; existing stories are bumped by N to make room.
---

## 2026-06-08 20:00 - US-007: Rewire cam next to supervisor + retire stop-hook driver

### What was done
- Rewrote `src/commands/next.ts` to dispatch to `runSupervisor()` instead of spawning
  claude with the stop-hook re-inject pattern.
- Removed: `materializeStopHook`, `writeSettingsLocal`, `buildHooksBlock`, `deepMerge`,
  `buildClaudeArgv`, `buildDashboardArgv`, `CAM_NEXT_PROMPT`, `STOP_HOOK_RELATIVE`,
  `SETTINGS_LOCAL_RELATIVE`, `hookMaterializer`/`settingsWriter`/`hostMode`/`spawn`
  injection points. Stop-hook driver RETIRED, not coexisting (closes CAM-29).
- `runNext()` now: reads worker pane marker (from cam plan), writes
  `.claude/cam-loop.local.md` with YAML frontmatter (active, iteration, started_at,
  pid, session_id, max_iterations) and empty body (no re-inject), then calls
  `runSupervisor()` with real I/O adapters.
- Rewrote `test/next.test.ts`: supervisor dispatch happy path, state-file shape
  contract (YAML fields + empty body), no stop-hook artifacts, missing pane guard,
  writer failure, blocked/max-iter/complete exit codes, custom maxIterations.
- Updated `test/attach-hint.test.ts`: replaced tmux-split-specific runNext tests
  with supervisor-based ones using `supervisorFn`/`workerPaneReader` injection.
- Updated `scripts/preview-screens.ts`: replaced removed injection points with
  `supervisorFn`/`workerPaneReader`.

### Files changed
- `src/commands/next.ts` (rewritten)
- `test/next.test.ts` (rewritten)
- `test/attach-hint.test.ts` (updated)
- `scripts/preview-screens.ts` (updated)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 605 pass

### Step 5.5 docs validation
- lib: js-yaml, version: 4.x, status: ok, url: https://github.com/nodeca/js-yaml
- summary: yaml.load() API confirmed: returns plain object or throws YAMLException.
  Usage in test (parsing YAML frontmatter section) matches current API.

### Notes
- vendor/cam-loop-stop-hook.sh left in place for older installs but is no longer
  written to .claude/hooks/ or registered in settings.local.json by this codepath.
- review dispatch is a placeholder (returns error) pending US-008.
- .claude/commands/cam-next.md documentation of retirement deferred to US-010
  per AC6 ("touched in US-010").
- `cam next` now reads worker pane from .claude/.cam-worker-pane (written by cam plan).
  If missing, returns 1 with hint to run cam plan first.
---

## 2026-06-08 19:00 - US-006: runSupervisor() loop tying primitives together

### What was done
- Created `src/supervisor/loop.ts` with `runSupervisor(opts): Promise<SupervisorResult>`.
- All side effects injected: spawn, waitFor, capturePane, readPrd, writePrd, readHandoff, clock, genUuid, genChannel, reviewDispatch, writeSessionMarker.
- Per iteration: calls decideNextAction -> implement branch mints uuid + channel, respawns worker pane, waits on channel, captures pane, calls readWorkerOutcome, then calls writeSessionMarker(actualStoryId, uuid) keyed to actual completed story (not advisory).
- Review branch is a placeholder: calls injected reviewDispatch(uuid), exits blocked on error, continues on ok.
- Hard max-iterations cap defaults to 50 (MAX_ITERATIONS constant).
- Returns `{ status: 'complete' | 'blocked' | 'max-iterations', iterations, lastOutcome }`.
- Worker outcome 'fail' or 'unknown' exits as 'blocked' (conservative: do not retry silently).
- Created `test/supervisor/loop.test.ts` with 14 tests covering: complete-immediately, blocked-immediately, PRD-unreadable, happy-path-2-stories, one-blocked, max-iterations, PRD_COMPLETE-cycle, fail-outcome, unknown-outcome, review-dispatch-error, writeSessionMarker-with-actual-id, spawn-called-per-iteration.

### Files changed
- `src/supervisor/loop.ts` (new)
- `test/supervisor/loop.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 621 pass

### Step 5.5 docs validation
- lib: none (pure TS, no external libs), status: no_external_lib_touched

### Notes
- `fileReader` for `readWorkerOutcome` calls `readPrd()` once (after worker exits) to get updated prd state. Tests must account for 2 readPrd() calls per implement iteration (top-of-loop + fileReader), except PRD_COMPLETE path which skips the fileReader.
- Advisory storyId from decideNextAction is used only for channel naming and logging; writeSessionMarker is always called with outcome.storyId (actual story from handoff/sentinel).
---

## 2026-06-08 18:00 - US-005: Port Branch-decision matrix to pure TS

### What was done
- Created `src/supervisor/decide.ts` with `decideNextAction(prd): SupervisorAction`.
- Returns one of: `{ kind: 'implement', storyId }`, `{ kind: 'review' }`, `{ kind: 'complete' }`, `{ kind: 'blocked-no-implementable' }`.
- Mirrors cam-next.md matrix: implementable story with passes=false picked first (priority asc, id asc tie-break), then blocked-no-implementable if only operator stories remain, then review/complete based on review state.
- Cap row: `roundsCompleted >= maxRounds` (default 3) returns complete regardless of lastVerdict.
- Terminal verdicts: CLEAN and MAX_ROUNDS_DEBT both return complete.
- Created `test/supervisor/decide.test.ts` with 23 tests covering all 4 outcome kinds, tie-breaking, operator-only, cap row, and implement-over-review priority.

### Files changed
- `src/supervisor/decide.ts` (new)
- `test/supervisor/decide.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 607 pass

### Step 5.5 docs validation
- lib: none (pure TS, no external libs), status: no_external_lib_touched

### Notes
- `blocked-no-implementable` fires when operator-incomplete stories exist even if non-operator stories all pass; review/complete only fires when ALL stories (including operator ones) have passes=true. This is the semantically correct interpretation: operator-pending stories block autonomous loop progression.
---

## 2026-06-08 17:00 - US-004: Deterministic worker result reader

### What was done
- Created `src/supervisor/result.ts` with `readWorkerOutcome({ prdPath, handoffPath, capturedPaneText, readFile })`.
- Returns `{ kind: 'pass' | 'fail' | 'blocked' | 'unknown', storyId: string | undefined, detail: string }`.
- Pure function: all file I/O injected via `FileReader = (path: string) => string | null` callback.
- Sentinel parsed from pane text via regex (`CAM_IMPLEMENTER_STATUS=(\S+)(?:\s+story=(US-\S+))?`); guarded for `noUncheckedIndexedAccess`.
- `pass` requires all three signals to agree: sentinel is DONE, `handoff.json lastCompletedStory.id` matches sentinel `story=US-XXX`, and prd.json shows `passes:true` for that story.
- `blocked` detected by any `BLOCKED_*` sentinel prefix OR a raw `BLOCKED_\w+` token in pane text without a full sentinel.
- `PRD_COMPLETE` sentinel produces `kind: 'pass', storyId: undefined` (all stories already passing).
- `unknown` when no sentinel found and no blocked token present.
- Created `test/supervisor/result.test.ts` with 14 tests covering all four outcome kinds with fake prd/handoff fixtures.

### Files changed
- `src/supervisor/result.ts` (new)
- `test/supervisor/result.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 584 pass

### Step 5.5 docs validation
- lib: none (no external libs; pure JSON/regex only), status: no_external_lib_touched

### Notes
- Guard pattern for `noUncheckedIndexedAccess` on regex: `match[1] ?? ''` and `match[2]` (already `string | undefined`, no assertion needed).
- Three-way cross-check (sentinel + handoff + prd) is the architectural invariant; any single source alone is insufficient.
---

## 2026-06-08 16:00 - US-003: Headless implementer worker argv builder

### What was done
- Created `src/supervisor/worker-argv.ts` with `buildImplementerWorkerArgv(opts)`: pure function, no I/O, returns the shell string for respawn-pane.
- Shape: `claude -p --permission-mode <mode> --session-id <uuid> --output-format text --agent <agentName> '<task>' ; tmux -L cam wait-for -S '<channel>'`.
- `shellEscape(s)` wraps the value in single quotes and replaces embedded `'` with `'\''`; applied to both taskPrompt and channel.
- Default `agentName` is `'subagent-implementer'` (exported as `DEFAULT_IMPLEMENTER_AGENT`).
- Created `test/supervisor/worker-argv.test.ts` with 11 tests: all required flags, wait-for chain ordering, prompt with `'` + `$` + backtick escaping, channel in wait-for argument, agentName override, and DEFAULT_IMPLEMENTER_AGENT value assertion.

### Files changed
- `src/supervisor/worker-argv.ts` (new)
- `test/supervisor/worker-argv.test.ts` (new)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 570 pass

### Step 5.5 docs validation
- lib: none (no external libs; pure string manipulation only), status: no_external_lib_touched

### Notes
- Shell escaping uses POSIX single-quote strategy (`'...'` + `'\''` for embedded quotes); chosen over double-quote because single quotes neutralize `$`, backtick, `\`, and `!` without special-casing each.
---

## 2026-06-08 15:00 - US-002: Per-story session marker + transcriptPathForSession resolver

### What was done
- Added `transcriptPathForSession(uuid, cwd, claudeDir)` to `src/transcript/usage.ts`: generalized resolver that builds the `<claudeDir>/projects/<encode(cwd)>/<uuid>.jsonl` path from an explicit uuid.
- Refactored `orchestratorTranscriptPath(cwd, claudeDir)` into a thin wrapper over `transcriptPathForSession`; external API unchanged.
- Added `writeWorkerSessionMarker(claudeDir, storyId, uuid)` and `readWorkerSessionMarker(claudeDir, storyId)` for `.claude/.cam-worker-<storyId>.session` round-trip; returns null on absence/empty; creates dir if needed.
- Added `WORKER_SESSION_MARKER_PREFIX` constant (module-private) for consistent filename construction.
- Extended `test/transcript/usage.test.ts` with 10 new tests covering `transcriptPathForSession` (3 tests) and marker round-trip (7 tests); updated import list.

### Files changed
- `src/transcript/usage.ts`
- `test/transcript/usage.test.ts`

### Quality gates
- Typecheck: ok
- Lint: n/a (no linter configured)
- Tests: 559 pass

### Step 5.5 docs validation
- lib: none (only Node.js built-ins fs, path; no new external libs), status: no_external_lib_touched

### Notes
- `orchestratorTranscriptPath` now delegates to `transcriptPathForSession`; all existing tests still pass unchanged, confirming backward compatibility.
---

## 2026-06-08 14:00 - US-001: tmux worker-slot primitives in session.ts

### What was done
- Added `respawnPaneArgv`, `waitForArgv`, `signalWaitForArgv`, `capturePaneArgv` pure argv builders to `src/tmux/session.ts`, all prepending `-L cam` via `tmuxArgs`.
- Added `workerWaitChannel(storyId, uuid)` for deterministic channel naming.
- Added `writeWorkerPaneMarker(claudeDir, paneId)` and `readWorkerPaneMarker(claudeDir)` for `.claude/.cam-worker-pane` persistence; exported `WORKER_PANE_MARKER` constant.
- Changed `openPaneInSession` return type from `void` to `string`: adds `-P -F #{pane_id}` and `stdio: 'pipe'` to the split-window call, returns the captured pane id.
- Updated `plan.ts` to capture the returned pane id and call `writeWorkerPaneMarker` before returning (first write of the slot marker).
- Extended `test/tmux-session.test.ts` with argv-shape tests for all new builders, marker round-trip tests, and openPaneInSession return value test.
- Extended `test/plan.test.ts` with two tests verifying `.claude/.cam-worker-pane` is written with a non-empty pane id.
- Fixed `test/attach-hint.test.ts` fake spawn to handle `stdio: 'pipe'` for split-window (returning a `stdout` buffer), which broke after the openPaneInSession signature change.

### Files changed
- `src/tmux/session.ts`
- `src/commands/plan.ts`
- `test/tmux-session.test.ts`
- `test/plan.test.ts`
- `test/attach-hint.test.ts`

### Quality gates
- Typecheck: ok
- Lint: n/a (no linter configured)
- Tests: 548 pass

### Step 5.5 docs validation
- lib: none (no new external libraries; only Node.js built-ins `fs`, `path`, `crypto`), status: no_external_lib_touched

### Notes
- The `attach-hint.test.ts` fake was the only collateral breakage: it returned bare `{ status: 0 }` without `stdout`, which crashed when `openPaneInSession` called `.stdout.toString()`. Pattern: any fake spawn for split-window/new-session must include `stdout: Buffer.from(...)` to avoid crashes on the return value.
---

## 2026-06-08 22:00 - US-009: cam stop / cam resume target the supervisor

### What was done
- Updated `src/commands/stop.ts`:
  - Added `KillFn` type and `killFn` + `workerPaneReader` injectable deps to `StopOptions`.
  - Added `supervisorKilled` and `workerPaneKilled` fields to `StopReport`.
  - `performStop` now reads the supervisor PID from the state file (via `parseStateFile`)
    and sends SIGTERM before removing the state file.
  - `performStop` reads the worker pane id from `.claude/.cam-worker-pane` and sends
    `tmux respawn-pane -k -t <pane> echo stopped` to kill the worker command cleanly.
    Attempted even when the session is not alive (race window protection).
  - `runStop` prints supervisor/worker pane kill status lines.
- Updated `src/commands/resume.ts`:
  - Added `'live-supervisor'` and `'orphaned-worker'` to `ResumeMode`.
  - Added `workerPaneAlive` to `ResumeReport`.
  - Added `isWorkerPaneAlive(paneId, spawnFn)`: uses `tmux -L cam list-panes -a -F #{pane_id}`
    to enumerate panes and checks membership.
  - `classifyResumeMode` gains optional `workerPaneAlive` 7th parameter:
    - PID alive → `live-supervisor` (was `respawn`).
    - PID dead but pane alive → `orphaned-worker`.
  - `buildResumeReport` reads worker pane marker and passes pane liveness to classifier.
  - `runResume` handles `live-supervisor` (informs operator, exit 0) and
    `orphaned-worker` (shows `cam next` with note that pane is allocated).
  - `printSummary` shows `worker: pane alive/absent` row.
  - Added `workerPaneReader` injectable dep to `ResumeOptions`.
- Updated `test/stop.test.ts`:
  - Added `KillFn` import and `respawnPaneSucceeds` to fake spawn handler.
  - Added tests: supervisor PID kill (alive, no pid field, dead PID, no file),
    worker pane kill (pane found, null pane, no session race window).
- Updated `test/resume.test.ts`:
  - Added `isWorkerPaneAlive` import; added `livePaneIds` to fake spawn handler.
  - Added `isWorkerPaneAlive` unit tests (null, empty, found, not found, list-panes fails).
  - Added US-009 classifier matrix tests (no-state, live-supervisor, orphan-pane,
    fully-dead, noop wins, live-supervisor wins over orphaned-worker).
  - Updated existing test: "heartbeat PID alive → respawn" renamed to
    "supervisor PID alive → live-supervisor".

### Files changed
- `src/commands/stop.ts` (KillFn type, supervisorKilled/workerPaneKilled, performStop logic)
- `src/commands/resume.ts` (new modes, isWorkerPaneAlive, buildResumeReport, runResume)
- `test/stop.test.ts` (supervisor PID and worker pane kill tests)
- `test/resume.test.ts` (classifier matrix, isWorkerPaneAlive tests, mode rename)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 653 pass

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched
- summary: US-009 uses only Node.js process.kill (built-in) and the existing tmux
  socket pattern (validated in prior stories). No new npm packages.

### Notes
- isWorkerPaneAlive uses `list-panes -a` (all panes on socket) rather than targeting
  a specific session, so the check works even if the session name is unknown to resume.
- The `live-supervisor` mode replaces the old `respawn` for the "PID alive" branch.
  Existing callers checking for `respawn` mode need updating (done in test file).
- Worker pane kill is attempted even when the project session is dead to cover the
  race where the pane outlives the has-session window.
---

## 2026-06-08 - US-011: Worker timeout + crash detection

### What was done
- Extended `WaitForFn` in `loop.ts` to return `{ timedOut: boolean }` and accept
  `timeoutMs: number` (breaking change: all callers and fakes updated).
- Added `IsPaneAlive = (paneId: string) => boolean` injectable type.
- Added `DEFAULT_PER_WORKER_TIMEOUT_MS = 30 * 60 * 1000` (30 min) constant.
- Added `isPaneAlive: IsPaneAlive` and `perWorkerTimeoutMs?: number` to
  `RunSupervisorOptions`.
- In the implement branch of `runSupervisor`: after `waitFor(channel, perWorkerTimeoutMs)`,
  check `waitResult.timedOut`: pane dead -> `blocked` detail='pane-died-pre-result';
  pane alive -> kill pane via `respawn-pane -k ... 'echo timeout'` + `blocked`
  detail='timeout'. Both timeout cases increment iterations and `continue` the loop
  (proceed to decideNextAction) instead of exiting, enabling recovery to the next story.
- Updated `next.ts`: real `waitFor` adapter uses `spawnSync timeout` option and checks
  `result.signal !== null` for `timedOut`; new `isPaneAlive` adapter uses `list-panes -t
  paneId` exit code; `perWorkerTimeoutMs` read from `CAM_WORKER_TIMEOUT_MS` env or default.
- Decoupled `review.ts` from `WaitForFn`: introduced `ReviewWaitForFn = (channel) => void`
  for the unbounded reviewer wait; stopped importing `WaitForFn` from loop.ts.
- Updated `loop.test.ts`: `makeBaseOpts` updated with new `waitFor` fake signature and
  `isPaneAlive` (default: pane alive); added 6 new tests: timeout-path, pane-died-path,
  waitFor-receives-timeoutMs, recovery-to-next-story, timeout-detail, pane-died-lastOutcome.
- Updated `review.test.ts`: replaced `WaitForFn` import with `ReviewWaitForFn`.

### Files changed
- `src/supervisor/loop.ts` (WaitForFn, IsPaneAlive, options, timeout branch)
- `src/commands/next.ts` (waitFor adapter, isPaneAlive adapter, perWorkerTimeoutMs)
- `src/supervisor/review.ts` (ReviewWaitForFn decoupling)
- `test/supervisor/loop.test.ts` (fakes updated, 6 new tests)
- `test/supervisor/review.test.ts` (ReviewWaitForFn import)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 660 pass

### Step 5.5 docs validation
- lib: tmux, status: ok, url: https://github.com/tmux/tmux/blob/master/cmd-wait-for.c
- summary: confirmed wait-for has no native timeout option; spawnSync timeout + signal
  check is the correct approach. list-panes -t paneId exit code for dead pane is non-zero
  (confirmed by isWorkerPaneAlive pattern from US-009 already in codebase).

### Notes
- The key design insight: timeout/pane-died cases CONTINUE the loop (not exit) so the
  supervisor can pick another story. Only sentinel BLOCKED_* cases exit the loop.
- Pane-died path does NOT send 'echo timeout' (pane already dead, no need to kill).
- ReviewWaitForFn decoupling keeps the review dispatch simple (no timeout needed there).
---

## 2026-06-08 - US-010: Docs: rewrite cam-next.md and update CLAUDE.md/README/CHANGELOG

### What was done
- Rewrote `.claude/commands/cam-next.md` to describe the deterministic TS supervisor.
  Removed all references to stop-hook driver, Task subagent dispatch, and /cam-next
  re-inject. Added Architecture diagram, Worker entrypoint section, Worker exit contract
  table (CAM_IMPLEMENTER_STATUS sentinel), and updated Branch decision matrix to reflect
  the supervisor loop (not the COMPLETE promise / stop hook).
- Updated `scripts/cam/CLAUDE.md` Stop Condition paragraph to reference runSupervisor
  instead of /cam-next. Fixed all em-dashes throughout the file (project convention).
- Updated `README.md`: added Architecture section describing the worker-slot + supervisor
  model, updated Recent changes to lead with CAM-22 deterministic supervisor entry.
- Created `CHANGELOG.md` with Unreleased entry: "Retired the stop-hook loop driver;
  workers are now real per-story claude sessions in a single reused tmux pane (CAM-22,
  closes CAM-29)."
- No TypeScript or test changes; this is a pure docs story.

### Files changed
- `.claude/commands/cam-next.md` (rewrite)
- `scripts/cam/CLAUDE.md` (stop condition + em-dash cleanup)
- `README.md` (Architecture section + Recent changes)
- `CHANGELOG.md` (new file)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 653 pass

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched
- summary: US-010 is a pure docs rewrite. No external libraries touched.

### Notes
- cam-next.md no longer describes a COMPLETE promise or stop-hook sentinel. The
  CAM_IMPLEMENTER_STATUS sentinel is still the worker exit contract, consumed by
  src/supervisor/result.ts, not by a stop-hook script.
---

## 2026-06-08 - US-012: Interactive review/plan in-pane with sentinel detection

### What was done
- Added `parseAnySentinel(paneText)` to `result.ts`: scans for `CAM_IMPLEMENTER_STATUS=*`,
  `CAM_REVIEWER_STATUS=*`, or `<review>CLEAN|FIXES_PENDING:N</review>` tags. Returns
  `{ source, raw }` or null. Used by the polling path to stop polling.
- Added `interactive?: boolean` to `ImplementerWorkerArgvOptions` in `worker-argv.ts`.
  When `true`: omits `-p`, omits `--output-format text`, and omits the `; tmux wait-for -S`
  chain entirely. The `channel` field is now optional (not used in interactive mode).
- Added four new injections to `RunSupervisorOptions` in `loop.ts`:
  - `implementerMode?: 'exit' | 'sentinel'` (default: `'exit'`).
  - `pollIntervalMs?: number` (default: `DEFAULT_POLL_INTERVAL_MS = 5000`).
  - `sleepFn?: (ms: number) => void` (default: no-op; inject for real sleep).
  - `nowMs?: () => number` (default: `Date.now`; inject for deterministic tests).
- Exported `DEFAULT_POLL_INTERVAL_MS = 5_000` constant.
- Added sentinel polling branch inside the implement action in `runSupervisor`: loops
  calling `sleepFn(pollIntervalMs)`, then checks `isPaneAlive`, then `capturePane` +
  `parseAnySentinel`, then elapsed-time timeout. Pane-died and timeout paths mirror the
  exit-mode timeout/pane-died paths (blocked + continue). Sentinel-found path runs the
  full `readWorkerOutcome` + outcome-handling identical to the exit path.
- Added 6 interactive-mode tests to `test/supervisor/worker-argv.test.ts`: omits -p,
  omits --output-format, omits wait-for chain, retains other flags, starts with "claude ",
  and tee still works.
- Added 4 sentinel-polling tests to `test/supervisor/loop.test.ts`: sentinel-detected,
  timeout-blocked (loops continues), pane-died-blocked (loop continues), constant value.

### Files changed
- `src/supervisor/result.ts` (new export: parseAnySentinel, AnySentinelMatch, AnySentinelSource)
- `src/supervisor/worker-argv.ts` (interactive option, channel now optional)
- `src/supervisor/loop.ts` (4 new injections, DEFAULT_POLL_INTERVAL_MS, sentinel polling branch)
- `test/supervisor/worker-argv.test.ts` (6 new interactive-mode tests)
- `test/supervisor/loop.test.ts` (4 new sentinel-polling tests)

### Quality gates
- Typecheck: ok
- Lint: n/a
- Tests: 677 pass

### Step 5.5 docs validation
- lib: tmux, status: ok, url: https://man.openbsd.org/tmux.1
- summary: capture-pane -p -S -N usage confirmed: -S start-line, -N end-line bound the
  scrollback region scanned; omitting them dumps entire visible pane which is fine for
  our polling use case. No mismatches found.

### Notes
- Default `sleepFn` is a no-op so production code needs the caller (next.ts) to inject
  `Bun.sleepSync` if real polling delays are wanted. Tests always get a no-op.
- `implementerMode` defaults to `'exit'` so all existing tests continue to use the exit
  path unchanged (no fakes needed for the new options).
- `channel` in ImplementerWorkerArgvOptions is now optional: not needed for interactive
  mode. Existing callers that pass `channel` explicitly are unaffected.
---
