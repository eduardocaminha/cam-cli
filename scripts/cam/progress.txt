## Codebase Patterns

<!--
Populated by implementers as reusable patterns emerge during stories.
Durable patterns carried across cycles:

- **Bun runtime**: always `Bun.spawn`/`Bun.$`/`Bun.file` over `node:child_process`/`fs` per project CLAUDE.md.
- **Permission mode**: never register a `--permission-mode` CLI flag on any subcommand,
  enforced by `test/no-permission-mode-flag.test.ts` (greps `index.ts` and `src/commands/`).
- **Tests**: `bun test` from repo root; test files mirror source under `test/`.
- **Conventional commits**: `feat:`, `fix:`, `chore:`, `refactor:`, `docs:`.
- **noUncheckedIndexedAccess**: tsconfig has `noUncheckedIndexedAccess: true`. Array indexing returns `T | undefined`; always guard with `?? fallback` or a justified non-null assertion.
- **Regex capture groups**: `match()` capture groups are `string | undefined` under strict+noUncheckedIndexedAccess; always use `?? ''` fallback.
- **Bun.spawn detached option**: `detached?: boolean` is in `bun.d.ts` (calls `setsid()` on POSIX) but the docs webpage omits it. Check `node_modules/bun-types/bun.d.ts` for the full option surface. Pair with `proc.unref()` and `stdio: ['ignore','ignore','ignore']` so the parent does not wait on the child.
- **tmux argv tests**: cam shells out to tmux via injectable spawn fns; unit tests assert the argv shape (new-session / split-window / has-session / send-keys / kill-session / select-pane), they do not spin a real tmux server. Follow this pattern for the cam-run-workspace cycle.
- **Ink screens**: success/failure is signalled by the glyph (accent check / destructive cross), never by divider color. Render and look at real output; do not trust header comments (lessons.md 2026-06-05).
- **No em-dash in persisted .md**: use commas, colons, parentheses, or periods.
- **src/tmux/session.ts is the canonical session module**: projectSessionName, ensureProjectSession, openPaneInSession, isInsideProjectSession all live here with injectable SpawnFn. Do not duplicate session logic in command files; import from this module instead.
- **isInsideProjectSession uses CAM_SESSION env tag**: because $TMUX_PANE only contains a pane index (not the session name), callers must set CAM_SESSION=<sessionName> when spawning claude inside a session for reliable inside-session detection.
-->

## [2026-06-05 12:00] - US-002: cam run uses the session module; dashboard becomes a permanent pane

### What was done
- Removed duplicate projectSessionName, tmuxHasSession, createOrchestratorSession, and buildRunMenuScript from src/commands/run.ts.
- run.ts now imports projectSessionName and ensureProjectSession from src/tmux/session.ts.
- Re-exports projectSessionName from run.ts for backward compatibility with test/run.test.ts.
- runRun accepts injectable SpawnFn via RunOptions.spawnFn (defaults to spawnSync) for unit-testable argv assertions.
- Right pane now runs `cam dashboard` via tmux send-keys to pane :0.1, replacing the static bash menu script.
- Added 6 new tmux argv assertion tests in test/run.test.ts (new-session flags, split-window, dashboard send-keys, orchestrator send-keys, session-exists skip, tmux-unavailable failure).

### Files changed
- src/commands/run.ts (rewritten: -143 lines net, session logic delegated to module)
- test/run.test.ts (extended: +92 lines, 6 new tests)
- scripts/cam/prd.json (passes: true for US-002)

### Quality gates
- Typecheck: ok
- Tests: 438 pass / 0 fail (6 new)

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched (only node:fs, node:path, and the internal session module)

### Notes
- projectSessionName is re-exported from run.ts (not removed) because test/run.test.ts imports it there; the canonical definition lives in src/tmux/session.ts.
- The send-keys approach (ensureProjectSession creates bash panes, then send-keys activates the commands) avoids embedding orchestrator boot logic in the shared session module.
---

## [2026-06-05 13:00] - US-003: Orchestrator exit tears down the session

### What was done
- Added `; tmux kill-session -t <sessionName>` suffix to the agentCmd string sent to pane 0 in setupOrchestratorSession (src/commands/run.ts).
- Added unit test "pane 0 command chains kill-session after claude exits (US-003)" in test/run.test.ts asserting the composed argv contains `kill-session -t <session>`.

### Files changed
- src/commands/run.ts (+2 lines: kill-session suffix in agentCmd)
- test/run.test.ts (+17 lines: new test for kill-session assertion)
- scripts/cam/prd.json (passes: true for US-003)

### Quality gates
- Typecheck: ok
- Tests: 439 pass / 0 fail (1 new)

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched (only internal session module and node builtins)

### Notes
- The kill-session is chained with `;` (not `&&`) so it fires even if claude exits non-zero.
---

## [2026-06-05 00:00] - US-001: Project session module (tmux helpers)

### What was done
- Created src/tmux/session.ts exporting: projectSessionName, ensureProjectSession (lazy-creates 2-pane detached session), openPaneInSession (split-window -v -d into existing session), isInsideProjectSession (pure env inspection via TMUX + CAM_SESSION).
- All helpers accept injectable SpawnFn; no global side effects.
- Created test/tmux-session.test.ts with 15 tests asserting exact tmux argv for has-session, new-session, and split-window calls.
- projectSessionName in src/commands/run.ts left in place (US-002 will migrate callers).

### Files changed
- src/tmux/session.ts (new, +130 lines)
- test/tmux-session.test.ts (new, +170 lines)
- scripts/cam/prd.json (passes: true for US-001)

### Quality gates
- Typecheck: ok
- Tests: 432 pass (15 new)

### Step 5.5 docs validation
- lib: none, status: no_external_lib_touched (only node:path and node:crypto built-ins)

### Notes
- isInsideProjectSession relies on CAM_SESSION env var (set by caller when spawning claude inside the session) because $TMUX_PANE does not encode the session name. This is the correct detection pattern for this cycle.
---
