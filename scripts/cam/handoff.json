{
  "lastCompletedStory": { "id": "US-002", "title": "Durable plan-preflight-failed marker module" },
  "branchName": "cam/pr-215-plan-preflight-failed-surfacing",
  "timestamp": "2026-07-08T21:30:00Z",
  "createdFiles": [
    "src/supervisor/plan-preflight-marker.ts",
    "test/supervisor/plan/plan-preflight-marker.test.ts"
  ],
  "modifiedFiles": [],
  "openQuestions": [],
  "nextStoryContext": "US-002 added src/supervisor/plan-preflight-marker.ts, mirroring src/supervisor/plan-escalation.ts verbatim in structure: PLAN_PREFLIGHT_FAILED_FILENAME = '.cam-plan-preflight-failed.json', interface PlanPreflightFailedMarker { step: string; detail: string; writtenAt: string } (no issueId), type PlanPreflightFailedWriterParams = Omit<PlanPreflightFailedMarker, 'writtenAt'>, readPlanPreflightFailedMarker (shape-guarded, never throws, null on any malformation), writePlanPreflightFailedMarker (overwrite, never throws), removePlanPreflightFailedMarker (best-effort unlink, never throws), and a pure truncatePreflightDetail(detail: string): string helper (single-line unchanged; multi-line -> firstLine + ' (+N more)', N = remaining line count after splitting on '\\n'). US-003 is next: wire this into runPostAuditAction (src/supervisor/plan-runner.ts, PostAuditActionResult union near :932, generic non-approved fall-through currently at :1147) -- add a new { kind: 'preflight-failed' } arm placed BEFORE the generic fall-through. GOTCHA (from PRD notes): at the point preflight fails inside runPlanPhase (plan-runner.ts:748-758), notifyFn is NOT in scope (only logEvent is) -- the marker write + notify must happen one layer up in runPostAuditAction, where notifyFn (makeNotifyOrchestrator, host.ts:198) is already constructed. Write the marker regardless of notify outcome (notify is best-effort silent no-op when the orch pane is gone; the durable marker is the source of truth). Follow the optional-seam-with-no-op-default pattern used for writeEscalationMarkerFn (plan-runner.ts:815, :849): add a writePreflightFailedMarkerFn?: (params: PlanPreflightFailedWriterParams) => void option, defaulting to a no-op. The notify line should use truncatePreflightDetail(detail) for the pane narration (format mirrors the existing ship-stalled/plan-escalated lines: step then detail) but the marker-writer seam must receive the FULL untruncated detail. Production wiring in src/commands/sidecar.ts: add makeWritePreflightFailedMarkerFn next to makeWriteEscalationMarkerFn (~sidecar.ts:1696), stamping writtenAt (ISO 8601) and writing to .claude/.cam-plan-preflight-failed.json, wired into runPostPlanActions; exitPhaseAfterPlan (sidecar.ts:1600) needs a new arm reverting phase to idle for the preflight-failed kind. Do NOT touch the existing plan-preflight-failed event write in runPlanPhase (plan-runner.ts:755, events.ts) -- US-003 only adds the marker + notify on top of it, unchanged event. US-004 (marker removal, Option B: remove on ANY non-preflight-failed result, not gated on issueId) and US-005 (orchestrator boot step 9 reading the marker, both .claude/agents/ and templates/agents/ copies, then bun run embed-vendor) come after US-003 and both depend on this module's exports as-is.",
  "officialDocsValidated": [
    { "lib": "none", "status": "no_external_lib_touched" }
  ]
}
