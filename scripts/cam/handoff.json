{
  "lastCompletedStory": {
    "id": "US-003",
    "title": "Fix stale cam-ship Step 3 pointer in patterns.md"
  },
  "branchName": "cam/issue-260",
  "timestamp": "2026-07-10T00:00:00Z",
  "modifiedFiles": [
    "scripts/cam/patterns.md"
  ],
  "openQuestions": [],
  "nextStoryContext": "US-003 rewrote the stale patterns.md bullet at ~line 116 (formerly 'cam-ship Step 3 ratchet raise-and-retry'). It no longer points at the removed `.claude/commands/cam-ship.md` Step 3; it now states the deterministic ship runner (`src/supervisor/ship-runner.ts`) does not auto-raise a ratchet ceiling, that the raise-and-retry (edit budget file + staged `_ref` + re-run gate) is the implementer's in-story responsibility before the ship phase runs, and that `docs/recovery-runbook.md` (j.1) remains the recovery-time fallback. Only `scripts/cam/patterns.md` changed (confirmed via prior story's notes: the `templates/scripts/cam/patterns.md` seed stub does not contain this bullet, so no lockstep edit or embed-vendor was needed). `scripts/check-file-sizes.ts` and `scripts/check-all.ts` were untouched (verified via `git diff --name-only origin/main`).",
  "officialDocsValidated": [
    { "lib": "none", "status": "no_external_lib_touched" }
  ]
}
