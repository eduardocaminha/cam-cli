{
  "lastCompletedStory": { "id": "US-002", "title": "Accept --issue-system none as deprecated alias in cam init and restore the build-release smoke to green" },
  "branchName": "cam/issue-239",
  "timestamp": "2026-07-09T00:00:00Z",
  "createdFiles": [
    "test/commands/setup-issue-system.test.ts"
  ],
  "modifiedFiles": [
    "src/commands/setup.ts",
    "index.ts",
    "scripts/file-size-budget.json"
  ],
  "openQuestions": [],
  "nextStoryContext": "US-002 added the 'none' -> 'local' alias branch to parseSetupArgs (src/commands/setup.ts) in BOTH the `--issue-system <val>` and `--issue-system=<val>` forms, each inserted before its own ISSUE_SYSTEMS.includes membership check. ISSUE_SYSTEMS stays ['linear', 'github', 'local'] (unchanged, still feeds the interactive Select at line 186); 'none' remains read-normalized-only, never selectable. index.ts help text (line 129 flag description + line 148 wizard-step body text) both corrected from 'linear | github | none' to 'linear | github | local' -- the AC's grep oracle scans the whole file, not just the cited line, so both occurrences needed fixing. Raised src/commands/setup.ts's ceiling in scripts/file-size-budget.json from 839->841 with a CAM-239 tracker ref in the same diff (required by checkDiffTrackerRef). Ran the full `bash scripts/build-release.sh` (no --install) end-to-end: compiled a fresh binary, hermetic init soft-check printed 'Issue system: local' confirming both this story's arg-parser fix and US-001's readIssueSystem reader fix now resolve the deprecated alias consistently end-to-end. This closes CAM-239's 2-story arc (US-001 reader + US-002 arg-parser); no further stories are queued in this PRD as of this handoff -- check prd.json for any newly-filed follow-ups before assuming PRD_COMPLETE.",
  "officialDocsValidated": [
    { "lib": "none", "status": "no_external_lib_touched" }
  ]
}
