{
  "lastCompletedStory": { "id": "US-002", "title": "Add the rotating-artifact-target OracleLintRule" },
  "branchName": "cam/issue-381",
  "timestamp": "2026-07-20T00:00:00Z",
  "createdFiles": [],
  "modifiedFiles": [
    "src/supervisor/prd-oracle-lint.ts",
    "test/supervisor/prd-oracle-lint.test.ts"
  ],
  "openQuestions": [],
  "nextStoryContext": "ROTATING_ARTIFACT_RULE is now the third entry in RULES (src/supervisor/prd-oracle-lint.ts), name 'rotating-artifact-target'. It flags an oracle command whose text contains the literal substring 'scripts/cam/handoff.json' or 'scripts/cam/review-artifact.txt' (ROTATING_ARTIFACT_TARGET_RE, a single alternation regex over the fixed literal 2-item target list from patterns.md:907/:224 -- handoff.json is the per-story implementer handoff, review-artifact.txt is the reviewer's gitignored Layer B capture-pane exit file). Deliberately does NOT flag scripts/cam/review-report.json (the structured, non-capture-pane sibling file) or any bare 'handoff.json'/'review-artifact.txt' mention lacking the scripts/cam/ prefix. Detection is purely lexical (regex.test over the oracle command string), no I/O, same OracleLintRule signature as the other two rules. Test file's pre-existing 'RULES: named-rules list shape' block was updated from length-2 to length-3 (order-sensitive: [GREP_Q_LIST_FILES_RULE, FROZEN_COMPARAND_RULE, ROTATING_ARTIFACT_RULE]) -- any future rule addition must update that assertion too. Two new dedicated describe blocks were added with load-bearing exact names ('rotating-artifact-target rule: flagged forms' / 'rotating-artifact-target rule: passing cases') consumed verbatim by this story's own `bun test -t` oracles; keep that naming convention for any future rule's dedicated test blocks. Full check:all spine (typecheck/test/lint/file-size/debt-markers/version-skips/coverage/dead-code/dup/ci-parity/agents-md/test-sleeps) is green, 5218 tests pass.",
  "officialDocsValidated": [
    { "lib": "none", "status": "no_external_lib_touched" }
  ]
}
