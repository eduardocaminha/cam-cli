{
  "project": "cam-cli",
  "branchName": "cam/issue-395",
  "description": "Convert the stale test/dispatch.test.ts:754 citation in the US-003/CAM-376 patterns.md bullet to a rot-proof content reference.",
  "issueNumber": 395,
  "type": "docs",
  "relatedDocs": [],
  "officialDocsConsulted": [],
  "userStories": [
    {
      "id": "US-001",
      "title": "Drop stale :754 line-number suffix from the US-003 patterns.md bullet",
      "description": "As a maintainer reading scripts/cam/patterns.md, I need the US-003/CAM-376 bullet to cite test/dispatch.test.ts by file plus its already-inline-quoted assertion text (not a frozen :754 line number) so the reference does not rot when unrelated lines drift, the same fix CAM-380 applied one bullet down.",
      "acceptanceCriteria": [
        "The stale line-number citation is gone from the US-003 bullet (the `:754` suffix no longer appears anywhere in patterns.md). [oracle: ! grep -qF 'test/dispatch.test.ts:754' scripts/cam/patterns.md]",
        "The bullet still anchors on the quoted historical assertion text so the reference survives without a line number. [oracle: grep -qF 'recordedSleeps.every((ms) => ms <= 1_000)' scripts/cam/patterns.md]",
        "The US-003/CAM-376 bullet still names test/dispatch.test.ts as the file (only the :754 suffix removed). [oracle: grep -qF 'test/dispatch.test.ts' scripts/cam/patterns.md]",
        "Only the :754 citation in that one bullet is changed; the lesson's meaning is unchanged and no sibling bullet (the US-R1-002 bullet CAM-380 fixed, nor the derive-don't-freeze/content-reference precedent bullets at patterns.md:903/:913) is edited. [oracle: reviewer-judgment]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 1,
      "passes": true,
      "requires": null,
      "notes": "Single edit in scripts/cam/patterns.md, the US-003/CAM-376 bullet (currently line 907, re-grep before editing since lines drift). Change the token `test/dispatch.test.ts:754` to bare `test/dispatch.test.ts`; leave the rest of the sentence, including the inline quote `recordedSleeps.every((ms) => ms <= 1_000)`, intact. GOTCHA: the quoted assertion is HISTORICAL (pre-fix) and no longer exists in test/dispatch.test.ts on main (live assertion is strict-equality `=== POLL_INTERVAL_MS` around line 777); the durable anchor is the inline quote in patterns.md itself, so do NOT add any oracle asserting the old text resolves in the test file (would be red-on-main). Do NOT re-freeze a new :NNN suffix. Do NOT target scripts/cam/handoff.json (rotating). Doc-only: NO coherence rebuild, binary/sidecar stay at current version; does not touch vendor/ or templates/. Precedent: CAM-380 applied the identical fix to the adjacent US-R1-002 bullet, following the derive-don't-freeze rule (patterns.md:903) and content-reference precedent (patterns.md:913)."
    }
  ]
}
