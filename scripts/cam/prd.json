{
  "project": "cam-cli",
  "branchName": "cam/issue-372",
  "description": "Append a falsifiability rule for verification oracles to patterns.md and extend the CLAUDE.md Test-quality invariant with a short pointer clause.",
  "issueNumber": 372,
  "type": "docs",
  "relatedDocs": [],
  "officialDocsConsulted": [],
  "userStories": [
    {
      "id": "US-001",
      "title": "Add the oracle-falsifiability rule to the knowledge layer",
      "description": "As a subagent-planner authoring acceptance criteria and as a subagent-implementer authoring test assertions, I need a durable, greppable rule requiring every verification oracle to be proven red against unmodified main via an exhaustive sweep, so that tautological oracles like CAM-363, CAM-368 and CAM-374 stop reaching a PRD or a test file.",
      "acceptanceCriteria": [
        "RED-ON-MAIN (swept, main returns 0): the falsifiability rule exists in patterns.md. [oracle: test \"$(grep -c 'falsifiab' scripts/cam/patterns.md)\" -ge 1]",
        "RED-ON-MAIN (swept, main has no match at all): the new bullet is appended strictly BELOW every pre-existing bullet (patterns.md is 897 lines on main, final bullet at line 897). [oracle: test \"$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1)\" -gt 897]",
        "PINNING ORACLE, deliberately GREEN-ON-MAIN (regression guard on invariance, not a claim about new behavior, exempt from the red-first duty by that fact): no pre-existing line shifted, so the many patterns.md:NNN cross-references elsewhere in the file remain valid. [oracle: sed -n '776p' scripts/cam/patterns.md | grep -q 'Test-quality convention, expanded']",
        "RED-ON-MAIN (swept): the new bullet cites all three precedents on its own single line. [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); sed -n \"${L}p\" scripts/cam/patterns.md | grep -q 'CAM-363' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -q 'CAM-368' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -q 'CAM-374']",
        "RED-ON-MAIN (swept): the new bullet covers BOTH authoring surfaces, PRD acceptance criteria and in-test assertions, not just PRDs. [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); sed -n \"${L}p\" scripts/cam/patterns.md | grep -qi 'acceptance' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -qi 'test assertion']",
        "RED-ON-MAIN (swept): the new bullet is keyword-dense enough to be found by a future flat-file grep, carrying the sweep-vs-sample and red-against-main language. [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); sed -n \"${L}p\" scripts/cam/patterns.md | grep -qi 'sweep' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -qi 'oracle' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -qi 'main']",
        "RED-ON-MAIN (swept): the new bullet back-references adjacent precedent bullets by line pointer in the established house style, naming patterns.md:776 and at least one of the CAM-236 sweep-completeness cluster (:566, :570, :572) or the CAM-301/CAM-309 self-nullifying-oracle antipattern (:772, :774). [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); sed -n \"${L}p\" scripts/cam/patterns.md | grep -q 'patterns.md:776' && sed -n \"${L}p\" scripts/cam/patterns.md | grep -qE 'patterns\\.md:(566|570|572|772|774)']",
        "RED-ON-MAIN (swept, main returns 0): the curated invariants block in scripts/cam/CLAUDE.md gains a short falsifiability clause. [oracle: test \"$(grep -c 'falsifiab' scripts/cam/CLAUDE.md)\" -ge 1]",
        "RED-ON-MAIN (swept): that CLAUDE.md clause lives INSIDE the existing 'Test quality' bullet rather than as a new standalone invariant bullet. [oracle: grep -n 'falsifiab' scripts/cam/CLAUDE.md | cut -d: -f1 | while read -r n; do test \"$n\" = \"$(grep -n '^- \\*\\*Test quality\\*\\*' scripts/cam/CLAUDE.md | head -1 | cut -d: -f1)\" || exit 1; done]",
        "HOUSE STYLE, conditional on the first criterion having matched (vacuously true if it fails, so meaningful only read together with it): the new patterns.md bullet is a single unwrapped line starting with `- **`. [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); sed -n \"${L}p\" scripts/cam/patterns.md | grep -q '^- \\*\\*']",
        "HOUSE STYLE: the new patterns.md bullet carries no `[resolved` prefix and no em-dash character, per the no-em-dash-in-persisted-md rule at patterns.md:24. [oracle: L=$(grep -n 'falsifiab' scripts/cam/patterns.md | head -1 | cut -d: -f1); ! sed -n \"${L}p\" scripts/cam/patterns.md | grep -q '\\[resolved' && ! sed -n \"${L}p\" scripts/cam/patterns.md | grep -q '—']",
        "No src/ or test/ file is modified by this story (doc-only scope). [oracle: test -z \"$(git diff --name-only main...HEAD -- src test)\"]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test), zero failures with the count at or above main's baseline. [oracle: bun test]",
        "Lint spine passes (bun run check:all). [oracle: bun run check:all]"
      ],
      "priority": 1,
      "passes": false,
      "requires": null,
      "notes": "Files: scripts/cam/patterns.md (APPEND AT EOF ONLY, currently 897 lines) and scripts/cam/CLAUDE.md (extend the existing 'Test quality' bullet at line 69, in place, short form + pointer to patterns.md). GOTCHAS: (1) patterns.md has NO sections: it is a `# Codebase Patterns` heading, a 6-line preamble, then a flat blank-line-separated list of top-level `- ` bullets to EOF. Agent docs saying 'grep for the section' rest on a false premise; discovery is pure keyword grep, so the new bullet must be KEYWORD-DENSE (falsifiable, oracle, acceptance criterion, grep, test assertion, sweep, main). (2) Many bullets cross-reference other bullets BY LINE NUMBER (patterns.md:20, :150, :324, :454, :770, :776 and others). Inserting anywhere except EOF silently invalidates every pointer; that is what the deliberately green-on-main line-776 pinning oracle guards. (3) Do NOT amend the line-776 test-quality bullet: it carries a pre-existing 'Four clauses' vs five-clauses off-by-one that is explicitly OUT OF SCOPE, and editing it shifts every subsequent line number. Back-reference it instead. (4) Do NOT touch test/dispatch.test.ts: the weak assertion there is CAM-374, a separate issue and a separate PR. (5) Never prefix a living rule with `[resolved YYYY-MM]`: `cam patterns archive` sweeps any bullet carrying it into patterns.archive.md, and patterns.md currently has zero such markers. (6) No file-size budget applies: scripts/check-file-sizes.ts:96 globs `{src,scripts,test}/**/*.{ts,tsx}` with no .md handling, so no budget raise and no tracker-ref ceremony. (7) No vendor/templates change, so `bun run embed-vendor:check` is not in scope; verified there is no template dual-copy of scripts/cam/CLAUDE.md (grep -rl 'Test quality' matches only that file). (8) Generalize, do not restate, the nearby oracle-CORRECTNESS bullets (:772/:774 self-nullifying `-q` + `-L`/`-l`, :616 structurally unsatisfiable file-count oracles, :412/:842/:588 negative-grep matching comments, and the CAM-236 sweep-completeness cluster at :566/:570/:572); this bullet is about oracle FALSIFIABILITY, which is genuinely new language (`grep falsifiab` returns 0 on main for both files). (9) The rule applies recursively to this PRD: every oracle above was swept red against unmodified main except the line-776 pinning oracle (deliberately green) and the two house-style oracles (vacuous unless the first criterion matches), both labelled inline."
    }
  ]
}
