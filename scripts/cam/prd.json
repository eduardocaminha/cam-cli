{
  "project": "cam-cli",
  "branchName": "cam/issue-482",
  "description": "Remove every hardcoded self-binary-name dependency from the code: auto-spawn resolves by execPath, process discovery stops comparing argv against a single name literal, and the self-invoke helper drops its own literal fallback.",
  "issueNumber": 482,
  "type": "feat",
  "relatedDocs": [
    "docs/adr/0054-gship-alias-and-the-pre-launch-window-for-internal-contract-renames.md",
    "docs/adr/0055-o-primeiro-release-do-gateship-sai-como-prerelease-para-manter-aberta-a-janela-de-rename-do-adr-0054.md"
  ],
  "officialDocsConsulted": [
    {
      "lib": "Bun",
      "channel": "WebFetch",
      "url": "https://bun.sh/docs/api/spawn",
      "version": "current",
      "fetchedAt": "2026-08-02T00:00:00.000Z",
      "summary": "Bun.spawn/Bun.spawnSync take `command: string[]` and use posix_spawn(3); cmd[0] is an executable path or name, so passing an absolute `process.execPath` is fully supported and needs no PATH lookup. An `argv0` option exists to override the argv[0] the child advertises, which is what other processes (and `ps -eo pid,args`) observe. No breaking changes or deprecations affecting this cycle.",
      "localConventionConflict": "Bun exposes `argv0` as a way to keep advertising a fixed name to `ps`, which would preserve stop.ts's current literal-name discovery; the local convention (execPath self-invoke, src/util/self-invoke.ts) instead makes the advertised argv0 vary by install path, so discovery must become name-agnostic rather than being pinned back with argv0.",
      "status": "ok"
    }
  ],
  "userStories": [
    {
      "id": "US-R8-001",
      "title": "CRITICAL: Layer B behavioral-gate FAIL (hard constraint): US-002 acceptance cri…",
      "priority": 1,
      "passes": true,
      "description": "CRITICAL [test/integration/dashboard-pane-selfspawn.test.ts:35]: Layer B behavioral-gate FAIL (hard constraint): US-002 acceptance criterion 4's oracle `bun test test/integration/dashboard-pane-selfspawn.test.ts` exited non-zero on 3 of the reviewer's 34 independent re-runs. The test is NOT hermetic: it pins a fixed tmux socket name (TEST_SOCK = 'cam-it-selfspawn', line 35) and runs `tmux kill-server` on it in beforeEach (line 54) and afterEach (line 62), so any second process running the same file destroys the first instance's server, the respawned pane child dies before writing marker.json, and the wait at line 101 expires at exactly 10s. Evidence (artifact section 4): idle 10/10 pass (1.5-1.7s); heavy CPU load with no second tmux client 12/12 pass at load average 15-27 (2.4-2.6s); with a concurrent `bun test` 1/12 FAIL at 10.07s; with two simultaneous copies 2/6 FAIL. This also FALSIFIES the round-7 finding's stated mechanism (host load consuming the 10s budget): raising the marker-wait budget would not fix it, because the pane's server is gone, not slow. This matters live for this project: `bun test` is run concurrently in the same repo by the worker quality gate, `cam next` preflight and the reviewer's own re-run. Mechanical 1-file fix: derive the socket name per process (e.g. `cam-it-selfspawn-${process.pid}`) so kill-server can never cross instances. The 15 pre-existing integration files sharing the fixed-socket convention are out of scope for this finding; only the file this PR introduces is in scope.",
      "notes": "CRITICAL [test/integration/dashboard-pane-selfspawn.test.ts:35]: Layer B behavioral-gate FAIL (hard constraint): US-002 acceptance criterion 4's oracle `bun test test/integration/dashboard-pane-selfspawn.test.ts` exited non-zero on 3 of the reviewer's 34 independent re-runs. The test is NOT hermetic: it pins a fixed tmux socket name (TEST_SOCK = 'cam-it-selfspawn', line 35) and runs `tmux kill-server` on it in beforeEach (line 54) and afterEach (line 62), so any second process running the same file destroys the first instance's server, the respawned pane child dies before writing marker.json, and the wait at line 101 expires at exactly 10s. Evidence (artifact section 4): idle 10/10 pass (1.5-1.7s); heavy CPU load with no second tmux client 12/12 pass at load average 15-27 (2.4-2.6s); with a concurrent `bun test` 1/12 FAIL at 10.07s; with two simultaneous copies 2/6 FAIL. This also FALSIFIES the round-7 finding's stated mechanism (host load consuming the 10s budget): raising the marker-wait budget would not fix it, because the pane's server is gone, not slow. This matters live for this project: `bun test` is run concurrently in the same repo by the worker quality gate, `cam next` preflight and the reviewer's own re-run. Mechanical 1-file fix: derive the socket name per process (e.g. `cam-it-selfspawn-${process.pid}`) so kill-server can never cross instances. The 15 pre-existing integration files sharing the fixed-socket convention are out of scope for this finding; only the file this PR introduces is in scope."
    },
    {
      "id": "US-R8-002",
      "title": "WARNING: The US-R6-002 fd-leak fix (`closeSync(logFd)` after Bun.spawn) ships w…",
      "priority": 2,
      "passes": true,
      "description": "WARNING [src/commands/sidecar-liveness-watch.ts:149]: The US-R6-002 fd-leak fix (`closeSync(logFd)` after Bun.spawn) ships with no falsifying test, so the suite cannot detect its removal. Verified independently: `grep -rn closeSync test/` returns no matches and `grep -rn lsof test/` (excluding test-deps) returns only comments and the skip gate, so nothing anywhere asserts the PARENT process's descriptor table. The only test that constructs the production closure (test/integration/stop-defaultlistprocesses-real.test.ts:204-219) asserts child discoverability and SIGTERM delivery, which hold identically with or without the parent-side close. This is the same falsifiability gap US-R6-003 was filed to close for US-R4-001, recurring one story later in the same file. The seam already exists: `makeSpawnSidecarFn(cwd, argvOverride)` is exported, so a test can invoke it N times and assert via `lsof -p <own pid>` that the parent holds no accumulating fd on .claude/cam-supervisor.log.",
      "notes": "WARNING [src/commands/sidecar-liveness-watch.ts:149]: The US-R6-002 fd-leak fix (`closeSync(logFd)` after Bun.spawn) ships with no falsifying test, so the suite cannot detect its removal. Verified independently: `grep -rn closeSync test/` returns no matches and `grep -rn lsof test/` (excluding test-deps) returns only comments and the skip gate, so nothing anywhere asserts the PARENT process's descriptor table. The only test that constructs the production closure (test/integration/stop-defaultlistprocesses-real.test.ts:204-219) asserts child discoverability and SIGTERM delivery, which hold identically with or without the parent-side close. This is the same falsifiability gap US-R6-003 was filed to close for US-R4-001, recurring one story later in the same file. The seam already exists: `makeSpawnSidecarFn(cwd, argvOverride)` is exported, so a test can invoke it N times and assert via `lsof -p <own pid>` that the parent holds no accumulating fd on .claude/cam-supervisor.log."
    },
    {
      "id": "US-R8-003",
      "title": "WARNING: patterns.md (an agent-read artifact injected into future workers' cont…",
      "priority": 3,
      "passes": false,
      "description": "WARNING [scripts/cam/patterns.md:1158]: patterns.md (an agent-read artifact injected into future workers' context) states the isSidecarArgv contract twice in a form that contradicts the shipped code, and one of the two references now dangles. Line 1154 says the predicate matches 'last element sidecar, length 2 or 3'; line 1158 says it 'now checks only argv.length >= 2 && argv[argv.length-1] === sidecar && argv[0].startsWith(\"/\")'. The shipped predicate (src/commands/stop.ts:342-345) is `argv.length >= 2 && last === 'sidecar'` with no token-count and no argv[0] requirement. Line 1158 additionally points at 'the \"[resolved 2026-08-02]\" US-004 bullet above', a marker US-R1-004 removed in this same branch, so the pointer no longer resolves. A future worker greps 'isSidecarArgv' and hits the false headline first; reconstructing the real 2-line predicate now requires reading four bullets in order. Mechanical 1-file fix: state the current contract once and drop or repoint the dangling reference.",
      "notes": "WARNING [scripts/cam/patterns.md:1158]: patterns.md (an agent-read artifact injected into future workers' context) states the isSidecarArgv contract twice in a form that contradicts the shipped code, and one of the two references now dangles. Line 1154 says the predicate matches 'last element sidecar, length 2 or 3'; line 1158 says it 'now checks only argv.length >= 2 && argv[argv.length-1] === sidecar && argv[0].startsWith(\"/\")'. The shipped predicate (src/commands/stop.ts:342-345) is `argv.length >= 2 && last === 'sidecar'` with no token-count and no argv[0] requirement. Line 1158 additionally points at 'the \"[resolved 2026-08-02]\" US-004 bullet above', a marker US-R1-004 removed in this same branch, so the pointer no longer resolves. A future worker greps 'isSidecarArgv' and hits the false headline first; reconstructing the real 2-line predicate now requires reading four bullets in order. Mechanical 1-file fix: state the current contract once and drop or repoint the dangling reference."
    },
    {
      "id": "US-R6-001",
      "title": "CRITICAL: Layer B behavioral-gate FAIL (hard constraint): the 'PIN DE INVARIANC…",
      "priority": 4,
      "passes": true,
      "description": "CRITICAL [src/commands/sidecar-liveness-watch.ts:103]: Layer B behavioral-gate FAIL (hard constraint): the 'PIN DE INVARIANCIA' oracle for the five internal ADR-0054 contracts is an acceptance criterion of US-001, US-002, US-003 and US-004 (all four recorded passes:true) and exits 1 on the reviewer's independent re-run. Three of the four patterns are at parity (CAM_ 164=164, .cam- 153=153, tmux -L cam 15=15) but /cam- is 205 on main vs 206 in the worktree, so `test \"$b\" = \"$a\"` fails. Per-file delta: src/commands/run.ts 16->15 (inline 'cam-supervisor.log' literal replaced by SIDECAR_LOG_FILENAME), src/commands/stop.ts 2->3 (new docstring line 403 spelling `.claude/cam-supervisor.log`), src/commands/sidecar-liveness-watch.ts 0->1 (new docstring line 103 spelling `.claude/cam-supervisor.log`); net +1. This is a pure git/grep computation over committed trees, run twice with identical results: deterministic, not flaky, not environmental, not pre-existing on main. It was also raised as CRITICAL by the round-5 reviewer, left unfixed, and the cycle closed as MAX_ROUNDS_DEBT with the four stories still marked passes:true. Mechanical 2-file fix: have the two new docstrings name the shared SIDECAR_LOG_FILENAME constant instead of spelling the `.claude/cam-supervisor.log` path literally, restoring count parity. Full evidence in scripts/cam/review-artifact.txt section 2, ORACLE 11.",
      "notes": "CRITICAL [src/commands/sidecar-liveness-watch.ts:103]: Layer B behavioral-gate FAIL (hard constraint): the 'PIN DE INVARIANCIA' oracle for the five internal ADR-0054 contracts is an acceptance criterion of US-001, US-002, US-003 and US-004 (all four recorded passes:true) and exits 1 on the reviewer's independent re-run. Three of the four patterns are at parity (CAM_ 164=164, .cam- 153=153, tmux -L cam 15=15) but /cam- is 205 on main vs 206 in the worktree, so `test \"$b\" = \"$a\"` fails. Per-file delta: src/commands/run.ts 16->15 (inline 'cam-supervisor.log' literal replaced by SIDECAR_LOG_FILENAME), src/commands/stop.ts 2->3 (new docstring line 403 spelling `.claude/cam-supervisor.log`), src/commands/sidecar-liveness-watch.ts 0->1 (new docstring line 103 spelling `.claude/cam-supervisor.log`); net +1. This is a pure git/grep computation over committed trees, run twice with identical results: deterministic, not flaky, not environmental, not pre-existing on main. It was also raised as CRITICAL by the round-5 reviewer, left unfixed, and the cycle closed as MAX_ROUNDS_DEBT with the four stories still marked passes:true. Mechanical 2-file fix: have the two new docstrings name the shared SIDECAR_LOG_FILENAME constant instead of spelling the `.claude/cam-supervisor.log` path literally, restoring count parity. Full evidence in scripts/cam/review-artifact.txt section 2, ORACLE 11."
    },
    {
      "id": "US-R6-002",
      "title": "WARNING: File-descriptor leak in the long-lived watcher process. makeSpawnSidec…",
      "priority": 5,
      "passes": true,
      "description": "WARNING [src/commands/sidecar-liveness-watch.ts:121]: File-descriptor leak in the long-lived watcher process. makeSpawnSidecarFn calls openSync(logPath,'a') INSIDE the returned closure, and nothing closes that fd. Unlike spawnSidecarDefault (src/commands/run.ts:857), which runs once per `cam run`, this closure runs on every respawn attempt, and handleOneTick resets state.attempts to 0 whenever the sidecar is observed alive again (lines 185-188), so a flapping sidecar accumulates descriptors without bound over a session's lifetime. Verified empirically by the reviewer rather than inferred: 5 openSync+Bun.spawn cycles left all 5 fds still open in the parent (scripts/cam/review-artifact.txt section 3); Bun's spawn docs confirm `number` is a supported stdout/stderr value and that the child gets its own duplicate, so the parent copy is redundant after spawn. Mechanical 1-file fix: closeSync(logFd) immediately after the Bun.spawn call.",
      "notes": "WARNING [src/commands/sidecar-liveness-watch.ts:121]: File-descriptor leak in the long-lived watcher process. makeSpawnSidecarFn calls openSync(logPath,'a') INSIDE the returned closure, and nothing closes that fd. Unlike spawnSidecarDefault (src/commands/run.ts:857), which runs once per `cam run`, this closure runs on every respawn attempt, and handleOneTick resets state.attempts to 0 whenever the sidecar is observed alive again (lines 185-188), so a flapping sidecar accumulates descriptors without bound over a session's lifetime. Verified empirically by the reviewer rather than inferred: 5 openSync+Bun.spawn cycles left all 5 fds still open in the parent (scripts/cam/review-artifact.txt section 3); Bun's spawn docs confirm `number` is a supported stdout/stderr value and that the child gets its own duplicate, so the parent copy is redundant after spawn. Mechanical 1-file fix: closeSync(logFd) immediately after the Bun.spawn call."
    },
    {
      "id": "US-R6-003",
      "title": "WARNING: The US-R4-001 regression test does not exercise the code US-R4-001 cha…",
      "priority": 6,
      "passes": true,
      "description": "WARNING [test/integration/stop-defaultlistprocesses-real.test.ts:189]: The US-R4-001 regression test does not exercise the code US-R4-001 changed, so it is not falsifiable against that story's fix. The fix lives in makeSpawnSidecarFn (src/commands/sidecar-liveness-watch.ts:118-125): open the sidecar log and hand the fd to Bun.spawn as stdout/stderr instead of 'ignore'. The test re-implements that shape in its own body (lines 189-194: buildSidecarRespawnArgv + a hand-rolled openSync + stdio:['ignore',logFd,logFd]) and never calls the production closure. makeSpawnSidecarFn is not exported, no test imports it, and test/commands/sidecar-liveness-watch.test.ts always injects spawnSidecarFn, so the production default is never constructed anywhere in the suite. Reverting makeSpawnSidecarFn to stdio:['ignore','ignore','ignore'] leaves all 5935 tests green. The test does verify defaultListProcesses (which is useful and real-I/O), but it is not the regression guard the story claims. Mechanical fix: export makeSpawnSidecarFn (or expose the spawn through the existing DI seam) and assert against the real respawned process that it holds .claude/cam-supervisor.log open.",
      "notes": "WARNING [test/integration/stop-defaultlistprocesses-real.test.ts:189]: The US-R4-001 regression test does not exercise the code US-R4-001 changed, so it is not falsifiable against that story's fix. The fix lives in makeSpawnSidecarFn (src/commands/sidecar-liveness-watch.ts:118-125): open the sidecar log and hand the fd to Bun.spawn as stdout/stderr instead of 'ignore'. The test re-implements that shape in its own body (lines 189-194: buildSidecarRespawnArgv + a hand-rolled openSync + stdio:['ignore',logFd,logFd]) and never calls the production closure. makeSpawnSidecarFn is not exported, no test imports it, and test/commands/sidecar-liveness-watch.test.ts always injects spawnSidecarFn, so the production default is never constructed anywhere in the suite. Reverting makeSpawnSidecarFn to stdio:['ignore','ignore','ignore'] leaves all 5935 tests green. The test does verify defaultListProcesses (which is useful and real-I/O), but it is not the regression guard the story claims. Mechanical fix: export makeSpawnSidecarFn (or expose the spawn through the existing DI seam) and assert against the real respawned process that it holds .claude/cam-supervisor.log open."
    },
    {
      "id": "US-R4-001",
      "title": "WARNING: Under-match regression, carried unfixed from round 3 and re-verified l…",
      "priority": 7,
      "passes": true,
      "description": "WARNING [src/commands/sidecar-liveness-watch.ts:105]: Under-match regression, carried unfixed from round 3 and re-verified live at Layer B this round. `makeSpawnSidecarFn` respawns the sidecar with `stdio: ['ignore','ignore','ignore']`, so that sidecar holds no fd on `.claude/cam-supervisor.log`. The new process-identity check in `defaultListProcesses` (src/commands/stop.ts:449-453) requires exactly that open fd, so a liveness-watch-respawned sidecar is invisible to `cam stop`'s fallback scoped scan and is silently left running in the one situation the fallback exists for (pid file absent or stale, e.g. after an earlier `cam stop` removed the pid file without the SIGTERM taking). main matched this class via `argv[0] === 'cam'`, so it is a regression. Reviewer's independent probe against the real ps+lsof production path (scripts/cam/review-artifact.txt section 3): pid A (spawnSidecarDefault shape, holds the log open) matched and was SIGTERM'd; pid B (byte-for-byte the makeSpawnSidecarFn shape) matched=false. This also falsifies the universal invariant the identity check is justified by, asserted at src/supervisor/sidecar-pid.ts:20-27 ('Every real sidecar process holds this file open on fd 1/2 for its entire lifetime') and repeated at src/commands/stop.ts:403-406, and the decoy case in test/integration/stop-defaultlistprocesses-real.test.ts:89-92 codifies that exact production shape as a process that must NOT be matched. Mechanical 2-file fix: give the respawn the same `openSync(join(cwd,'.claude',SIDECAR_LOG_FILENAME),'a')` stdout/stderr redirection `spawnSidecarDefault` uses (src/commands/run.ts:856-860; Bun docs confirm a raw fd number is a supported stdout/stderr value), plus a regression case in test/integration/stop-defaultlistprocesses-real.test.ts asserting a liveness-watch-shaped respawn IS discovered.",
      "notes": "WARNING [src/commands/sidecar-liveness-watch.ts:105]: Under-match regression, carried unfixed from round 3 and re-verified live at Layer B this round. `makeSpawnSidecarFn` respawns the sidecar with `stdio: ['ignore','ignore','ignore']`, so that sidecar holds no fd on `.claude/cam-supervisor.log`. The new process-identity check in `defaultListProcesses` (src/commands/stop.ts:449-453) requires exactly that open fd, so a liveness-watch-respawned sidecar is invisible to `cam stop`'s fallback scoped scan and is silently left running in the one situation the fallback exists for (pid file absent or stale, e.g. after an earlier `cam stop` removed the pid file without the SIGTERM taking). main matched this class via `argv[0] === 'cam'`, so it is a regression. Reviewer's independent probe against the real ps+lsof production path (scripts/cam/review-artifact.txt section 3): pid A (spawnSidecarDefault shape, holds the log open) matched and was SIGTERM'd; pid B (byte-for-byte the makeSpawnSidecarFn shape) matched=false. This also falsifies the universal invariant the identity check is justified by, asserted at src/supervisor/sidecar-pid.ts:20-27 ('Every real sidecar process holds this file open on fd 1/2 for its entire lifetime') and repeated at src/commands/stop.ts:403-406, and the decoy case in test/integration/stop-defaultlistprocesses-real.test.ts:89-92 codifies that exact production shape as a process that must NOT be matched. Mechanical 2-file fix: give the respawn the same `openSync(join(cwd,'.claude',SIDECAR_LOG_FILENAME),'a')` stdout/stderr redirection `spawnSidecarDefault` uses (src/commands/run.ts:856-860; Bun docs confirm a raw fd number is a supported stdout/stderr value), plus a regression case in test/integration/stop-defaultlistprocesses-real.test.ts asserting a liveness-watch-shaped respawn IS discovered."
    },
    {
      "id": "US-R2-001",
      "title": "CRITICAL: The false-positive class US-R1-001 was written to close is still OPEN…",
      "priority": 8,
      "passes": true,
      "description": "CRITICAL [src/commands/stop.ts:330]: The false-positive class US-R1-001 was written to close is still OPEN, verified live at Layer B against the REAL production path (real `ps -eo pid,args` + real `lsof` via defaultListProcesses, killFn stubbed to record only): `Bun.spawn(['/usr/bin/tail','-F','sidecar'], {cwd})` still yields fallbackSidecarKilled=true and killFn receives the tail's pid (see scripts/cam/review-artifact.txt section 3). That is the EXACT reproduction round 1 recorded as the CRITICAL, so the story's own cited evidence case is not fixed. `isSidecarArgv` accepts any argv with length >= 2 whose LAST token is the literal word 'sidecar' and whose argv[0] starts with '/', so the anchor only excludes hand-typed bare-name commands; every tool/agent/editor-spawned process uses an absolute argv[0] (`/opt/homebrew/bin/rg -n sidecar`, `/opt/homebrew/bin/bun test sidecar`, `/usr/bin/tail -F sidecar`), and in this repo `sidecar` is a routine grep/test filter term. The docstring's justifying claim (stop.ts:307-310, 'an interactive command ... is invoked via a bare PATH-relative name as argv[0], never an absolute path') is disproven by the runtime probe. main was immune (it required argv[0] === 'cam'), so this is a live regression: `cam stop` SIGTERMs unrelated processes. Argv shape alone cannot separate the two classes; close it with a process-identity check the fallback can afford (it already runs `lsof` per candidate), e.g. require the candidate pid to hold this project's sidecar artifact open (.claude/cam-supervisor.log / the sidecar pid file), which also closes the under-match in the WARNING below.",
      "notes": "CRITICAL [src/commands/stop.ts:330]: The false-positive class US-R1-001 was written to close is still OPEN, verified live at Layer B against the REAL production path (real `ps -eo pid,args` + real `lsof` via defaultListProcesses, killFn stubbed to record only): `Bun.spawn(['/usr/bin/tail','-F','sidecar'], {cwd})` still yields fallbackSidecarKilled=true and killFn receives the tail's pid (see scripts/cam/review-artifact.txt section 3). That is the EXACT reproduction round 1 recorded as the CRITICAL, so the story's own cited evidence case is not fixed. `isSidecarArgv` accepts any argv with length >= 2 whose LAST token is the literal word 'sidecar' and whose argv[0] starts with '/', so the anchor only excludes hand-typed bare-name commands; every tool/agent/editor-spawned process uses an absolute argv[0] (`/opt/homebrew/bin/rg -n sidecar`, `/opt/homebrew/bin/bun test sidecar`, `/usr/bin/tail -F sidecar`), and in this repo `sidecar` is a routine grep/test filter term. The docstring's justifying claim (stop.ts:307-310, 'an interactive command ... is invoked via a bare PATH-relative name as argv[0], never an absolute path') is disproven by the runtime probe. main was immune (it required argv[0] === 'cam'), so this is a live regression: `cam stop` SIGTERMs unrelated processes. Argv shape alone cannot separate the two classes; close it with a process-identity check the fallback can afford (it already runs `lsof` per candidate), e.g. require the candidate pid to hold this project's sidecar artifact open (.claude/cam-supervisor.log / the sidecar pid file), which also closes the under-match in the WARNING below."
    },
    {
      "id": "US-R2-002",
      "title": "WARNING: Symmetric under-match: because `isSidecarArgv` now REQUIRES argv[0].st…",
      "priority": 9,
      "passes": true,
      "description": "WARNING [src/commands/stop.ts:333]: Symmetric under-match: because `isSidecarArgv` now REQUIRES argv[0].startsWith('/'), a live sidecar advertised as `cam sidecar` (bare argv[0]) is invisible to the fallback scan. That is exactly the case US-004's own notes call out as legitimate ('a legitimate sidecar may be the ALREADY-INSTALLED binary under today's name') and is the shape every pre-US-002 binary spawns, so during the upgrade window (operator rebuilds/reinstalls while a sidecar from the previous binary is still running with a stale/absent pid file) `cam stop` silently leaves it running. main matched this shape. The diff itself had to rewrite the two pre-existing fixtures from ['cam','sidecar'] to ['/usr/local/bin/cam','sidecar'] (test/stop-sidecar-fallback.test.ts:51 and :82) to accommodate the loss, and no test covers the bare-name shape any more. A pid/artifact-based identity check (see the CRITICAL) resolves both directions with one change.",
      "notes": "WARNING [src/commands/stop.ts:333]: Symmetric under-match: because `isSidecarArgv` now REQUIRES argv[0].startsWith('/'), a live sidecar advertised as `cam sidecar` (bare argv[0]) is invisible to the fallback scan. That is exactly the case US-004's own notes call out as legitimate ('a legitimate sidecar may be the ALREADY-INSTALLED binary under today's name') and is the shape every pre-US-002 binary spawns, so during the upgrade window (operator rebuilds/reinstalls while a sidecar from the previous binary is still running with a stale/absent pid file) `cam stop` silently leaves it running. main matched this shape. The diff itself had to rewrite the two pre-existing fixtures from ['cam','sidecar'] to ['/usr/local/bin/cam','sidecar'] (test/stop-sidecar-fallback.test.ts:51 and :82) to accommodate the loss, and no test covers the bare-name shape any more. A pid/artifact-based identity check (see the CRITICAL) resolves both directions with one change."
    },
    {
      "id": "US-R1-001",
      "title": "CRITICAL: isSidecarArgv over-matches and makes `cam stop` SIGTERM unrelated pro…",
      "priority": 10,
      "passes": true,
      "description": "CRITICAL [src/commands/stop.ts:298]: isSidecarArgv over-matches and makes `cam stop` SIGTERM unrelated processes. The predicate accepts ANY argv of length 2 or 3 whose last token is the literal word 'sidecar', so any command running in the project cwd that happens to end in that word is classified as this project's sidecar and sent SIGTERM by the fallback scan (performStop step 1c, stop.ts:481-491). Proven at Layer B against the REAL production path (real `ps -eo pid,args` + real `lsof` via defaultListProcesses, killFn stubbed to record only): `Bun.spawn(['/usr/bin/tail','-F','sidecar'], {cwd})` yielded fallbackSidecarKilled=true and killFn received the tail's pid 58293 (see scripts/cam/review-artifact.txt section 3). Realistic collisions in a dev cwd: `rg sidecar`, `tail -F sidecar`, `git log sidecar`. This is a regression: main anchored the match on record.argv[0] === 'cam', so no unrelated command could match. It also defeats the intent of US-004 acceptance criterion 2 case 4 ('a process in the project cwd that is not running the sidecar subcommand does NOT match'): the chosen fixture ['bun','/repo/index.ts','run'] passes while the whole false-positive class stays open. Fix by keeping name-agnosticism but re-anchoring on a property the real self-spawn always has and an ad-hoc command does not, e.g. require argv[0] to be an absolute executable path (both production shapes, [execPath,'sidecar'] and [runtime,script,'sidecar'], always carry one because Bun.spawn is handed process.execPath), and add a negative test for the ['tail','-F','sidecar'] / ['rg','sidecar'] class.",
      "notes": "CRITICAL [src/commands/stop.ts:298]: isSidecarArgv over-matches and makes `cam stop` SIGTERM unrelated processes. The predicate accepts ANY argv of length 2 or 3 whose last token is the literal word 'sidecar', so any command running in the project cwd that happens to end in that word is classified as this project's sidecar and sent SIGTERM by the fallback scan (performStop step 1c, stop.ts:481-491). Proven at Layer B against the REAL production path (real `ps -eo pid,args` + real `lsof` via defaultListProcesses, killFn stubbed to record only): `Bun.spawn(['/usr/bin/tail','-F','sidecar'], {cwd})` yielded fallbackSidecarKilled=true and killFn received the tail's pid 58293 (see scripts/cam/review-artifact.txt section 3). Realistic collisions in a dev cwd: `rg sidecar`, `tail -F sidecar`, `git log sidecar`. This is a regression: main anchored the match on record.argv[0] === 'cam', so no unrelated command could match. It also defeats the intent of US-004 acceptance criterion 2 case 4 ('a process in the project cwd that is not running the sidecar subcommand does NOT match'): the chosen fixture ['bun','/repo/index.ts','run'] passes while the whole false-positive class stays open. Fix by keeping name-agnosticism but re-anchoring on a property the real self-spawn always has and an ad-hoc command does not, e.g. require argv[0] to be an absolute executable path (both production shapes, [execPath,'sidecar'] and [runtime,script,'sidecar'], always carry one because Bun.spawn is handed process.execPath), and add a negative test for the ['tail','-F','sidecar'] / ['rg','sidecar'] class."
    },
    {
      "id": "US-R1-002",
      "title": "WARNING: The same predicate under-matches a real sidecar whose executable path…",
      "priority": 11,
      "passes": true,
      "description": "WARNING [src/commands/stop.ts:336]: The same predicate under-matches a real sidecar whose executable path contains a space. defaultListProcesses splits the `ps` args column with `argsStr.split(/\\s+/)`, and isSidecarArgv requires exactly 2 or 3 tokens; before this branch argv[0] was always the bare literal 'cam' so token count was stable, but US-002 now puts the full absolute execPath (and, interpreted, the full script path) into the ps line. An install under e.g. `/Users/x/My Tools/cam` produces 3+ tokens for the compiled shape and 4+ for the interpreted one, so the live sidecar becomes invisible to the fallback scan and `cam stop` silently leaves it running. The diff itself treats space-bearing paths as in-scope (test/integration/dashboard-pane-selfspawn.test.ts:74-76 deliberately uses 'cam script dir/run me.ts'), so the ps parser should tolerate them too (e.g. anchor on the last token plus an absolute-path prefix rather than an exact token count).",
      "notes": "WARNING [src/commands/stop.ts:336]: The same predicate under-matches a real sidecar whose executable path contains a space. defaultListProcesses splits the `ps` args column with `argsStr.split(/\\s+/)`, and isSidecarArgv requires exactly 2 or 3 tokens; before this branch argv[0] was always the bare literal 'cam' so token count was stable, but US-002 now puts the full absolute execPath (and, interpreted, the full script path) into the ps line. An install under e.g. `/Users/x/My Tools/cam` produces 3+ tokens for the compiled shape and 4+ for the interpreted one, so the live sidecar becomes invisible to the fallback scan and `cam stop` silently leaves it running. The diff itself treats space-bearing paths as in-scope (test/integration/dashboard-pane-selfspawn.test.ts:74-76 deliberately uses 'cam script dir/run me.ts'), so the ps parser should tolerate them too (e.g. anchor on the last token plus an absolute-path prefix rather than an exact token count)."
    },
    {
      "id": "US-R1-003",
      "title": "WARNING: US-004's only new coverage feeds hand-built ProcessRecord objects stra…",
      "priority": 12,
      "passes": true,
      "description": "WARNING [test/commands/stop-name-agnostic.test.ts:38]: US-004's only new coverage feeds hand-built ProcessRecord objects straight into matchesSidecarForProject; nothing in the diff exercises defaultListProcesses (src/commands/stop.ts:318-359), the code that actually parses real `ps -eo pid,args` output and calls `lsof` and that this story changed (quick filter swapped, argv[0] basename normalization deleted). Both findings above were found by a real-process probe and are invisible to the fixture-only tests: fixtures encode exactly the token shape the new code expects, which is the fakes-lie pattern patterns.md records for CAM-55 and which the project's own test-quality rule ('hit real I/O at wire boundaries') forbids. Add one integration test that spawns a real process with the interpreted self-spawn argv shape in a temp cwd and asserts discovery finds it (and asserts an adjacent non-sidecar command in the same cwd is NOT matched).",
      "notes": "WARNING [test/commands/stop-name-agnostic.test.ts:38]: US-004's only new coverage feeds hand-built ProcessRecord objects straight into matchesSidecarForProject; nothing in the diff exercises defaultListProcesses (src/commands/stop.ts:318-359), the code that actually parses real `ps -eo pid,args` output and calls `lsof` and that this story changed (quick filter swapped, argv[0] basename normalization deleted). Both findings above were found by a real-process probe and are invisible to the fixture-only tests: fixtures encode exactly the token shape the new code expects, which is the fakes-lie pattern patterns.md records for CAM-55 and which the project's own test-quality rule ('hit real I/O at wire boundaries') forbids. Add one integration test that spawns a real process with the interpreted self-spawn argv shape in a temp cwd and asserts discovery finds it (and asserts an adjacent non-sidecar command in the same cwd is NOT matched)."
    },
    {
      "id": "US-R1-004",
      "title": "WARNING: The new bullet is prefixed `[resolved 2026-08-02]`, which does not mat…",
      "priority": 13,
      "passes": true,
      "description": "WARNING [scripts/cam/patterns.md:1154]: The new bullet is prefixed `[resolved 2026-08-02]`, which does not match the archiver's marker regex RESOLVED_MARKER_RE = /\\[resolved \\d{4}-\\d{2}\\]/ (src/commands/patterns-archive.ts:44) nor the documented `[resolved YYYY-MM]` form (scripts/cam/CLAUDE.md:91); verified with the real regex (full-date form: false, YYYY-MM form: true). Every one of the 11 existing markers in patterns.archive.md uses YYYY-MM. Effect: `cam patterns archive` will silently never archive this bullet. Separately, reconsider marking it resolved at all: unlike the GOTCHA it closes, the bullet documents the LIVE isSidecarArgv shape contract, so archiving it would remove active knowledge.",
      "notes": "WARNING [scripts/cam/patterns.md:1154]: The new bullet is prefixed `[resolved 2026-08-02]`, which does not match the archiver's marker regex RESOLVED_MARKER_RE = /\\[resolved \\d{4}-\\d{2}\\]/ (src/commands/patterns-archive.ts:44) nor the documented `[resolved YYYY-MM]` form (scripts/cam/CLAUDE.md:91); verified with the real regex (full-date form: false, YYYY-MM form: true). Every one of the 11 existing markers in patterns.archive.md uses YYYY-MM. Effect: `cam patterns archive` will silently never archive this bullet. Separately, reconsider marking it resolved at all: unlike the GOTCHA it closes, the bullet documents the LIVE isSidecarArgv shape contract, so archiving it would remove active knowledge."
    },
    {
      "id": "US-001",
      "title": "Drop the literal binary-name fallback from the self-invoke helper",
      "description": "As the loop driver, I need resolveSelfInvokeArgv to never emit a hardcoded product name, so that every self-spawn built on it resolves to the exact executable currently running instead of whatever the PATH happens to hold.",
      "acceptanceCriteria": [
        "resolveSelfInvokeArgv carries no literal binary name, in the returned argv or in its docstring. [oracle: bash -c '! grep -qE \"argv1 \\?\\? .cam.\" src/util/self-invoke.ts']",
        "The degenerate interpreted-mode case (argv1 undefined) resolves to an execPath-only argv, the helper docstring states that shape, and the existing unit case for the old PATH-lookup fallback is rewritten to assert it. [oracle: bash -c '! grep -q \"falls back to the bare\" test/util/self-invoke.test.ts']",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test), including the two existing production call sites of the helper (src/retry/launcher.ts and src/commands/run.ts via resolveOrchResolverCmd). [oracle: bun test]",
        "The quality spine passes. [oracle: bun run check:all]",
        "PIN DE INVARIANCIA: este ciclo nao invade o empacotamento. O nome do pacote continua identico ao da main, comparando derivado ao vivo dos dois lados, e nenhum instalador aparece. [oracle: named-command bash -c 'test \"$(jq -r .name package.json)\" = \"$(git show main:package.json | jq -r .name)\" && ! test -f install.sh']",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, derivadas ao vivo e nao congeladas. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro, porque a regra frozen-comparand do lint exige o token de derivacao na MESMA clausula e um test \"$b\" -gt 0 dispara mesmo com $b derivado ao vivo (over-fire rastreado no CAM-438). [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done']"
      ],
      "priority": 14,
      "passes": true,
      "requires": null,
      "notes": "Files: src/util/self-invoke.ts (the literal lives at line 41 and is echoed in the docstring at line 34), test/util/self-invoke.test.ts (the 'undefined argv1' case at lines 27-30 asserts the old shape and will fail until rewritten). Callers spread the returned array, so a shorter argv is safe: src/retry/launcher.ts:242 and src/commands/run.ts:284 (resolveOrchResolverCmd). Do NOT add a new fallback name of any kind; execPath is always defined, so the degenerate branch is the interpreted-run-without-script case and an execPath-only argv is the honest answer. This story is the foundation the three following ones build on. Bun docs (fetched this cycle): Bun.spawn's cmd[0] may be an absolute executable path, so no PATH-resolvable name is needed anywhere. Keep the pins green: this story must not touch package.json and must not rename CAM_, .cam-, the tmux -L socket, or any /cam- slash command."
    },
    {
      "id": "US-002",
      "title": "Respawn the loop's own children (dashboard, sidecar, watchers) by execPath",
      "description": "As the running loop, I need my five self-respawn sites to launch the exact binary that is running me, so that a long-lived session never respawns a stale PATH binary whose code disagrees with the branch that spawned it.",
      "acceptanceCriteria": [
        "No background self-spawn in the loop host or the liveness watcher names the binary literally. [oracle: bash -c '! grep -qE \"Bun\\.spawn\\(..cam.\" src/commands/run.ts src/commands/sidecar-liveness-watch.ts']",
        "The dashboard pane command assembled for tmux respawn-pane no longer carries the literal name either. [oracle: bash -c '! grep -qE \".cam., ?.dashboard.\" src/commands/run.ts']",
        "A NEW argv-shape test, red against main before the change, asserts that each of the five self-respawn argvs (dashboard pane command, sidecar, orch-recycle-watch, sidecar-liveness-watch in run.ts, and the sidecar respawn in sidecar-liveness-watch.ts) is built from an INJECTED execPath/argv1 pair, starts with that execPath, and contains no product-name literal; it must exercise an execPath whose basename differs from the current binary's. [oracle: bash -c 'test -f test/commands/run-self-spawn-argv.test.ts && bun test test/commands/run-self-spawn-argv.test.ts']",
        "A NEW real-tmux integration test, red against main before the change, proves that a multi-element execPath-derived pane command (the interpreted two-element case, runtime plus script) actually starts inside a respawned pane rather than being mangled by tmux word-splitting; it skips cleanly when tmux is absent. [oracle: bash -c 'test -f test/integration/dashboard-pane-selfspawn.test.ts && bun test test/integration/dashboard-pane-selfspawn.test.ts']",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "The quality spine passes. [oracle: bun run check:all]",
        "PIN DE INVARIANCIA: este ciclo nao invade o empacotamento. O nome do pacote continua identico ao da main, comparando derivado ao vivo dos dois lados, e nenhum instalador aparece. [oracle: named-command bash -c 'test \"$(jq -r .name package.json)\" = \"$(git show main:package.json | jq -r .name)\" && ! test -f install.sh']",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, derivadas ao vivo e nao congeladas. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro, porque a regra frozen-comparand do lint exige o token de derivacao na MESMA clausula e um test \"$b\" -gt 0 dispara mesmo com $b derivado ao vivo (over-fire rastreado no CAM-438). [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done']"
      ],
      "priority": 15,
      "passes": true,
      "requires": null,
      "notes": "Files: src/commands/run.ts (dashboard respawn-pane argv at line 740, spawnSidecarDefault at 822, spawnWatcherDefault at 853, spawnSidecarLivenessWatchDefault at 895), src/commands/sidecar-liveness-watch.ts (makeSpawnSidecarFn at line 92), plus the two new test files. Reuse resolveSelfInvokeArgv exactly as src/retry/launcher.ts:242 already does (spread it, then append the subcommand); do not invent a second detection mechanism. GOTCHA (from the issue): line 740 is NOT a Bun.spawn, it is a tmux respawn-pane whose pane command is assembled as argv words, so the edit point is the tmux argv assembly; run.ts already has a quoting helper `q` used by buildOrchestratorPaneCommand for the bash -c wrapper, and the orchestrator pane at line 729 shows the established shape. Expose the argv assembly as pure, injectable builders so the new test asserts real argv instead of 'the mock was called' (curated invariant: no tautological mock-call assertions). patterns.md, CAM-55 bullet: unit fakes that return the output the code expects cannot catch tmux-real behavior, which is why criterion 4 requires a real-tmux case; follow the skip idiom of test/integration/tmux-introspect.test.ts. Note that after this change a dev-mode (interpreted) sidecar advertises `<bun> <script> sidecar` in ps instead of `cam sidecar`, which is precisely the discovery breakage US-004 must absorb."
    },
    {
      "id": "US-003",
      "title": "Autostart the hub from the thin proxies by execPath, and clear the residual name literals",
      "description": "As a thin-proxy subcommand, I need to bootstrap the hub using the executable that is running me, so that `cam plan`/`next`/`issue`/`spec`/`review`/`ship` never boot a different (older) hub binary than the one the operator just invoked.",
      "acceptanceCriteria": [
        "No file under src/commands bootstraps the hub by literal binary name, in code or in a docstring. [oracle: bash -c '! grep -rqE \"spawnSync\\(.cam.,\" src/commands']",
        "No auto-spawn site anywhere in src or index.ts invokes the binary by literal name (the full sweep, including the remaining comment in src/commands/sidecar.ts). [oracle: bash -c '! grep -rqE \"spawnSync\\(.cam.,|Bun\\.spawn\\(..cam.,|.cam., ?.dashboard.\" src index.ts']",
        "A NEW test, red against main before the change, asserts the hub-autostart argv is built from an INJECTED execPath/argv1 pair (basename deliberately different from the current binary's), starts with that execPath, ends with the `run --no-attach` subcommand, and contains no product-name literal. [oracle: bash -c 'test -f test/commands/hub-autostart-argv.test.ts && bun test test/commands/hub-autostart-argv.test.ts']",
        "The six proxies keep their existing injectable bootstrap seam and their current bootstrap semantics (same exit-status handling, same cwd, stdio ignored), so their existing tests keep passing unchanged. [oracle: bun test]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "The quality spine passes. [oracle: bun run check:all]",
        "PIN DE INVARIANCIA: este ciclo nao invade o empacotamento. O nome do pacote continua identico ao da main, comparando derivado ao vivo dos dois lados, e nenhum instalador aparece. [oracle: named-command bash -c 'test \"$(jq -r .name package.json)\" = \"$(git show main:package.json | jq -r .name)\" && ! test -f install.sh']",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, derivadas ao vivo e nao congeladas. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro, porque a regra frozen-comparand do lint exige o token de derivacao na MESMA clausula e um test \"$b\" -gt 0 dispara mesmo com $b derivado ao vivo (over-fire rastreado no CAM-438). [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done']"
      ],
      "priority": 16,
      "passes": true,
      "requires": null,
      "notes": "Files: src/commands/plan.ts:94, next.ts:270, issue.ts:102, spec.ts:136, review.ts:97, ship.ts:99 (each has an identical `doBootstrap` with a `spawnSync('cam', ['run','--no-attach'])` default plus a docstring one-liner naming it, at plan.ts:80ish, next.ts:221, issue.ts:60, spec.ts:94, review.ts:66, ship.ts:68), src/commands/sidecar.ts:8 (comment), plus the new test file. The six copies are byte-identical, so route them through ONE shared argv builder (jscpd already runs in check:all and duplicated closures have been consolidated before: see the sibling-pattern bullet in patterns.md) rather than editing six literals in place. Keep the `spawnSync` exit-status guard: `(result.status ?? 1) === 0`, spawnSync never throws on non-zero (patterns.md spawnSync exit-status guard bullet). Update the docstrings in the same edit: criterion 2 is the full 17-line sweep and stays red while any docstring still shows the literal form. Do not change the thin-proxy dispatch order (liveness/bootstrap preamble then paneCountMutex, patterns.md mutex bullet). An oracle exercising CLI behavior must use `bun index.ts <sub>`, never the installed binary."
    },
    {
      "id": "US-004",
      "title": "Make sidecar process discovery in cam stop name-agnostic",
      "description": "As the operator running `cam stop`, I need sidecar discovery to recognize a live sidecar regardless of the name (or runtime) it was launched under, so that stopping the loop keeps working once self-spawn resolves by execPath and once the binary is eventually installed under a different name.",
      "acceptanceCriteria": [
        "Process discovery no longer compares argv against a single binary-name literal. [oracle: bash -c '! grep -qE \"argv.0. === .cam.|cmdBase !== .cam.\" src/commands/stop.ts']",
        "A NEW test file, red against main before the change (it must fail against the current stop.ts, not merely pass against the new one), proves discovery still finds a live sidecar while exercising a binary name different from the current one, and covers all four cases: a compiled sidecar whose argv[0] basename differs from the running binary MATCHES; an interpreted sidecar advertised as `<runtime> <script> sidecar` MATCHES; a process in a different cwd does NOT match; a process in the project cwd that is not running the sidecar subcommand does NOT match. [oracle: bash -c 'test -f test/commands/stop-name-agnostic.test.ts && bun test test/commands/stop-name-agnostic.test.ts']",
        "Discovery scoping is not widened: exact cwd equality against the project cwd is still required (no prefix or substring match). [oracle: bash -c 'grep -q \"record.cwd === projectCwd\" src/commands/stop.ts']",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test), including the existing stop tests and the sidecar-pid path. [oracle: bun test]",
        "The quality spine passes. [oracle: bun run check:all]",
        "PIN DE INVARIANCIA: este ciclo nao invade o empacotamento. O nome do pacote continua identico ao da main, comparando derivado ao vivo dos dois lados, e nenhum instalador aparece. [oracle: named-command bash -c 'test \"$(jq -r .name package.json)\" = \"$(git show main:package.json | jq -r .name)\" && ! test -f install.sh']",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, derivadas ao vivo e nao congeladas. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro, porque a regra frozen-comparand do lint exige o token de derivacao na MESMA clausula e um test \"$b\" -gt 0 dispara mesmo com $b derivado ao vivo (over-fire rastreado no CAM-438). [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done']"
      ],
      "priority": 17,
      "passes": true,
      "requires": null,
      "notes": "Files: src/commands/stop.ts (matchesSidecarForProject at line ~292 with `record.argv[0] === 'cam'`, and the `ps -eo pid,args` quick filter at line ~328 with `cmdBase !== 'cam'`), plus the new test/commands/stop-name-agnostic.test.ts. GOTCHA (from the issue): discovery inspects OTHER processes' argv, so it cannot just use its own execPath: a legitimate sidecar may be the ALREADY-INSTALLED binary under today's name, or (after US-002) an interpreted `<bun> <script> sidecar`, so the fixed argv[1] === 'sidecar' index assumption breaks too. Recognize the sidecar by its subcommand position in the argv rather than by a name comparison, while keeping the cwd-equality scoping that stops one project killing another's sidecar. The `ps` quick filter exists only to avoid an lsof call per process, so it may relax but must not become unbounded (do not lsof every process on the machine). Existing DI seams to reuse: listProcessesFn / ProcessRecord in stop.ts and the sidecarPidReader family (patterns.md, sidecar PID persistence bullet). Existing coverage lives in test/stop.test.ts; the new file is additive and must be swept red against main before it is trusted."
    }
  ],
  "review": {
    "roundsCompleted": 8,
    "maxRounds": 9,
    "lastVerdict": "FIXES_PENDING:3",
    "findings": [
      {
        "severity": "CRITICAL",
        "file": "test/integration/dashboard-pane-selfspawn.test.ts",
        "line": 35,
        "text": "Layer B behavioral-gate FAIL (hard constraint): US-002 acceptance criterion 4's oracle `bun test test/integration/dashboard-pane-selfspawn.test.ts` exited non-zero on 3 of the reviewer's 34 independent re-runs. The test is NOT hermetic: it pins a fixed tmux socket name (TEST_SOCK = 'cam-it-selfspawn', line 35) and runs `tmux kill-server` on it in beforeEach (line 54) and afterEach (line 62), so any second process running the same file destroys the first instance's server, the respawned pane child dies before writing marker.json, and the wait at line 101 expires at exactly 10s. Evidence (artifact section 4): idle 10/10 pass (1.5-1.7s); heavy CPU load with no second tmux client 12/12 pass at load average 15-27 (2.4-2.6s); with a concurrent `bun test` 1/12 FAIL at 10.07s; with two simultaneous copies 2/6 FAIL. This also FALSIFIES the round-7 finding's stated mechanism (host load consuming the 10s budget): raising the marker-wait budget would not fix it, because the pane's server is gone, not slow. This matters live for this project: `bun test` is run concurrently in the same repo by the worker quality gate, `cam next` preflight and the reviewer's own re-run. Mechanical 1-file fix: derive the socket name per process (e.g. `cam-it-selfspawn-${process.pid}`) so kill-server can never cross instances. The 15 pre-existing integration files sharing the fixed-socket convention are out of scope for this finding; only the file this PR introduces is in scope."
      },
      {
        "severity": "WARNING",
        "file": "src/commands/sidecar-liveness-watch.ts",
        "line": 149,
        "text": "The US-R6-002 fd-leak fix (`closeSync(logFd)` after Bun.spawn) ships with no falsifying test, so the suite cannot detect its removal. Verified independently: `grep -rn closeSync test/` returns no matches and `grep -rn lsof test/` (excluding test-deps) returns only comments and the skip gate, so nothing anywhere asserts the PARENT process's descriptor table. The only test that constructs the production closure (test/integration/stop-defaultlistprocesses-real.test.ts:204-219) asserts child discoverability and SIGTERM delivery, which hold identically with or without the parent-side close. This is the same falsifiability gap US-R6-003 was filed to close for US-R4-001, recurring one story later in the same file. The seam already exists: `makeSpawnSidecarFn(cwd, argvOverride)` is exported, so a test can invoke it N times and assert via `lsof -p <own pid>` that the parent holds no accumulating fd on .claude/cam-supervisor.log."
      },
      {
        "severity": "WARNING",
        "file": "scripts/cam/patterns.md",
        "line": 1158,
        "text": "patterns.md (an agent-read artifact injected into future workers' context) states the isSidecarArgv contract twice in a form that contradicts the shipped code, and one of the two references now dangles. Line 1154 says the predicate matches 'last element sidecar, length 2 or 3'; line 1158 says it 'now checks only argv.length >= 2 && argv[argv.length-1] === sidecar && argv[0].startsWith(\"/\")'. The shipped predicate (src/commands/stop.ts:342-345) is `argv.length >= 2 && last === 'sidecar'` with no token-count and no argv[0] requirement. Line 1158 additionally points at 'the \"[resolved 2026-08-02]\" US-004 bullet above', a marker US-R1-004 removed in this same branch, so the pointer no longer resolves. A future worker greps 'isSidecarArgv' and hits the false headline first; reconstructing the real 2-line predicate now requires reading four bullets in order. Mechanical 1-file fix: state the current contract once and drop or repoint the dangling reference."
      },
      {
        "severity": "SUGGESTION",
        "file": "src/util/hub-bootstrap.ts",
        "line": 41,
        "text": "Carried unaddressed from rounds 1-7: bootstrapHub has no injectable spawnSync seam, so its exit-status mapping ((result.status ?? 1) === 0, including the signal-killed null case) and its cwd/stdio wiring have zero direct coverage, and the `if (cmd === undefined) return false` guard at line 41 is an unreachable branch that exists only to satisfy noUncheckedIndexedAccess. Injecting a spawnSync fn (as src/commands/stop.ts already does with spawnSyncFn) makes the mapping testable and removes the dead branch. Cheaper now than after the six proxies depend on it."
      },
      {
        "severity": "SUGGESTION",
        "file": "src/commands/stop.ts",
        "line": 286,
        "text": "isSidecarArgv carries a ~57-line docstring (lines 286-341) over a 3-line body, most of it a chronological narrative of the argv[0] anchor being added (US-R1-001) and then dropped (US-R2-002). The same narrative already lives verbatim in scripts/cam/patterns.md and in the file-size-budget _ref, and it accounts for a large share of the stop.ts 689 -> 799 ceiling raise. A short statement of the current contract plus a pointer would preserve the knowledge and recover most of the raise. The raise itself is documented in the _ref tracker, so this is not blocking."
      },
      {
        "severity": "SUGGESTION",
        "file": "scripts/cam/patterns.md",
        "text": "9 of the added lines use the em-dash character, which the project convention bans in persisted markdown. Unchanged in kind from main (43 such lines already there) and patterns.md is primarily agent-consumed, but the new bullets extend the drift rather than hold the line."
      },
      {
        "severity": "SUGGESTION",
        "file": "test/integration/stop-defaultlistprocesses-real.test.ts",
        "line": 94,
        "text": "The log fd opened at line 94 (`openSync(join(claudeDir, 'cam-supervisor.log'), 'a')`) is never closed, so the test process leaks one descriptor per run and the finally block rmSync's the directory while the fd is still open. A closeSync in the finally block would match the fd hygiene the same test exists to defend."
      }
    ]
  }
}
