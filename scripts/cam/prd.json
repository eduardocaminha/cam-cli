{
  "project": "cam-cli",
  "branchName": "cam/issue-510",
  "description": "Close the five product-side temp-leak sites (cam-ship-pr, cam-cli-vendor, cam-codex instructions, cam-config.log, cam-cli-task-prompts-<pid>) behind fixed parent directories with durable per-site tests, and prove steady state across two consecutive suite runs.",
  "issueNumber": 510,
  "type": "fix",
  "relatedDocs": [],
  "officialDocsConsulted": [
    {
      "lib": "Bun",
      "channel": "WebFetch",
      "url": "https://bun.sh/docs/runtime/nodejs-apis",
      "version": "current",
      "fetchedAt": "2026-08-06T12:00:00Z",
      "summary": "Bun documents node:fs as fully implemented (92% of Node's test suite passes) and node:os as fully implemented (100% passes), so writeFileSync's mode option (0o600), readdirSync/rmSync-based reaping, and os.tmpdir() all behave as in Node. This confirms the plan's two load-bearing runtime assumptions: a 0600-mode synchronous write is supported, and os.tmpdir() is a plain node:os call whose value follows TMPDIR at call time (the in-process redirect the durable tests rely on).",
      "status": "ok"
    }
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Bound the ship-PR body tempfile under one fixed reused parent",
      "description": "As a cam operator, I need `cam ship`'s PR-body tempfile to live under a single fixed, reused parent directory instead of a fresh randomized one per invocation, so that shipping stops adding a permanent cam-ship-pr-XXXXXX entry to my temp root on every run.",
      "acceptanceCriteria": [
        "AC1 (change detector): src/commands/sidecar.ts no longer creates a randomized directory per invocation; the production writeTempFile writes under a single fixed, reused parent and no mkdtempSync rooted in tmpdir() remains in the file. RED-ON-MAIN: swept 2026-08-06, exits 1 because src/commands/sidecar.ts:1201 still matches mkdtempSync(join(tmpdir(), 'cam-ship-pr-')). [oracle: file-assert bash -c '! grep -qE \"mkdtempSync\\(join\\(tmpdir\\(\\)\" src/commands/sidecar.ts']",
        "AC2 (behavioral proof): test/release/ship-pr-tempfile.test.ts exercises the REAL production writeTempFile twice with process.env.TMPDIR redirected at a repo-local scratch root (restored in finally) and asserts that (a) no per-invocation cam-ship-pr-* directory survives, (b) the root gains at most ONE top-level entry, the fixed parent, and (c) the body file existed and was readable at the instant it was handed to the spawn seam, for both the PR-body path and the artifact-comment path, proving the reap never deletes a live in-flight file. RED-ON-MAIN: swept 2026-08-06, exits 1 because the test file does not exist. [oracle: named-command bun test test/release/ship-pr-tempfile.test.ts]",
        "The file-size ratchet stays green after the sidecar.ts edit, with the raised ceiling in scripts/file-size-budget.json carrying a CAM-510 tracker reference in its staged diff (rule 2 of the gate). [oracle: named-command bun scripts/check-file-sizes.ts]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 1,
      "passes": true,
      "requires": null,
      "notes": "Site 1 of 5. Production site: src/commands/sidecar.ts:1201, inside the ship-pr production-deps factory (writeTempFile closure next to spawnFn/readReviewArtifact). Shape to adopt: one fixed top-level parent (e.g. join(tmpdir(), 'cam-ship-pr')) with the variable segment (pid) NESTED inside it, mirroring src/supervisor/task-prompt-file.ts:26-32; GOTCHA 2 of the issue is the invariant that no product path may ever create a top-level entry with a per-run unique name. GOTCHA 8 (concurrency): a parent reaped before every write can delete a second live cam process's file, so nest by pid and prune only old pid subdirectories; AC2(c) is exactly the regression guard for reaping a live file. GOTCHA 5: any reap/prune must readdir ONLY its own fixed parent, never the temp root (enumerating a bloated root blew 30-120s timeouts). Test file: test/release/ship-pr-tempfile.test.ts MUST obtain its scratch root from createTestTmpdir() in test/helpers/test-tmpdir.ts and point process.env.TMPDIR at it (restore in finally); it must NOT import tmpdir from node:os, so it needs no allowlist entry in scripts/check-test-tmpdir.ts (GOTCHA 13/14: the allowlist ends this issue with exactly ONE entry, the inherited check-agent-frontmatter-standalone one). Measured 2026-08-06: bun's os.tmpdir() re-reads TMPDIR on every call, so a repo-local redirect target works. GOTCHA 6 (budget): src/commands/sidecar.ts is 3721 lines against a 3722 ceiling, so this diff forces raising scripts/file-size-budget.json; the staged budget diff needs a CAM-510 tracker ref plus the usual _ref prose entry. GOTCHA 4 (CAM-507): never use rm -rf in any oracle, script, or composed command, in production code or in a test's cleanup shell-out; the harness intercepts it with an interactive dialog that wedges the pane until the 30-minute timeout. patterns.md context: grep for CAM-508 bullets (test/helpers/test-tmpdir.ts contract, lines ~1239 and ~1247)."
    },
    {
      "id": "US-002",
      "title": "Nest the vendor cache versions under one fixed parent and correct the false reboot comment",
      "description": "As a cam operator, I need the embedded-vendor extraction cache to nest every version under a single fixed parent instead of creating one top-level cam-cli-vendor-<version> entry per installed version, so that upgrading cam stops leaving a permanent directory per version behind on the false premise that reboot wipes them.",
      "acceptanceCriteria": [
        "AC3 (change detector): src/vendor/embedded.ts no longer creates a top-level entry per version (versions are nested under a single fixed parent) and the :63-66 comment claiming the cache is wiped on reboot, the false premise that produced the measured survivors, is rewritten in the same diff. RED-ON-MAIN: swept 2026-08-06, exits 1, and each half exits 1 in isolation (line 73 and the :63-66 comment). [oracle: file-assert bash -c 'set -e; ! grep -q cam-cli-vendor- src/vendor/embedded.ts; ! grep -q \"wiped on reboot\" src/vendor/embedded.ts']",
        "AC4 (behavioral proof): test/vendor/embedded-cache-bounded.test.ts, with TMPDIR redirected in-process at a repo-local scratch root (restored in finally), asserts that after materializeEmbedded no flat cam-cli-vendor-<version> entry exists at the root, that a stale version directory planted INSIDE the fixed parent does not survive, that the materialized file for the current version is still byte-correct, and that the CAM_VENDOR_CACHE_DIR override still beats the default parent. The pruning reads only the fixed parent, never the temp root. RED-ON-MAIN: swept 2026-08-06, exits 1 because the test file does not exist. [oracle: named-command bun test test/vendor/embedded-cache-bounded.test.ts]",
        "Vendor drift check passes (bun run embed-vendor:check). [oracle: named-command bun run embed-vendor:check]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 2,
      "passes": true,
      "requires": null,
      "notes": "Site 2 of 5. Production site: src/vendor/embedded.ts:73 (cacheDir()) plus the comment block at :63-66 that asserts 'the cache is wiped on reboot' (false: 7 survivors measured in this tree, 281 in the original survey). Shape: join(tmpdir(), 'cam-cli-vendor') as the single fixed top-level parent, CAM_VERSION as a NESTED segment under it; keep the CAM_VENDOR_CACHE_DIR override precedence exactly as-is (AC4 pins it). GOTCHA 8 (concurrency, symmetric risk here): pruning sibling versions can delete the extraction an older still-running cam is using, so either make materializeEmbedded tolerate a vanished file and re-extract, or limit pruning to entries older than an mtime threshold. GOTCHA 5: prune by reading only the fixed parent, never the temp root. src/vendor/embedded.ts is hand-written (the codegen writes src/vendor/_generated.ts, NOT embedded.ts), so editing it is safe; embed-vendor:check is included as a cheap regression guard because the file sits under src/vendor/. GOTCHA 6 (budget): src/vendor/embedded.ts is 110 lines against a 111 ceiling, so raising scripts/file-size-budget.json with a CAM-510 tracker ref in the staged diff is likely (bun scripts/check-file-sizes.ts is part of check:all and will say so). Test file: test/vendor/embedded-cache-bounded.test.ts must use createTestTmpdir() from test/helpers/test-tmpdir.ts as the TMPDIR redirect target and must NOT import tmpdir from node:os (GOTCHA 14: no new allowlist entries in scripts/check-test-tmpdir.ts). Do not touch test/vendor/check-agent-frontmatter-standalone.test.ts or its two 20_000 budgets (GOTCHA 13: its real-OS-temp rooting plus --no-install is a load-bearing zero-dependency invariant; US-006's fixation guard pins the budgets). GOTCHA 4 (CAM-507): no rm -rf anywhere."
    },
    {
      "id": "US-003",
      "title": "Bound and mode-restrict the codex instructions file across the whole dispatch lifecycle",
      "description": "As a cam operator, I need the codex instructions file to live in a dedicated bounded directory, be written 0600 instead of world-readable, and be removed at the dispatch terminal, so that the largest leak site (45,580 files, 1.87 GB, each able to carry up to 200 KB of my git diff at mode 0644) stops accumulating and stops exposing my diff to every local user.",
      "acceptanceCriteria": [
        "AC5 (change detector): src/supervisor/backend-adapter.ts writes the codex instructions under a dedicated, bounded directory, reaping that directory's siblings before each write (mirroring src/supervisor/task-prompt-file.ts:26-32, no second lifecycle invented), and writes with mode 0o600 instead of the current world-readable 0644. RED-ON-MAIN: swept 2026-08-06, exits 1 because the file today contains none of readdirSync, rmSync, or 0o600. [oracle: file-assert bash -c 'set -e; grep -q readdirSync src/supervisor/backend-adapter.ts; grep -qE 0o600 src/supervisor/backend-adapter.ts']",
        "AC6 (behavioral proof at the dispatch TERMINAL, not merely reap-before-next-write): test/supervisor/codex-instructions-reap.test.ts drives a complete dispatch and asserts against the real filesystem that AFTER the dispatch terminal ZERO instructions files for that agent survive, that while alive the file's mode is 0600, and that the temp root gains at most ONE top-level entry (the fixed parent, with agent/pid/uuid as nested segments). The removeTaskPromptFile half of the task-prompt-file pattern is mandatory: with reap-before-write alone the LAST dispatch always leaves a file of up to 200 KB of the user's diff behind. RED-ON-MAIN: swept 2026-08-06, exits 1 because the test file does not exist. [oracle: named-command bun test test/supervisor/codex-instructions-reap.test.ts]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 3,
      "passes": false,
      "requires": null,
      "notes": "Site 3 of 5, the largest by volume. Production site: src/supervisor/backend-adapter.ts:415 (writeCodexInstructionsFile), today join('/tmp', `cam-codex-${agentName}-${randomUUID()}.md`) with a default-mode writeFileSync. GOTCHA 3: prefer deriving the parent from tmpdir() instead of the literal '/tmp' so a test can redirect it via process.env.TMPDIR in-process; either root still satisfies US-006's AC12, which counts $TMPDIR and /tmp together. GOTCHA 7: the file must stay OUTSIDE the repo tree (it is deliberately not under .claude/ so the plan preflight's clean-tree guard never sees it, ADR-0014). GOTCHA 7 (seam): renderActor returns only the argv string, so the caller does not know the path; either expose the path from the adapter or have the reaper work by agent prefix inside the dedicated directory (mirroring removeTaskPromptFile's shape). GOTCHA 8: concurrent implementer and reviewer dispatches must not clobber each other, so scope reaping to the agent (and pid) segment rather than blanket-reaping the parent. GOTCHA 2: agent, pid and uuid all become NESTED segments; nothing unique may appear in a top-level name. GOTCHA 5: reap by reading only the dedicated directory. The reviewer branch appends up to REVIEWER_DIFF_MAX_CHARS (200_000) of the user's git diff to this body (CAM-354), which is why 0600 is a real confidentiality fix and not cosmetics. src/supervisor/backend-adapter.ts (638 lines) is NOT listed in scripts/file-size-budget.json and needs no budget entry. Test file: test/supervisor/codex-instructions-reap.test.ts must use createTestTmpdir() from test/helpers/test-tmpdir.ts as the TMPDIR redirect target and must NOT import tmpdir from node:os (GOTCHA 14: no new allowlist entries). Assert the mode with statSync(...).mode & 0o777 against a real file, not a mock. GOTCHA 4 (CAM-507): no rm -rf anywhere. patterns.md: grep CAM-354 (reviewer diff injection into the instructions body) and CAM-433 (task-prompt-file lifecycle)."
    },
    {
      "id": "US-004",
      "title": "Namespace and cap the setup config log instead of a fixed shared /tmp path",
      "description": "As a cam operator, I need `cam setup`'s config-pane log to derive from a fixed parent with the pid in its name and to cap its own byte size in the generated shell, so that an unbounded, unrotated, non-namespaced /tmp/cam-config.log stops being shared across users and runs.",
      "acceptanceCriteria": [
        "AC7 (change detector): src/commands/setup.ts no longer produces the fixed shared path /tmp/cam-config.log; the generated bash script derives the log from a fixed parent with the pid in the name and applies a byte ceiling in the shell itself (the log is written by tmux pipe-pane inside a generated bash string, so the cap must be a shell construction, not TypeScript). RED-ON-MAIN: swept 2026-08-06, exits 1, and both halves exit 1 in isolation (line 492 carries the literal; none of the three cap constructions exists in the file). [oracle: file-assert bash -c 'set -e; ! grep -q /tmp/cam-config.log src/commands/setup.ts; grep -qE \"tail -c|head -c|truncate\" src/commands/setup.ts']",
        "AC8 (behavioral proof): test/setup-config-log.test.ts runs the generated cap snippet under REAL bash with input larger than the ceiling and asserts that the resulting file does not exceed the ceiling, that two distinct pids produce two distinct paths, and that the log is removed on menu exit with no residue. The test writes into a repo-local scratch root, never into the real /tmp. RED-ON-MAIN: swept 2026-08-06, exits 1 because the test file does not exist. [oracle: named-command bun test test/setup-config-log.test.ts]",
        "The file-size ratchet stays green after the setup.ts edit, with any raised ceiling in scripts/file-size-budget.json carrying a CAM-510 tracker reference in its staged diff. [oracle: named-command bun scripts/check-file-sizes.ts]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 4,
      "passes": false,
      "requires": null,
      "notes": "Site 4 of 5. Production site: src/commands/setup.ts:492, inside the generated bash menu string: the v|V branch does `tmux pipe-pane ... 'cat >> /tmp/cam-config.log'; tail -f /tmp/cam-config.log`. Both occurrences on that line must move to a pid-namespaced path under one fixed parent (GOTCHA 2: the pid is a nested segment or a filename segment under a fixed parent directory, never a new top-level unique entry), and the append must be bounded in-shell. Note the file is a bash HEREDOC-style template inside TypeScript, so every dollar sign and brace already carries escaping; follow the existing escaping style of the surrounding lines exactly. The cap has to survive `cat >>` appends (e.g. a bounded rotate step using tail -c on the file, or a head -c ceiling on the piped stream), which is exactly what AC8 executes under real bash. Removal on menu exit (the q|Q branch and any other exit path) is part of AC8. GOTCHA 6 (budget): src/commands/setup.ts is 884 lines against an 885 ceiling, so raising scripts/file-size-budget.json with a CAM-510 tracker ref in the staged diff is expected. Test file: test/setup-config-log.test.ts must root its scratch directory in createTestTmpdir() from test/helpers/test-tmpdir.ts and must NOT import tmpdir from node:os (GOTCHA 14: no new allowlist entries). Run the snippet through a real bash subprocess (Bun.spawnSync/spawn), not a simulated string check, per the curated test-quality invariant on real I/O at wire boundaries. GOTCHA 4 (CAM-507): no rm -rf in the generated shell, the test, or any oracle."
    },
    {
      "id": "US-005",
      "title": "Nest the claudeDir tmpdir fallback pid under one fixed parent across all three call sites",
      "description": "As a cam operator, I need the tmpdir-rooted claudeDir fallback to nest the pid under a single fixed parent at all three call sites, so that the one site guaranteed to add exactly one new top-level entry per process stops doing so and steady state becomes reachable at all.",
      "acceptanceCriteria": [
        "AC9 (change detector): the tmpdir-rooted claudeDir fallback no longer creates a top-level entry per pid; src/supervisor/plan-runner.ts:771, src/supervisor/worker-dispatch.ts:37 and src/supervisor/backend-adapter.ts:242 all nest the pid under a single fixed parent, with bounded pruning of old pids that reads only that parent. Measured 2026-08-05: 250 cam-cli-task-prompts-<pid> directories present, exactly +1 per suite run, which alone would make US-006's equality impossible even with everything else fixed. RED-ON-MAIN: swept 2026-08-06, exits 1 because all three call sites match. [oracle: file-assert bash -c '! grep -rq cam-cli-task-prompts- src']",
        "AC10 (behavioral proof): test/supervisor/task-prompt-root-bounded.test.ts, with TMPDIR redirected in-process at a repo-local scratch root (restored in finally), triggers the fallback twice and asserts that the root gains exactly ONE top-level entry (the fixed parent) with the pid as a nested segment inside it, and that an old pid directory planted inside the parent is pruned. RED-ON-MAIN: swept 2026-08-06, exits 1 because the test file does not exist. [oracle: named-command bun test test/supervisor/task-prompt-root-bounded.test.ts]",
        "The file-size ratchet stays green after the plan-runner.ts edit, with any raised ceiling in scripts/file-size-budget.json carrying a CAM-510 tracker reference in its staged diff. [oracle: named-command bun scripts/check-file-sizes.ts]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 5,
      "passes": false,
      "requires": null,
      "notes": "Site 5 of 5, discovered during this issue's own measurement and not optional: because the directory name carries the pid, it alone guarantees +1 top-level entry per run and would keep US-006's AC12 red by construction. The three identical fallback expressions are src/supervisor/plan-runner.ts:771, src/supervisor/worker-dispatch.ts:37 and src/supervisor/backend-adapter.ts:242, each `claudeDir ?? join(tmpdir(), `cam-cli-task-prompts-${process.pid}`, '.claude')`. Prefer one shared exported helper (natural home: src/supervisor/task-prompt-file.ts, which already owns this lifecycle) that all three call, so the invariant cannot drift back apart; the injected claudeDir override must keep winning exactly as today. GOTCHA 2: the pid becomes a nested segment under a fixed parent (e.g. join(tmpdir(), 'cam-cli-task-prompts', String(process.pid), '.claude')). GOTCHA 8: prune old pid directories by mtime rather than blanket-reaping, since a second live cam process owns its own pid directory. GOTCHA 5: prune by reading only the fixed parent, never the temp root. Note that AC9's oracle is tree-wide over src/, so any leftover spelling anywhere in src/ keeps it red. GOTCHA 6 (budget): src/supervisor/plan-runner.ts is 2452 lines against a 2453 ceiling; src/supervisor/worker-dispatch.ts and src/supervisor/backend-adapter.ts are not listed and need no entry. Test file: test/supervisor/task-prompt-root-bounded.test.ts must use createTestTmpdir() from test/helpers/test-tmpdir.ts as the TMPDIR redirect target and must NOT import tmpdir from node:os (GOTCHA 14: no new allowlist entries). GOTCHA 4 (CAM-507): no rm -rf anywhere. patterns.md: grep CAM-433 (task-prompt transport) and CAM-508 (test scratch-root helper contract)."
    },
    {
      "id": "US-006",
      "title": "Prove steady state, keep the allowlist at one entry, and record the mechanism in patterns.md",
      "description": "As a cam operator, I need proof that two consecutive full suite runs leave the temp-entry count unchanged, with the tmpdir gate's allowlist still holding exactly the one inherited entry and the mechanism written down durably, so that the leak is demonstrably stopped rather than merely edited.",
      "acceptanceCriteria": [
        "AC11 (allowlist invariant, criterion INVERTED per GOTCHA 14): none of this issue's five new tests references os.tmpdir, so none of them carries an allowlist entry in scripts/check-test-tmpdir.ts, whose test-path entry count on this branch equals the count RE-DERIVED from main in the same clause (the single inherited test/vendor/check-agent-frontmatter-standalone.test.ts entry, GOTCHA 13), and the gate runs clean. The comparand is re-derived live via `git show main:scripts/check-test-tmpdir.ts` inside the same clause as the comparison, never frozen as a literal integer (patterns.md:932 derive-don't-freeze). RED-ON-MAIN: swept 2026-08-06, exits 1 because the five test files do not exist yet; the count halves are equal today (1 = 1), so the criterion goes green only once the tests exist AND no allowlist entry was added. [oracle: file-assert bash -c 'set -e; for f in test/release/ship-pr-tempfile.test.ts test/vendor/embedded-cache-bounded.test.ts test/supervisor/task-prompt-root-bounded.test.ts test/supervisor/codex-instructions-reap.test.ts test/setup-config-log.test.ts; do test -f \"$f\"; ! grep -q \"$f\" scripts/check-test-tmpdir.ts; done; test \"$(grep -cE \"path: .test/\" scripts/check-test-tmpdir.ts)\" -eq \"$(git show main:scripts/check-test-tmpdir.ts | grep -cE \"path: .test/\")\"; bun scripts/check-test-tmpdir.ts']",
        "AC13 (fixation guard, GREEN on main by construction, not a red-sweep claim): the two 20_000 budgets in test/vendor/check-agent-frontmatter-standalone.test.ts were not raised, because raising them is exactly the gate-gaming patterns.md:1094 forbids; the worktree count is compared against the count re-derived from main in the same clause, with no frozen literal anywhere. Verified GREEN on main 2026-08-06 (2 = 2). [oracle: file-assert bash -c 'set -e; a=$(git show main:test/vendor/check-agent-frontmatter-standalone.test.ts | grep -c 20_000); b=$(grep -c 20_000 test/vendor/check-agent-frontmatter-standalone.test.ts); test \"$a\" = \"$b\"']",
        "AC14 (durable record): the measured mechanism and the result of the two runs become a durable bullet in scripts/cam/patterns.md, in the per-story attribution format the repo uses. RED-ON-MAIN: swept 2026-08-06, exits 1 with zero matches. Do NOT weaken this to grep -q CAM-503 or '^- \\*\\*.*CAM-503': both are GREEN on main because scripts/cam/patterns.md:1221 already cites (CAM-503) in prose, and that tautological oracle is what failed an earlier round. [oracle: file-assert grep -qE '^- \\*\\*.*\\(US-[0-9R-]+, CAM-503, 2026-' scripts/cam/patterns.md]",
        "AC12 (the loop-closing criterion, STEADY STATE rather than absolute growth): warm up with one full suite run (so the fixed parent entries this issue creates already exist), count the ^cam- entries summing $TMPDIR and /tmp, run the suite again, count again, and require the two counts to be EQUAL with both runs reporting 0 fail. RED-ON-MAIN: swept 2026-08-05 by executing the oracle body literally, two consecutive full runs on clean main: a=96760, b=97032, delta +272 per run (+196 in $TMPDIR, +76 in /tmp), both runs 6023 pass / 0 fail in 93.53s and 95.98s, so the redness comes purely from the count inequality and not from any failing test. [oracle: file-assert bash -c 'set -e; T=\"${TMPDIR:-/tmp}\"; c(){ echo $(( $(ls \"$T\" | grep -c \"^cam-\" || true) + $(ls /tmp | grep -c \"^cam-\" || true) )); }; o1=$(bun test 2>&1 || true); a=$(c); o2=$(bun test 2>&1 || true); b=$(c); printf \"%s\" \"$o1\" | grep -q \" 0 fail\"; printf \"%s\" \"$o2\" | grep -q \" 0 fail\"; test \"$a\" -eq \"$b\"']",
        "AC15/AC17 (gates): the full quality spine is green, including the CAM-508 tmpdir gate running inside it. [oracle: named-command bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: named-command bun run typecheck]",
        "Tests pass (bun test). [oracle: named-command bun test]"
      ],
      "priority": 6,
      "passes": false,
      "requires": null,
      "notes": "Closing story: it edits scripts/cam/patterns.md and runs the proofs; it must NOT re-open the five production sites (US-001..US-005 own those). AC11 rewrite rationale (this is the auditor finding from the previous round): the spec's original oracle compared the allowlist entry count against the frozen literal 1, which the frozen-comparand lint rule blocks (patterns.md:932, src/supervisor/prd-oracle-lint.ts hasFrozenIntegerComparand) because a literal rots the moment a later commit edits scripts/check-test-tmpdir.ts on main; the comparand is now re-derived live with `git show main:scripts/check-test-tmpdir.ts | grep -cE ...` INSIDE the same clause as the test, which is the prescribed fix and preserves the exact semantics intended (the branch adds zero allowlist entries; main holds exactly the one inherited entry today, verified 1 = 1 on 2026-08-06). Same-clause placement matters: a derivation token in a different clause does not excuse the comparand (patterns.md:934). AC14 bullet: the attribution must literally read (US-006, CAM-503, 2026-08-06) for the oracle to match, per GOTCHA 9; cite CAM-510 in the bullet's prose body so traceability to this issue is not lost. GOTCHA 12: do not claim test/vendor/check-agent-frontmatter-standalone.test.ts fails today, it passes in the full suite and in isolation (434ms); the proof rests on count equality alone. GOTCHA 10 (measurement hygiene): the two runs cost about 190s combined; run from the repo root with no live cam worker pane writing temp in parallel, and take each count AFTER its run, never before the first. If a count command hangs, the environment is still bloated from before and needs the operator's find/xargs palliative first: that is an environment precondition, never a licence to weaken the criterion. GOTCHA 11: the repo-local scratch root, the helper, the test migration and the gate mechanism belong to CAM-508 and must not be re-asserted or modified here. GOTCHA 1: never rewrite AC12 as after <= before; the fixes deliberately create four permanent fixed-parent entries that do not exist today, so equality after a warm-up run is the correct and strictly stronger form. GOTCHA 4 (CAM-507): no rm -rf in any oracle or command. Because the five tests are repo-local by construction (US-001..US-005), AC12's equality holds structurally rather than depending on each test's own cleanup."
    }
  ]
}
