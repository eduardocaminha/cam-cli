{
  "project": "cam-cli",
  "branchName": "cam/issue-146",
  "description": "Rename the 'grill' surface vocabulary to 'spec'/'interview' across all cam-owned artifacts (enum value, skill directory, living docs), preserving the vendored Matt Pocock /grilling + /domain-modeling calls verbatim.",
  "issueNumber": 146,
  "type": "chore",
  "relatedDocs": [],
  "officialDocsConsulted": [],
  "userStories": [
    {
      "id": "US-001",
      "title": "Rename specSource enum value grill -> interview across schema + code + tests",
      "description": "As a maintainer, I need the specSource enum value 'grill' renamed to 'interview' everywhere it is declared (TS type, validators, JSON schema both copies, type literals) so that the persisted enum stops carrying vendored Matt Pocock vocabulary while the surface docs vocabulary becomes 'spec'.",
      "acceptanceCriteria": [
        "src/issues/types.ts specSource type union becomes 'interview' | 'derived' | 'operator' (no 'grill'). [oracle: ! grep -q grill src/issues/types.ts]",
        "src/issues/spec.ts: isValidSpecSourceEnum accepts 'interview' and rejects 'grill'; the absent-default resolves to 'interview'; the error string lists interview/derived/operator; NO legacy 'grill' accept branch is added; all grill mentions (incl doc-comments) are gone. [oracle: ! grep -q grill src/issues/spec.ts]",
        "src/issues/alloc.ts and src/commands/issue-file.ts type literals become 'interview' | 'derived' | 'operator'. [oracle: ! grep -q grill src/issues/alloc.ts]",
        "src/commands/issue-file.ts carries no grill token. [oracle: ! grep -q grill src/commands/issue-file.ts]",
        "index.ts specSource type-literal annotations (lines ~2230, ~2284) no longer contain the 'grill' enum literal (prose help strings are out of scope for this story and handled in US-004). [oracle: ! grep -q \"'grill'\" index.ts]",
        "scripts/cam/issues.schema.json specSource enum + description text are renamed to 'interview' (no grill), covering the SCOPE clause 'enum value grill -> interview in schema + code'. [oracle: ! grep -q grill scripts/cam/issues.schema.json]",
        "templates/scripts/cam/issues.schema.json is updated identically (the two copies are byte-identical). [oracle: ! grep -q grill templates/scripts/cam/issues.schema.json]",
        "test/issues/spec.test.ts is updated so isValidSpecSourceEnum accepts 'interview', rejects 'grill', the absent-default resolves to 'interview', and the error-message assertion lists the new triad; the file legitimately retains the string 'grill' only as the rejected-value fixture. [oracle: bun test test/issues/spec.test.ts]",
        "Vendor embed regenerated (never hand-edit src/vendor/_generated.ts) so the embedded issues.schema.json matches on-disk. [oracle: bun run embed-vendor:check]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 1,
      "passes": true,
      "requires": null,
      "notes": "FRONT A. Files: src/issues/types.ts:50, src/issues/spec.ts (:4,:55-56,:69-70,:89,:92-93), src/issues/alloc.ts (:76,:130), src/commands/issue-file.ts:60, index.ts (:2230,:2284 type literals ONLY), scripts/cam/issues.schema.json (:66-67), templates/scripts/cam/issues.schema.json (identical twin), test/issues/spec.test.ts. GOTCHA (spec): enum VALUE is 'interview' (accurate provenance word, non-tautological), NOT 'spec'; surface DOCS use 'spec' but the enum literal is 'interview'. GOTCHA: the absent-default (`specSource === undefined ? 'interview'`) is the LIVE common path, not back-compat; renaming it is the one behavior-touching line. No issue-file data migration (0 files carry explicit specSource:'grill'). issues.schema.json is embedded via templates/ so touching it requires bun run embed-vendor + embed-vendor:check; src/vendor/_generated.ts is AUTOGENERATED, regenerate never hand-edit. Only test/issues/spec.test.ts asserts the enum value (verified: no alloc/issue-file test does), so no other test breaks here. Do NOT touch index.ts prose help strings (US-004) or code comments in other files (US-002)."
    },
    {
      "id": "US-002",
      "title": "Rename residual grill code/test comments in otherwise-untouched files",
      "description": "As a maintainer, I need the leftover 'grill' comment tokens (invisible to typecheck) renamed to 'spec'/'interview' across the code and test files that US-001 did not touch, so that every grill vocabulary token in cam-owned source has an owning story and is machine-verified.",
      "acceptanceCriteria": [
        "src/config/models.ts has no grill comment token. [oracle: ! grep -q grill src/config/models.ts]",
        "src/commands/issue-specify.ts has no grill token (incl the re-grilled/re-grill comments at ~:600 and ~:613). [oracle: ! grep -q grill src/commands/issue-specify.ts]",
        "src/commands/spec.ts has no grill comment token. [oracle: ! grep -q grill src/commands/spec.ts]",
        "src/commands/domain-docs.ts has no grill comment token. [oracle: ! grep -q grill src/commands/domain-docs.ts]",
        "src/supervisor/events.ts has no grill comment token. [oracle: ! grep -q grill src/supervisor/events.ts]",
        "test/integration/issue-specify-ref-only.test.ts has no grill comment token (the re-grill comment at ~:245). [oracle: ! grep -q grill test/integration/issue-specify-ref-only.test.ts]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 2,
      "passes": true,
      "requires": "US-001",
      "notes": "FRONT A residual (auditor findings #2 and #3). Comment-only edits; comments are invisible to typecheck so each file gets its own absence-grep oracle. Files: src/config/models.ts:58, src/commands/issue-specify.ts (:29,:75,:82,:600,:613 -- the :600/:613 re-grill comments are folded HERE, not orphaned to a later story), src/commands/spec.ts:332, src/commands/domain-docs.ts:35, src/supervisor/events.ts:314+316, test/integration/issue-specify-ref-only.test.ts:245. Reword sensibly (grill -> spec/interview, re-grilled -> re-specified, re-grill -> re-spec, 'grill layer'/'grill spec layer' -> 'spec layer'); exact wording is reviewer-judgment but the absence oracle is the gate. These files carry NO vendored /grilling or /domain-modeling token (verified), so blanket absence oracles are safe. requires US-001 so the enum vocabulary is settled before comment vocabulary follows."
    },
    {
      "id": "US-003",
      "title": "Rename skill dir grill-with-docs -> spec-with-docs (both copies) + cam-spec.md + embed + embedded.test.ts",
      "description": "As a maintainer, I need the grill-with-docs skill directory renamed to spec-with-docs in both dual-copies, the cam-spec.md by-path reference and prose updated, the vendor embed regenerated, and test/embedded.test.ts realigned, all in one story so the bun-test and embed-drift gates stay green at the story boundary.",
      "acceptanceCriteria": [
        "The old skill directory is gone in both copies. [oracle: ! test -d .claude/skills/grill-with-docs]",
        "The renamed skill directory exists in both copies. [oracle: test -d .claude/skills/spec-with-docs && test -d templates/skills/spec-with-docs]",
        "SKILL.md name: frontmatter is spec-with-docs in the dogfood copy. [oracle: grep -q 'name: spec-with-docs' .claude/skills/spec-with-docs/SKILL.md]",
        "The vendored by-name calls on SKILL.md line 7 are preserved verbatim. [oracle: grep -q '/grilling' .claude/skills/spec-with-docs/SKILL.md && grep -q '/domain-modeling' .claude/skills/spec-with-docs/SKILL.md]",
        "The two skill copies stay byte-identical after the rename. [oracle: diff -q .claude/skills/spec-with-docs/SKILL.md templates/skills/spec-with-docs/SKILL.md]",
        ".claude/commands/cam-spec.md loads the skill by the new path and carries no grill token (by-path + all prose renamed). [oracle: ! grep -q grill .claude/commands/cam-spec.md]",
        "templates/commands/cam-spec.md is updated identically and carries no grill token. [oracle: ! grep -q grill templates/commands/cam-spec.md]",
        "cam-spec dual-copy parity holds. [oracle: diff -q .claude/commands/cam-spec.md templates/commands/cam-spec.md]",
        "test/embedded.test.ts references the renamed skill (no grill-with-docs remains: describe/test titles, comments, templatesContents keys at ~:182/:556, existsSync paths at ~:484/:553/:598, and the toContain assertions at ~:589/:595 all become spec-with-docs). [oracle: ! grep -q 'grill-with-docs' test/embedded.test.ts]",
        "test/embedded.test.ts leaves the vendored grilling skill assertions untouched. [oracle: grep -q grilling test/embedded.test.ts]",
        "Vendor embed regenerated (never hand-edit _generated.ts); the embedded keys change from skills/grill-with-docs to skills/spec-with-docs and the inlined cam-spec.md updates. [oracle: bun run embed-vendor:check]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 3,
      "passes": true,
      "requires": "US-002",
      "notes": "FRONT B + cam-spec FRONT C, combined per auditor finding #1 to keep the bun-test gate green at the boundary. HARD BOUNDARY: .claude/skills/grilling/ and .claude/skills/domain-modeling/ (and templates twins) are VENDORED upstream (mattpocock/skills) and are NOT renamed; SKILL.md line 7 ('Run a /grilling session, using the /domain-modeling skill') is preserved verbatim. Files: git mv .claude/skills/grill-with-docs -> spec-with-docs (edit SKILL.md name: line 2), git mv templates/skills/grill-with-docs -> spec-with-docs, .claude/commands/cam-spec.md (by-path at :24 + prose at :1,:7,:9,:22,:35,:95,:97), templates/commands/cam-spec.md (identical), src/vendor/_generated.ts (regen), test/embedded.test.ts. GOTCHA cam-dual-copy-is-per-file: the two skill trees are currently byte-identical (verified) -- keep them identical. GOTCHA: embedded.test.ts :493-496 asserts skills count == 5 (domain-modeling 3 + grilling 1 + spec-with-docs 1); the count is UNCHANGED by a rename, do not touch it. embedded.test.ts :181-182 (CAM-145 merit-clause test) uses the templatesContents key 'skills/grill-with-docs/SKILL.md' -> update to spec-with-docs. Combining the skill rename with ALL cam-spec.md edits means the toContain('grill-with-docs') assertions flip to spec-with-docs in the same commit that removes the last grill-with-docs token from cam-spec, so no gate is red mid-story. requires US-002 (linear chain: enum + comments settled first)."
    },
    {
      "id": "US-004",
      "title": "Rename grill vocabulary in index.ts CLI surface strings",
      "description": "As a maintainer, I need the index.ts CLI help/tagline/description strings and the re-grilled comment renamed to spec vocabulary so the CLI surface stops advertising 'grill-with-docs'/'re-grilled' to operators.",
      "acceptanceCriteria": [
        "index.ts carries no grill token anywhere (the type literals were cleaned in US-001; this story cleans the help strings at ~:115,:283,:324,:386 and the comment at ~:1024). [oracle: ! grep -q grill index.ts]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 4,
      "passes": true,
      "requires": "US-003",
      "notes": "FRONT C (index.ts surface). Single file: index.ts prose at :115 ('via grill-with-docs interview'), :283 ('via grill-with-docs'), :324 ('grill-with-docs interview runs inside the pane'), :386 ('re-grilled'), :1024 comment ('re-grilled'). Rename grill-with-docs -> spec-with-docs and re-grilled -> re-specified. The whole-file absence oracle `! grep -q grill index.ts` is valid here because US-001 (transitively required via the US-001<-US-002<-US-003<-US-004 chain) already removed the enum literals; index.ts has no vendored /grilling token. requires US-003 so the spec-with-docs skill name is settled before the CLI strings reference it."
    },
    {
      "id": "US-005",
      "title": "Rename grill vocabulary in recovery-runbook and patterns.md",
      "description": "As a maintainer, I need the recovery-runbook and patterns.md references to the grill flow / grill-with-docs skill renamed to spec vocabulary so the living operator docs match the renamed surface, while the vendored domain-modeling by-path reference is preserved.",
      "acceptanceCriteria": [
        "docs/recovery-runbook.md carries no grill token (section heading, prose ~15 refs, and by-path refs to the skill all updated to spec / spec-with-docs). [oracle: ! grep -q grill docs/recovery-runbook.md]",
        "The vendored domain-modeling by-path reference in recovery-runbook.md is preserved. [oracle: grep -q 'domain-modeling' docs/recovery-runbook.md]",
        "scripts/cam/patterns.md carries no grill token (the grill-with-docs skill-name mention is renamed to spec-with-docs). [oracle: ! grep -q grill scripts/cam/patterns.md]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 5,
      "passes": false,
      "requires": "US-004",
      "notes": "FRONT C (living docs, skill-referencing). Files: docs/recovery-runbook.md (heading :1684, prose :1686-1795, by-path refs :1688,:1833,:1839 to .claude/skills/grill-with-docs/SKILL.md -> spec-with-docs), scripts/cam/patterns.md:582 ('grill-with-docs' skill name -> spec-with-docs). NEITHER file is embedded: the embed maps to the templates/ stubs (templates/scripts/cam/patterns.md has 0 grill, verified), and recovery-runbook is under docs/ not templates/ -- so no embed regen and no dual-copy edit here. recovery-runbook references the vendored domain-modeling skill by path but has NO /grilling token (verified), so the absence oracle is safe. requires US-004 (linear chain; spec-with-docs skill name settled in US-003)."
    },
    {
      "id": "US-006",
      "title": "Rename grill vocabulary in CLAUDE.md, README.md, CONTEXT.md",
      "description": "As a maintainer, I need the conceptual grill references in the cam agent instructions, README, and glossary renamed to spec vocabulary so the last cam-owned surface docs are decoupled from the vendored terminology.",
      "acceptanceCriteria": [
        "scripts/cam/CLAUDE.md carries no grill token (the 'grill layer'/'grill session' mentions in the CAM-64 self-improvement bullet become 'spec layer'/'spec session'). [oracle: ! grep -q grill scripts/cam/CLAUDE.md]",
        "README.md carries no grill token (the 'via grill-with-docs' cam-spec help line becomes spec-with-docs). [oracle: ! grep -q grill README.md]",
        "CONTEXT.md carries no grill token (the '/cam-spec grill interview' glossary entry is reworded). [oracle: ! grep -q grill CONTEXT.md]",
        "Lint spine passes. [oracle: bun run check:all]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]"
      ],
      "priority": 6,
      "passes": false,
      "requires": "US-005",
      "notes": "FRONT C (conceptual living docs). Files: scripts/cam/CLAUDE.md:110 (CAM-64 bullet: 'grill layer', 'grill session'), README.md:135 ('via grill-with-docs'), CONTEXT.md:243 ('/cam-spec grill interview' -> '/cam-spec interview'). The live scripts/cam/CLAUDE.md is NOT the embedded template stub (templates/scripts/cam/CLAUDE.md has 0 grill, verified) so no embed regen and no dual-copy edit. None of these files carry a vendored /grilling or /domain-modeling token (verified), so blanket absence oracles are safe. OUT OF SCOPE (spec): append-only history is untouched -- docs/adr/0001,0002,0012,0033, scripts/cam/journal.md, scripts/cam/journal.archive.md, CHANGELOG.md keep their point-in-time grill references. requires US-005 (linear chain)."
    }
  ]
}
