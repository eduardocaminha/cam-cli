{
  "project": "cam-cli",
  "branchName": "cam/issue-337",
  "description": "Update the stale 'cam-cli' product name in the Splash.tsx line-3 header comment to 'CAM Runtime'.",
  "issueNumber": 337,
  "type": "chore",
  "relatedDocs": [],
  "officialDocsConsulted": [],
  "userStories": [
    {
      "id": "US-001",
      "title": "Refresh stale product name in Splash header comment",
      "description": "As a maintainer, I need the file-header comment of src/ui/Splash.tsx to name the current product 'CAM Runtime' instead of the stale 'cam-cli' so that the source comment matches the product's human-facing display-name.",
      "acceptanceCriteria": [
        "src/ui/Splash.tsx line 3 header comment reads 'Entry / identity panel for CAM Runtime.' and the stale 'cam-cli' product reference is gone. [oracle: grep -q 'Entry / identity panel for CAM Runtime.' src/ui/Splash.tsx]",
        "The line-3 stale phrasing 'Entry / identity panel for cam-cli.' no longer appears anywhere in the file. [oracle: ! grep -q 'Entry / identity panel for cam-cli.' src/ui/Splash.tsx]",
        "The runtime repo slug default url = 'eduardocaminha/cam-cli' (~line 100) is left byte-for-byte unchanged. [oracle: grep -q \"eduardocaminha/cam-cli\" src/ui/Splash.tsx]",
        "No rendered JSX/output is altered; the change is comment-only. [oracle: reviewer-judgment]",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "Lint spine passes (bun run check:all). [oracle: bun run check:all]"
      ],
      "priority": 1,
      "passes": true,
      "requires": null,
      "notes": "File: src/ui/Splash.tsx line 3 only. Pure comment edit, zero runtime/render effect. GOTCHA: line ~100 `url = 'eduardocaminha/cam-cli'` is the real GitHub repo slug rendered at runtime as `v{version} · {url}` and MUST NOT be touched; do NOT run a blind global s/cam-cli/CAM Runtime/ over the file. The Splash panel already renders the 'CAM Runtime' identity in JSX; do not alter any rendered output. No external library touched, so no Step 5.5 doc fetch is required."
    }
  ]
}
