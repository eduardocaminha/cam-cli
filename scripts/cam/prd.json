{
  "project": "Gateship packaged install path",
  "branchName": "cam/issue-460",
  "description": "Publish the first packaged install path for the product: a publishable manifest under the new name, four cross-compiled binaries released as prerelease assets, a curl-able installer, honest install docs, and the executable instruction files migrated to the new typed command.",
  "issueNumber": 460,
  "type": "feat",
  "relatedDocs": [
    "docs/launch-readiness.md",
    "docs/adr/0054-gship-alias-and-the-pre-launch-window-for-internal-contract-renames.md",
    "docs/adr/0055-o-primeiro-release-do-gateship-sai-como-prerelease-para-manter-aberta-a-janela-de-rename-do-adr-0054.md"
  ],
  "officialDocsConsulted": [
    {
      "lib": "Bun",
      "channel": "WebFetch",
      "url": "https://bun.sh/docs/bundler/executables",
      "version": "current",
      "fetchedAt": "2026-08-03T00:00:00Z",
      "summary": "Confirms single-host cross-compilation for standalone executables: bun build --compile --target=bun-<os>-<arch> <entrypoint> --outfile <out>, with bun-darwin-arm64, bun-darwin-x64, bun-linux-x64 and bun-linux-arm64 all listed as supported CompileTarget values. No deprecation affecting the four targets this PRD needs; --outfile is required because --outdir is unsupported under --compile. macOS codesign of the produced binary is documented as the Gatekeeper remedy, matching the ad-hoc re-sign already in scripts/build-release.sh.",
      "status": "ok"
    }
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Tornar o manifesto publicavel sob o nome do produto e sincronizar o lockfile",
      "description": "As a stranger evaluating the project, I need the package manifest to declare a real, publishable package with an executable name, so that the packaging path has a coherent starting point instead of a private manifest with no binary.",
      "acceptanceCriteria": [
        "O manifesto e publicavel sob o nome do produto: private deixa de ser true, name passa a gateship, e bin, files, license, repository e engines passam a existir. [oracle: named-command bash -c 'test \"$(jq -r .private package.json)\" != \"true\" && test \"$(jq -r .bin package.json)\" != \"null\" && test \"$(jq -r .license package.json)\" != \"null\" && test \"$(jq -r .files package.json)\" != \"null\" && test \"$(jq -r .repository package.json)\" != \"null\" && test \"$(jq -r .engines package.json)\" != \"null\" && test \"$(jq -r .name package.json)\" = \"gateship\"'] (ESPECIE: change-detection. Varrido VERMELHO na main 2026-08-01: name=cam-runtime, private=true, e os cinco campos ausentes.)",
        "O mapa bin expoe os dois nomes canonicos decididos pelo ADR-0054. [oracle: named-command bash -c 'test \"$(jq -r .bin.gateship package.json)\" != \"null\" && test \"$(jq -r .bin.gship package.json)\" != \"null\"'] (ESPECIE: change-detection. Varrido VERMELHO na main 2026-08-01: bin ausente, os dois lados null.)",
        "O lockfile acompanha o rename do manifesto no mesmo commit, senao o required check do CI reprova com todos os gates locais verdes. [oracle: named-command bash -c '! grep -qF \"cam-runtime\" bun.lock && grep -qF \"gateship\" bun.lock && bun install --frozen-lockfile'] (ESPECIE: change-detection, forma livre de literal numerico. Varrido VERMELHO na main 2026-08-03 pela orquestradora, exit 1: bun.lock:6 carrega o nome antigo no workspace raiz, 1 ocorrencia, e 0 de gateship. O .github/workflows/ci.yml roda bun install --frozen-lockfile nas linhas 24 e 78 como parte do check obrigatorio, e nenhum gate local roda frozen-lockfile, entao a divergencia entre package.json e bun.lock e invisivel a typecheck, bun test e check:all e so aparece no PR, travando a fase de ship. ATENCAO AO DESENHO DO ORACULO: o oraculo sugerido pelo auditor usava jq sobre bun.lock e foi medido INVALIDO, porque bun.lock nao e JSON parseavel por jq, erro de parse na linha 17; o criterio falharia por erro de comando e nao por assercao. Por isso este usa grep de string fixa mais o proprio frozen-lockfile. Origem: F-03 important do audit round 2 deste issue.)",
        "PIN DE INVARIANCIA: a fonte da versao continua sendo src/version.ts e o manifesto publicavel nao diverge dela, comparando derivado ao vivo dos dois lados e nao congelado. [oracle: named-command bash -c 'test \"$(jq -r .version package.json)\" = \"$(grep -oE \"[0-9]+\\.[0-9]+\\.[0-9]+\" src/version.ts | head -1)\"'] (ESPECIE: pin de invariancia, verde por construcao. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 0 VERDE: os dois lados leem a mesma versao. Varrido VERMELHO contra divergencia simulada. Existe porque tornar o manifesto publicavel cria uma segunda aparencia de fonte-de-versao, e o gotcha do issue exige que as duas nunca divirjam.)",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "A espinha de qualidade passa. [oracle: named-command bun run check:all]"
      ],
      "priority": 1,
      "passes": true,
      "requires": null,
      "notes": "Arquivos: package.json, bun.lock. LICENSE ja existe na raiz, entao o campo license descreve o que ja esta la. O nome canonico e gateship e o alias digitado e gship (ADR-0054, decisao parte 1); o mapa bin expoe os dois. NPM NAO E PUBLICADO NESTE CICLO (ADR-0055): o objetivo e tornar publicavel, nao publicar, entao nao rode nenhum npm publish nem bun publish. index.ts nao tem shebang hoje (linha 1 e comentario), entao se bin apontar para ./index.ts adicione o shebang no mesmo commit ou aponte bin para o entrypoint que o build produz; escolha uma das duas e seja consistente com o campo files. engines deve refletir o runtime real: CLAUDE.md exige Bun >= 1.2 e o toolchain pinado em .bun-version e 1.3.14. A versao continua vindo de src/version.ts (parseada em scripts/build-release.sh:49-54); package.json apenas espelha, nunca vira a fonte. Ordem de trabalho do lockfile: edite package.json, rode bun install (sem --frozen) para regravar o nome do workspace raiz em bun.lock:6, e so entao valide com bun install --frozen-lockfile; .github/workflows/ci.yml roda --frozen-lockfile nas linhas 24 e 78 e nenhum gate local roda, entao a divergencia so apareceria no PR e travaria a fase de ship."
    },
    {
      "id": "US-002",
      "title": "Renomear a superficie digitada do binario (help, usage, --version) e desancorar o gate agents-md do prefixo aposentado",
      "description": "As a newly installed user, I need the binary help and version output to name the command I actually have, so that nothing the product prints teaches me to type a command that no longer exists.",
      "acceptanceCriteria": [
        "O help do binario nao instrui um recem-instalado a digitar o comando aposentado: nem o help de topo nem o help de qualquer subcomando imprime o comando velho, e o nome novo aparece no help de topo. [oracle: named-command bash -c 'bun index.ts --help | grep -q gship || exit 1; for c in init config run plan journal patterns suggestions next review ship tag status; do bun index.ts $c --help | grep -qE \"(^|[^a-zA-Z_/-])cam \" && exit 1; done; ! bun index.ts --help | grep -qE \"(^|[^a-zA-Z_/-])cam \"'] (ESPECIE: change-detection, forma livre de literal numerico. Varrido VERMELHO na main 2026-08-03 pela orquestradora, exit 1: 1 match no help de topo, 22 matches somados nos helps dos doze subcomandos, e 0 ocorrencias de gship. Cumpre a ADR-0054 linha 96, que manda documentation, help output e usage strings migrarem, e que a AC7 nao cobre porque varre arquivos de instrucao e nao o binario. ATENCAO AO DESENHO DO ORACULO: o oraculo sugerido pelo auditor varria SO bun index.ts --help contra a lista de subcomandos e foi medido VERDE na main, ou seja nasceria tautologico, porque o help de topo lista os subcomandos SEM o prefixo e as usage strings vivem nos campos usage: de index.ts, alcancaveis so pelo help de cada subcomando. Este oraculo varre os dois niveis de proposito. Origem: F-02 critical do audit round 2 deste issue.)",
        "O binario tambem se identifica pelo nome novo no --version, nao apenas no help. [oracle: named-command bash -c 'bun index.ts --version | grep -qE \"^gateship [0-9]\"'] (ESPECIE: change-detection, forma livre de literal numerico. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 1 VERMELHO: index.ts:2919 imprime o nome aposentado seguido da versao. Existe porque a AC do help varre apenas --help e o smoke de scripts/build-release.sh compara a saida de --version contra uma string com o nome antigo, entao sem este criterio o nome aposentado sobrevive na unica saida que o instalador novo manda o usuario conferir.)",
        "O gate agents-md continua checando os claims depois do rename: o extrator deixa de fixar o prefixo do comando aposentado, e existe teste durable provando que um comando desconhecido num arquivo varrido ainda produz finding. [oracle: named-command bash -c '! grep -qF \"CAM_CMD_RE = /^cam\" scripts/validate-agents-md.ts && bun test test/validate-agents-md.test.ts -t \"comando desconhecido em arquivo varrido gera finding\"'] (ESPECIE: change-detection, forma livre de literal numerico. Varrido VERMELHO na main 2026-08-03 pela orquestradora, exit 1: scripts/validate-agents-md.ts:221 fixa o extrator em CAM_CMD_RE ancorado no comando velho, e o teste nomeado nao existe. A falsificabilidade do -t foi confirmada a parte: bun test -t com nome inexistente sai 1, entao o criterio nao pode ficar verde por ausencia de teste. Sem este criterio o rename exigido pela AC7 faz 37 dos 52 claims da superficie varrida pararem de ser checados enquanto check:all segue verde, ou seja converte uma guarda shipada em tautologia sem que nenhum oraculo da propria story consiga perceber. O arquivo de teste test/validate-agents-md.test.ts ja existe e e onde o teste novo entra. Origem: F-01 critical do audit round 2 deste issue.)",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam neste ciclo. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, comparando derivado ao vivo e nao congelado. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro. [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done'] (ESPECIE: pin de invariancia, verde por construcao. Varrido VERDE na main 2026-08-01 com CAM_=164, .cam-=153, socket=15, /cam-=205; varrido VERMELHO contra drift simulada. Este pin substitui o AC5 do spec anterior, que pinava TODO rename e por isso contradizia o ADR-0054; aqui o pin proibe so as cinco camadas internas, e permite explicitamente o rename do binario e do comando digitado.)",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "A espinha de qualidade passa. [oracle: named-command bun run check:all]"
      ],
      "priority": 2,
      "passes": true,
      "requires": null,
      "notes": "Arquivos: index.ts, scripts/validate-agents-md.ts, test/validate-agents-md.test.ts, scripts/build-release.sh (duas linhas). O help e centralizado: renderHelp monta um bloco por comando (index.ts:116 topo, depois ~25 blocos ate index.ts:1090) e o guard central --help/-h dispara antes do switch via HELP_REGISTRY (patterns.md:678, CAM-211). O help de topo lista subcomandos SEM prefixo, entao o prefixo aposentado vive nos campos usage: de cada bloco, alcancaveis so pelo help de cada subcomando: e por isso que o oraculo herdado varre os dois niveis. Prosa usa o nome do produto (Gateship), exemplos digitados usam o alias (gship), conforme ADR-0054. O --version imprime em index.ts:2919; ao muda-lo, mova NO MESMO STORY as duas strings esperadas do smoke em scripts/build-release.sh (EXPECTED por volta de :83 e EXPECTED_INST por volta de :139), senao o script quebra por nome e nao por comportamento, e a US-003 herda a falha. O extrator do gate esta em scripts/validate-agents-md.ts:221 (CAM_CMD_RE ancorado no comando velho): desancore para que um comando desconhecido continue virando finding depois do rename, e escreva o teste durable com o nome EXATO 'comando desconhecido em arquivo varrido gera finding' em test/validate-agents-md.test.ts (o -t do bun test sai 1 quando nenhum teste casa, entao o nome tem que bater literalmente). Varra o teste novo VERMELHO contra o extrator atual antes de considera-lo falsificavel. O PIN dos cinco contratos internos esta aqui de proposito: editar help em index.ts e a forma mais provavel de mexer sem querer em /cam-, CAM_ ou .cam-, que este ciclo NAO renomeia (ADR-0054, sequenciamento; o rename interno e o ciclo seguinte). Oraculo de comportamento de CLI invoca bun index.ts <sub>, nunca o binario instalado (regra 5 do bloco curado)."
    },
    {
      "id": "US-003",
      "title": "Compilar os quatro alvos a partir de um host unico e publica-los como assets de um Release marcado prerelease",
      "description": "As a stranger on any of the four supported platforms, I need a tagged build to publish a real binary for my machine, so that an install path exists that does not require me to clone and compile the source.",
      "acceptanceCriteria": [
        "O build produz os quatro alvos, nao so o arm64 da maquina do autor. [oracle: named-command bash -c 'for t in bun-darwin-arm64 bun-darwin-x64 bun-linux-x64 bun-linux-arm64; do grep -rqF \"$t\" scripts/build-release.sh .github/workflows 2>/dev/null || exit 1; done'] (ESPECIE: change-detection, forma livre de literal numerico. Varrido VERMELHO na main 2026-08-01: 1 alvo dos 4, hardcoded em scripts/build-release.sh:63.)",
        "O build roda de ponta a ponta num host unico e os quatro artefatos saem com a arquitetura de maquina certa, nao apenas com o nome de arquivo certo. [oracle: named-command bash -c 'bash scripts/build-release.sh >/dev/null && file dist/gateship-darwin-arm64 | grep -q arm64 && file dist/gateship-darwin-x64 | grep -q x86_64 && file dist/gateship-linux-x64 | grep -q \"x86-64\" && file dist/gateship-linux-arm64 | grep -q aarch64'] (ESPECIE: change-detection comportamental, forma livre de literal numerico. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 1 VERMELHO: a main produz um unico artefato, dist/cam-darwin-arm64, e nenhum dos quatro caminhos gateship-* existe. A planner mediu os quatro cross-compiles de verdade no mesmo dia, num Mac arm64, e inspecionou cada saida com file: darwin-arm64 61 MB Mach-O arm64, darwin-x64 66 MB Mach-O x86_64, linux-x64 98 MB ELF x86-64, linux-arm64 98 MB ELF aarch64. Existe porque um oraculo so de grep no workflow fica verde mesmo quando o asset nunca e produzido, que e exatamente como o round 1 de plano deste issue morreu.)",
        "Existe automacao disparada por tag que publica os binarios como assets de um GitHub Release marcado prerelease. [oracle: named-command bash -c 'f=$(grep -rlE \"gh release create|action-gh-release\" .github/workflows 2>/dev/null); test -n \"$f\" && grep -qE \"^[[:space:]]*tags:\" $f && grep -q prerelease $f'] (ESPECIE: change-detection. Varrido VERMELHO na main 2026-08-01: existe exatamente um workflow, ci.yml, disparado so por push e pull_request na main; zero gatilho por tag e zero passo de release em todo o .github.)",
        "O caminho de instalacao do build cria os dois nomes digitados no destino e e aditivo: o script nunca remove o binario aposentado. [oracle: named-command bash -c 'D=$(mktemp -d); bash scripts/build-release.sh --install \"$D/gateship\" >/dev/null 2>&1 && test -x \"$D/gateship\" && test -x \"$D/gship\" && ! grep -qE \"rm .*(/|[[:space:]])cam([[:space:]]|$)\" scripts/build-release.sh'] (ESPECIE: change-detection comportamental com clausula negativa de nao-destrutividade, forma livre de literal numerico. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 1 VERMELHO: scripts/build-release.sh:130 instala um unico arquivo com o nome aposentado e nunca cria gship. Roda hermetico num mktemp -d, entao prova o mecanismo sem tocar o PATH do operador. Existe porque a instalacao aditiva e restricao medida, nao preferencia: o loop se auto-spawna pelo binario em cinco lugares e perder o binario instalado no meio da branch tira do loop a capacidade de respawnar sidecar, watcher e dashboard.)",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "A espinha de qualidade passa. [oracle: named-command bun run check:all]"
      ],
      "priority": 3,
      "passes": true,
      "requires": null,
      "notes": "Arquivos: scripts/build-release.sh, .github/workflows/<novo>.yml. UM RUNNER SO: bun cross-compila, medido pela planner em 2026-08-03 num Mac arm64 com file confirmando cada saida (darwin-arm64 Mach-O arm64 61 MB, darwin-x64 Mach-O x86_64 66 MB, linux-x64 ELF x86-64 98 MB, linux-arm64 ELF aarch64 98 MB); os quatro juntos levaram menos de um segundo de compile. NAO use matriz de runners e NAO use macos-13: o round 1 deste issue morreu porque essa imagem foi aposentada em 2025-12-08 e o leg darwin-x64 nunca produzia asset enquanto o grep no workflow seguia verde. Docs oficiais confirmam a forma bun build --compile --target=bun-<os>-<arch> <entrypoint> --outfile <saida> e listam os quatro triples (fetch registrado em officialDocsConsulted); o --target de bun build --help NAO documenta esses triples, entao nao conclua ausencia a partir do help. Escolha o runner macOS para o job de Release: codesign so existe la e os dois artefatos darwin precisam da re-assinatura ad-hoc que scripts/build-release.sh:69 e :139 ja fazem; nao tente assinar os ELF. O smoke de --version so pode rodar no artefato nativo do host; para os outros tres verifique a arquitetura com file, nao execute. O teto de 100 MB do script tem folga apertada nos dois alvos linux (98 MB medidos): se ele passar a abortar, trate como sinal real, nao afrouxe sem justificativa. Convencao de nome de artefato, que a US-004 consome: gateship-darwin-arm64, gateship-darwin-x64, gateship-linux-x64, gateship-linux-arm64. O Release e prerelease por decisao registrada no ADR-0055 (mantem aberta a janela de rename do ADR-0054). scripts/check-ci-parity.ts le SOMENTE .github/workflows/ci.yml (CI_YML_PATH), entao um workflow novo de release nao entra na parity e nao deve ser adicionado ao ci.yml. INSTALACAO ADITIVA E RESTRICAO: o --install passa a criar gateship e gship no destino e NUNCA remove o cam ja instalado (o loop se auto-spawna pelo binario em cinco lugares, run.ts:740/822/853/895 e sidecar-liveness-watch.ts:92); o default de DEST em :130 aponta hoje para o nome aposentado. Pattern relevante: patterns.md:322 (soft-check de build-release deve sondar a presenca do prerequisito antes de tolerar exit nao-zero) e patterns.md:244 (toda resposta obrigatoria nova do init exige atualizar o smoke)."
    },
    {
      "id": "US-004",
      "title": "Publicar um instalador curl-avel e documentar a instalacao empacotada com fidelidade sobre a assinatura",
      "description": "As a stranger with only a terminal, I need one curl-able command that installs the released binary and a README that tells the truth about what the signature does and does not guarantee, so that I can get a working command without reading the build scripts.",
      "acceptanceCriteria": [
        "Existe um instalador curl-avel que baixa do Release, instala os dois nomes, e NAO remove o cam existente. [oracle: named-command bash -c 'test -f install.sh && grep -q \"releases/download\" install.sh && grep -q gateship install.sh && grep -q gship install.sh && ! grep -qE \"rm .*(/|[[:space:]])cam([[:space:]]|$)\" install.sh'] (ESPECIE: change-detection com clausula negativa de nao-destrutividade. Varrido VERMELHO na main 2026-08-01: nenhum install.sh existe no repo.)",
        "O instalador e shell valido e mapeia os quatro alvos, nao apenas a maquina do autor. [oracle: named-command bash -c 'test -f install.sh && bash -n install.sh && for a in darwin-arm64 darwin-x64 linux-x64 linux-arm64; do grep -qF \"$a\" install.sh || exit 1; done'] (ESPECIE: change-detection, forma livre de literal numerico. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 1 VERMELHO: nenhum install.sh existe. Existe porque o criterio herdado do instalador nao exige que ele seja parseavel nem que cubra os quatro alvos, e um instalador que so conhece um alvo devolve 404 em todo Mac Intel e em todo Linux, que e a mesma classe de falha do round 1.)",
        "O README documenta a instalacao empacotada e diz a verdade sobre o que a assinatura garante: ad-hoc, NAO notarizada, com o comando de remediacao que o usuario realmente digita. [oracle: named-command bash -c 'grep -q \"releases/download\" README.md && grep -q \"xattr -d com.apple.quarantine\" README.md && grep -qi \"not notarized\" README.md'] (ESPECIE: fidelidade de evidencia, regra 4 do bloco curado. Varrido VERMELHO na main 2026-08-01: zero ocorrencias de releases/download. E varrido VERMELHO tambem contra uma copia FABRICADA plausivel em /tmp que contem releases/download e afirma 'fully notarized by Apple and run without any additional steps': a copia fabricada da exit 1 e a honesta da exit 0, entao o oraculo distingue prosa inventada de prosa fiel, nao apenas presenca de token.)",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "A espinha de qualidade passa. [oracle: named-command bun run check:all]"
      ],
      "priority": 4,
      "passes": false,
      "requires": null,
      "notes": "Arquivos: install.sh (novo, na raiz), README.md. O instalador detecta os/arch e baixa de https://github.com/eduardocaminha/cam-cli/releases/download/<tag>/<asset>, com os nomes de asset fixados pela US-003: gateship-darwin-arm64, gateship-darwin-x64, gateship-linux-x64, gateship-linux-arm64. Instala em diretorio user-owned (default ~/.local/bin, sem sudo) e cria os DOIS nomes, gateship e gship. NUNCA remove nem sobrescreve um cam existente: a remocao e passo deliberado do operador depois do ciclo (ADR-0055, ultima consequencia). Um binario BAIXADO recebe a quarentena do Gatekeeper, diferente de um compilado local, entao o texto tem que trazer o comando que o usuario realmente digita: xattr -d com.apple.quarantine <caminho>. Sem conta Apple Developer (decisao do operador 2026-08-01) os binarios saem ad-hoc assinados e NAO notarizados; o README ja carrega a ressalva honesta nas linhas 62-67 (Public distribution note), que deve ser ESTENDIDA e nunca contradita. O oraculo herdado do README foi desenhado para distinguir prosa fiel de prosa inventada: uma copia fabricada afirmando notarizacao completa da exit 1. Nao escreva que da para instalar via npm: o manifesto fica publicavel mas NAO e publicado neste ciclo (ADR-0055). A secao Install atual do README documenta so o caminho from-source e deve passar a liderar com o caminho empacotado, mantendo o from-source como alternativa. README.md nao esta em scripts/file-size-budget.json, entao nao ha teto a levantar."
    },
    {
      "id": "US-005",
      "title": "Migrar os arquivos de instrucao executavel para o comando novo e regenerar o blob embutido",
      "description": "As the autonomous loop, I need every executable instruction file to name the command that now exists, so that an agent following those instructions types something the machine can run.",
      "acceptanceCriteria": [
        "Nenhum arquivo de instrucao EXECUTAVEL manda digitar cam como comando de shell. [oracle: named-command bash -c '! grep -rqE \"(^|[^a-zA-Z_/-])cam (run|stop|next|plan|ship|review|issue|spec|sidecar|resume|tag|journal|suggestions|config|patterns|decide|init|prune|claude|orch-resolve)( |$)\" .claude/agents .claude/commands .claude/skills .claude/hooks templates scripts/cam/CLAUDE.md'] (ESPECIE: change-detection, forma livre de literal numerico. Varrido VERMELHO na main 2026-08-01: 17 arquivos. O escopo do oraculo EXCLUI deliberadamente journal.md, journal.archive.md, patterns.md e patterns.archive.md, que somam 445 linhas casantes: reescrever historico falsifica o registro e o ADR-0054 ja proibe renomear historico. Um grep repo-wide aqui seria conjuntamente insatisfazivel, que e exatamente o modo de falha do CAM-434.)",
        "O blob embutido acompanha os templates: nenhuma linha do vendor gerado manda digitar o comando aposentado. [oracle: named-command bash -c '! grep -qE \"(^|[^a-zA-Z_/-])cam (run|next|plan|ship|review|issue|spec|journal|patterns|suggestions|prune)( |$)\" src/vendor/_generated.ts'] (ESPECIE: change-detection, forma livre de literal numerico. CRITERIO AUTORAL, varrido pela planner na main 2026-08-03, exit 1 VERMELHO: 10 linhas casantes em src/vendor/_generated.ts. Existe porque o oraculo herdado dos arquivos de instrucao nao varre src, entao sem este criterio o prompt de agente realmente embarcado no binario continuaria mandando digitar o comando aposentado enquanto os arquivos-fonte ja migraram.)",
        "Vendor drift check passes (bun run embed-vendor:check). [oracle: bun run embed-vendor:check]",
        "PIN DE INVARIANCIA: a prosa historica nao e reescrita. Os quatro arquivos de historico mantem a contagem que a main tem, com o comparando derivado ao vivo dos dois lados. A vivacidade do extrator e provada pelo codigo de saida de grep -q, nao por comparacao contra inteiro. [oracle: named-command bash -c 'S=\"run|stop|next|plan|ship|review|issue|spec|sidecar|tag|journal|suggestions|config|patterns|decide|init|prune\"; for f in scripts/cam/journal.md scripts/cam/journal.archive.md scripts/cam/patterns.md scripts/cam/patterns.archive.md; do git show main:$f | grep -qE \"(^|[^a-zA-Z_/-])cam ($S)( |$)\" || exit 1; b=$(git show main:$f | grep -cE \"(^|[^a-zA-Z_/-])cam ($S)( |$)\"); a=$(grep -cE \"(^|[^a-zA-Z_/-])cam ($S)( |$)\" $f); test \"$b\" = \"$a\" || exit 1; done'] (ESPECIE: pin de invariancia, verde por construcao. Varrido VERDE na main 2026-08-01; varrido VERMELHO contra extrator morto e VERMELHO contra drift simulada, entao a vivacidade continua garantida sem nenhum literal inteiro.)",
        "PIN DE INVARIANCIA: os cinco contratos internos do ADR-0054 NAO renomeiam neste ciclo. As contagens de CAM_, .cam-, do socket tmux -L cam e de /cam- em src mais index.ts ficam identicas as da main, comparando derivado ao vivo e nao congelado. A vivacidade do extrator e provada pelo codigo de saida de git grep -q, nao por comparacao contra inteiro. [oracle: named-command bash -c 'for pat in \"CAM_\" \"\\.cam-\" \"tmux -L cam|\\-L., ?.cam\" \"/cam-\"; do git grep -qE \"$pat\" main -- src index.ts || exit 1; b=$(git grep -cE \"$pat\" main -- src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); a=$(grep -rcE \"$pat\" src index.ts | awk -F: \"{s+=\\$NF} END{print s}\"); test \"$b\" = \"$a\" || exit 1; done'] (ESPECIE: pin de invariancia, verde por construcao. Varrido VERDE na main 2026-08-01 com CAM_=164, .cam-=153, socket=15, /cam-=205; varrido VERMELHO contra drift simulada. Este pin substitui o AC5 do spec anterior, que pinava TODO rename e por isso contradizia o ADR-0054; aqui o pin proibe so as cinco camadas internas, e permite explicitamente o rename do binario e do comando digitado.)",
        "Typecheck passes (bun run typecheck). [oracle: bun run typecheck]",
        "Tests pass (bun test). [oracle: bun test]",
        "A espinha de qualidade passa. [oracle: named-command bun run check:all]"
      ],
      "priority": 5,
      "passes": false,
      "requires": null,
      "notes": "17 arquivos, com contagem medida pela planner em 2026-08-03: .claude/agents/subagent-orchestrator.md 20, templates/agents/subagent-orchestrator.md 19, templates/commands/cam-spec.md 16, .claude/commands/cam-spec.md 16, templates/commands/cam-issue.md 7, .claude/commands/cam-issue.md 7, templates/commands/cam-next.md 3, .claude/commands/cam-next.md 3, templates/scripts/cam/orch-handoff.schema.json 2, templates/commands/cam-prune.md 2, scripts/cam/CLAUDE.md 2, .claude/commands/cam-prune.md 2, templates/scripts/cam/journal.md 1, templates/scripts/cam/issues.schema.json 1, templates/scripts/cam/CLAUDE.md 1, templates/commands/cam-plan.md 1, .claude/commands/cam-plan.md 1. TEMPLATES SAO FONTE E O EMBED E DERIVADO: depois de editar templates/ rode bun run embed-vendor, senao embed-vendor --check reprova dentro do check:all (scripts/check-all.ts:79). ATENCAO ao par .claude/ e templates/: os dois lados NAO sao byte-identicos por padrao (memoria cam-dual-copy-is-per-file), entao diffe antes de assumir espelhamento. SO O COMANDO DIGITADO MUDA: os nomes de arquivo (cam-spec.md, cam-next.md) e os slash-commands /cam-* ficam, assim como CAM_, .cam- e o socket tmux -L cam, todos pinados pela AC de invariancia dos cinco contratos internos. NAO TOQUE em scripts/cam/journal.md, journal.archive.md, patterns.md nem patterns.archive.md: sao historico, somam 445 linhas casantes e estao pinados pela AC de invariancia historica; reescrever historico falsifica o registro e o ADR-0054 ja proibe. SEQUENCIA OPERACIONAL, e a razao de esta story ser a ultima: a partir do commit dela o orquestrador vivo passa a ser instruido a DIGITAR o nome novo, entao rode tambem, nesta story, bash scripts/build-release.sh --install (que a US-003 tornou aditivo e de dois nomes) para que gateship e gship existam no PATH antes que o loop releia essas instrucoes num respawn; jamais remova ~/.local/bin/cam, porque e por ele que o loop respawna sidecar, watcher e dashboard no meio desta mesma branch."
    }
  ]
}
