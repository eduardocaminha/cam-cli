{
	"lanes": {
		"host": {
			"expectedSkips": 0,
			"passFloor": 5720,
			"measuredAt": "2026-07-27",
			"measuredVia": "full-PATH `bun test` on cam/issue-424 (US-006, CAM-424), AFTER this story's own 4 new tests (test/test-in-container.test.ts: 1 skip-mismatch case + 3 buildContainerTestExecArgv cases): 5720 pass / 0 fail / no skip-summary-line printed (Bun omits the line entirely at zero)."
		},
		"container": {
			"expectedSkips": 40,
			"passFloor": 5680,
			"measuredAt": "2026-07-27",
			"measuredVia": "FINALIZED (US-006) on real Docker locally: `bun scripts/test-in-container.ts` (waiver wired via `docker exec -e CAM_TEST_WAIVERS=tmux,pgrep,uuidgen`) measured 5680 pass / 0 fail / 40 skip, exit 0 (skip count matches this recorded expectation). Cross-checked by directly running `bun test` inside the container WITHOUT the waiver (`docker exec cam-worker sh -lc 'cd /workspace && bun test'`): 5677 pass / 3 fail / 40 skip, real exit code 1 -- the 3 fails are exactly US-002's preflight tests for tmux/pgrep/uuidgen (absent in oven/bun:1.2-slim), confirming the waiver is what flips those 3 tests from fail to pass (5680 = 5677 + 3) while leaving the skip count (40) unaffected (the individual skipIf gates on those same binaries already skip independently of the waiver)."
		}
	},
	"triage": {
		"hardDependency": 42,
		"legitimateEnvironmental": 1,
		"measuredVia": "distinct test/** files (excluding test/helpers/ and test/fixtures/) that `import { xAvailable } from '.../test-deps.ts'`, bucketed by that binary's DepClassification in test/helpers/test-deps.ts. hardDependency = 42 distinct files across the union of tmux/git/jq/pgrep/uuidgen/bun (some files import more than one hard-dependency binding, e.g. test/integration/orch-recycle-watch.test.ts imports tmux+pgrep+uuidgen). legitimateEnvironmental = 1 file (test/integration/orch-recycle-pid-resolve.test.ts, the sole importer of psAvailable)."
	}
}
