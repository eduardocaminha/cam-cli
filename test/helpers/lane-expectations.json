{
	"lanes": {
		"host": {
			"expectedSkips": 0,
			"passFloor": 5500,
			"measuredAt": "2026-07-28",
			"measuredVia": "RE-MEASURED (US-001, CAM-447 PRD) on cam/issue-447 at branch HEAD on 2026-07-28, via full-PATH `bun test`: 5761 pass / 0 fail / 0 skip (Bun omits the ' N skip' summary line at zero; recorded explicitly per the token form this file's own contract requires). passFloor rounded down to the nearest multiple of 100 leaving a 261-pass gap, comfortably clear of MIN_PASS_FLOOR_HEADROOM (50), so the floor does not need re-recording again next cycle."
		},
		"container": {
			"expectedSkips": 40,
			"passFloor": 5500,
			"measuredAt": "2026-07-28",
			"measuredVia": "RE-MEASURED (US-002, CAM-447 PRD) on cam/issue-447 at branch HEAD on 2026-07-28, via real Docker locally, command `bun scripts/test-in-container.ts` (waiver wired via `docker exec -e CAM_TEST_WAIVERS=tmux,pgrep,uuidgen` against the running cam-worker container): 5721 pass / 0 fail / 40 skip, exit 0 (skip count matches this recorded expectation). passFloor rounded down to the nearest multiple of 100 leaving a 221-pass gap, comfortably clear of MIN_PASS_FLOOR_HEADROOM (50), so the floor does not need re-recording again next cycle."
		}
	},
	"triage": {
		"hardDependency": 42,
		"legitimateEnvironmental": 1,
		"measuredVia": "distinct test/** files (excluding test/helpers/ and test/fixtures/) that `import { xAvailable } from '.../test-deps.ts'`, bucketed by that binary's DepClassification in test/helpers/test-deps.ts. hardDependency = 42 distinct files across the union of tmux/git/jq/pgrep/uuidgen/bun (some files import more than one hard-dependency binding, e.g. test/integration/orch-recycle-watch.test.ts imports tmux+pgrep+uuidgen). legitimateEnvironmental = 1 file (test/integration/orch-recycle-pid-resolve.test.ts, the sole importer of psAvailable)."
	}
}
