node_modules/
dist/
out/
*.tgz
.*.bun-build

coverage/
*.lcov
gate-results.json

.env
.env.*.local
*.log
*.tsbuildinfo
.cache/

.idea/
.cursor/
.DS_Store
Refactor*.md

# The UI bundle is a build output, produced by the `prepare` script on install
# and by `build:release`. It is never committed: a committed copy can only ever
# be older than webui/src.
webui/dist/

# Local runtime and test state.
.gship/
.claude/
.gship-test-tmp/

# Optional, local-only Claude Code source reference.
claude-code-harness/

# Operator-licensed font, local only: the Displaay license does not grant
# redistribution, so this file must never enter the repository or a release.
webui/public/fonts/SaansVF.woff2
